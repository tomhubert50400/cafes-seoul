---
phase: 11-user-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/page.tsx
  - src/app/dashboard/layout.tsx
  - src/components/dashboard/user-stats.tsx
  - src/components/dashboard/status-badge.tsx
  - src/lib/constants/routes.ts
  - src/components/auth/user-menu.tsx
  - src/lib/i18n/translations.ts
autonomous: true

must_haves:
  truths:
    - "User sees total count of submitted cafes"
    - "User sees total count of ratings given with average"
    - "User sees total count of uploaded photos"
    - "Dashboard accessible via user menu"
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Dashboard page with parallel data fetching"
      min_lines: 80
    - path: "src/components/dashboard/user-stats.tsx"
      provides: "Mini stats card component"
      min_lines: 25
    - path: "src/components/dashboard/status-badge.tsx"
      provides: "Status badge component for items"
      min_lines: 20
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "supabase queries"
      via: "Promise.all parallel fetching"
      pattern: "Promise\\.all"
    - from: "src/components/auth/user-menu.tsx"
      to: "/dashboard"
      via: "Link component"
      pattern: "href.*dashboard"
---

<objective>
Create the user dashboard page with statistics aggregation and navigation entry point.

Purpose: Enable users to see summary of their contributions (DASH-04) and access the dashboard from profile menu.
Output: Dashboard page at /dashboard with stats cards, route constant, user menu link, and i18n translations.
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-user-dashboard/11-CONTEXT.md
@.planning/phases/11-user-dashboard/11-RESEARCH.md

# Existing patterns to follow
@src/app/admin/page.tsx
@src/components/admin/admin-stats.tsx
@src/components/auth/user-menu.tsx
@src/lib/constants/routes.ts
@src/lib/i18n/translations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard route with layout and auth protection</name>
  <files>
    src/app/dashboard/layout.tsx
    src/app/dashboard/page.tsx
  </files>
  <action>
Create dashboard route structure:

1. `src/app/dashboard/layout.tsx`:
   - Server component that checks auth via `createClient().auth.getUser()`
   - If no user, redirect to `/login?redirect=/dashboard`
   - Simple layout wrapper (container, padding) - NOT admin-style sidebar
   - Export metadata with title "My Contributions"

2. `src/app/dashboard/page.tsx`:
   - Server component with parallel data fetching using Promise.all
   - Fetch 6 queries in parallel (pattern from admin/page.tsx):
     - Count cafe_submissions by user_id (head: true for efficiency)
     - Count cafe_ratings by user_id
     - Count photos by user_id
     - Recent 5 cafe_submissions with status, rejection_reason
     - Recent 5 cafe_ratings with overall score and cafe join (cafe:cafes(id, name, slug))
     - Recent 5 photos with status, upvote_count, cafe join
   - Calculate average rating: ratings.reduce sum / length, use .toFixed(1)
   - Handle edge case: avgRating = null if no ratings
   - Page title: "My Contributions" (from translations)
   - Render three sections: Cafes, Ratings, Photos
   - Each section has UserStats card above list placeholder (lists in Plan 02)
   - Include empty state for new users (all counts = 0): welcome message with CTAs to browse/submit

Use language from cookies (pattern from admin/page.tsx getLanguageFromCookies).
Import and use getTranslation for all user-facing text.
  </action>
  <verify>
    - `npm run build` compiles without errors
    - Visit /dashboard while logged in shows page with three sections
    - Visit /dashboard while logged out redirects to /login
  </verify>
  <done>Dashboard page renders with stats for current user, protected by auth</done>
</task>

<task type="auto">
  <name>Task 2: Create UserStats and StatusBadge components</name>
  <files>
    src/components/dashboard/user-stats.tsx
    src/components/dashboard/status-badge.tsx
  </files>
  <action>
Create reusable dashboard components:

1. `src/components/dashboard/user-stats.tsx`:
   - Props: icon (Lucide component type), title (string), count (number), metric (optional string like "avg 4.2★")
   - Layout: horizontal flex with icon circle on left, title/count on right
   - Icon in 48px rounded-full bg-muted container
   - Count as text-2xl font-bold
   - Optional metric as text-sm text-muted-foreground next to count
   - Use Tailwind for styling (consistent with admin-stats.tsx aesthetic)
   - Pattern from RESEARCH.md Pattern 2

2. `src/components/dashboard/status-badge.tsx`:
   - Props: status ('pending' | 'approved' | 'declined' | 'rejected'), label (string)
   - Color variants:
     - pending: bg-yellow-100 text-yellow-800 dark:bg-yellow-900 dark:text-yellow-200
     - approved: bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-200
     - declined/rejected: bg-red-100 text-red-800 dark:bg-red-900 dark:text-red-200
   - Rounded-full px-2.5 py-0.5 text-xs font-medium
   - Pattern from RESEARCH.md Pattern 3

Both are client components (no state needed but may need 'use client' for tree shaking).
Actually, make them server components - no interactivity needed.
  </action>
  <verify>
    - Components import without TypeScript errors
    - Storybook or page renders components correctly
  </verify>
  <done>UserStats and StatusBadge components exist and display correctly</done>
</task>

<task type="auto">
  <name>Task 3: Add dashboard route and user menu link with i18n</name>
  <files>
    src/lib/constants/routes.ts
    src/components/auth/user-menu.tsx
    src/lib/i18n/translations.ts
  </files>
  <action>
Integrate dashboard into navigation:

1. `src/lib/constants/routes.ts`:
   - Add DASHBOARD: '/dashboard' to ROUTES object (in User section)

2. `src/components/auth/user-menu.tsx`:
   - Add new menu item after Profile link (before My Reviews):
     ```tsx
     <DropdownMenuItem asChild>
       <Link href={ROUTES.DASHBOARD} className="flex cursor-pointer items-center gap-2">
         <LayoutDashboard className="h-4 w-4" />
         {t('nav.contributions')}
       </Link>
     </DropdownMenuItem>
     ```
   - Import LayoutDashboard from lucide-react
   - Import ROUTES.DASHBOARD

3. `src/lib/i18n/translations.ts`:
   - Add translations for all 5 languages (en, ko, fr, zh, vi):

   English (en):
   - 'nav.contributions': 'My Contributions'
   - 'dashboard.title': 'My Contributions'
   - 'dashboard.welcome.title': 'Welcome to your dashboard!'
   - 'dashboard.welcome.subtitle': 'Track your contributions to the Seoul Cafes community'
   - 'dashboard.welcome.browse': 'Browse cafes to rate'
   - 'dashboard.welcome.submit': 'Submit a cafe'
   - 'dashboard.cafes.title': 'Cafe Submissions'
   - 'dashboard.ratings.title': 'Ratings Given'
   - 'dashboard.ratings.avg': 'avg'
   - 'dashboard.photos.title': 'Photos Uploaded'
   - 'dashboard.status.pending': 'Pending'
   - 'dashboard.status.approved': 'Approved'
   - 'dashboard.status.declined': 'Declined'
   - 'dashboard.status.rejected': 'Rejected'

   Korean (ko):
   - 'nav.contributions': '내 기여'
   - 'dashboard.title': '내 기여'
   - 'dashboard.welcome.title': '대시보드에 오신 것을 환영합니다!'
   - 'dashboard.welcome.subtitle': '서울 카페 커뮤니티에 대한 기여를 추적하세요'
   - 'dashboard.welcome.browse': '카페 둘러보기'
   - 'dashboard.welcome.submit': '카페 제출하기'
   - 'dashboard.cafes.title': '카페 제출'
   - 'dashboard.ratings.title': '평가'
   - 'dashboard.ratings.avg': '평균'
   - 'dashboard.photos.title': '업로드된 사진'
   - 'dashboard.status.pending': '대기 중'
   - 'dashboard.status.approved': '승인됨'
   - 'dashboard.status.declined': '거절됨'
   - 'dashboard.status.rejected': '거부됨'

   Add equivalent translations for fr, zh, vi following the pattern.
  </action>
  <verify>
    - Route constant exports DASHBOARD
    - User menu shows "My Contributions" link when logged in
    - Clicking link navigates to /dashboard
    - All 5 languages have dashboard translations
  </verify>
  <done>Dashboard accessible from user menu with proper i18n support</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` succeeds
2. Log in and click user avatar - "My Contributions" appears in menu
3. Click "My Contributions" - navigates to /dashboard
4. Dashboard shows three stats cards (Cafes, Ratings, Photos) with counts
5. If user has ratings, average rating displays (e.g., "avg 4.2★")
6. If new user (no contributions), welcome message with CTAs appears
7. Log out and visit /dashboard - redirects to /login
8. Switch language - dashboard labels update correctly
</verification>

<success_criteria>
- Dashboard page exists at /dashboard with auth protection
- Stats show cafe count, rating count (with average), photo count
- User menu has "My Contributions" link to dashboard
- All translations present for 5 languages
- Empty state for new users with CTAs
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-dashboard/11-01-SUMMARY.md`
</output>
