---
phase: 11-user-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/dashboard/submissions-list.tsx
  - src/components/dashboard/ratings-list.tsx
  - src/components/dashboard/photos-list.tsx
  - src/app/api/dashboard/submissions/route.ts
  - src/app/api/dashboard/ratings/route.ts
  - src/app/api/dashboard/photos/route.ts
  - src/app/dashboard/page.tsx
  - src/lib/i18n/translations.ts
autonomous: true

must_haves:
  truths:
    - "User sees list of their submitted cafes with status (DASH-01)"
    - "User sees list of their ratings with cafe name and overall score (DASH-02)"
    - "User sees list of their uploaded photos with thumbnails (DASH-03)"
    - "User can load more items beyond initial 5"
    - "Rejected items show rejection reason on expand"
    - "Pending submissions have Edit/Delete actions"
  artifacts:
    - path: "src/components/dashboard/submissions-list.tsx"
      provides: "Cafe submissions list with status and actions"
      min_lines: 60
    - path: "src/components/dashboard/ratings-list.tsx"
      provides: "Ratings list with cafe links"
      min_lines: 50
    - path: "src/components/dashboard/photos-list.tsx"
      provides: "Photos grid with thumbnails"
      min_lines: 50
    - path: "src/app/api/dashboard/submissions/route.ts"
      provides: "API for loading more submissions"
      min_lines: 25
  key_links:
    - from: "src/components/dashboard/submissions-list.tsx"
      to: "/api/dashboard/submissions"
      via: "fetch for load more"
      pattern: "fetch.*api/dashboard/submissions"
    - from: "src/components/dashboard/ratings-list.tsx"
      to: "/cafes/[slug]"
      via: "Link to cafe detail"
      pattern: "href.*cafes/"
    - from: "src/app/dashboard/page.tsx"
      to: "list components"
      via: "component imports"
      pattern: "SubmissionsList|RatingsList|PhotosList"
---

<objective>
Build contribution list components for cafe submissions, ratings, and photos with load more functionality.

Purpose: Enable users to view their contributions (DASH-01, DASH-02, DASH-03) with proper status display and actions.
Output: Three list components with load more, API endpoints for pagination, integrated into dashboard page.
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-user-dashboard/11-CONTEXT.md
@.planning/phases/11-user-dashboard/11-RESEARCH.md
@.planning/phases/11-user-dashboard/11-01-SUMMARY.md

# Components to build on
@src/app/dashboard/page.tsx
@src/components/dashboard/user-stats.tsx
@src/components/dashboard/status-badge.tsx

# Existing patterns
@src/components/submissions/my-submissions-list.tsx
@src/components/photos/photo-gallery.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SubmissionsList component with actions</name>
  <files>
    src/components/dashboard/submissions-list.tsx
    src/app/api/dashboard/submissions/route.ts
  </files>
  <action>
Create submissions list with status display and pending actions:

1. `src/components/dashboard/submissions-list.tsx`:
   - 'use client' - needs state for load more and expandable reasons
   - Props: submissions (array), totalCount (number), userId (string), translations (object)
   - Use StatusBadge component for status display
   - Each item shows:
     - Cafe name (name.en || name.ko)
     - Address (truncated)
     - StatusBadge
     - Created date (formatted)
   - For declined items:
     - Expandable rejection reason (ChevronDown/Up toggle)
     - Show reason in red-tinted box when expanded
   - For pending items:
     - Edit button -> Link to /profile/submissions/[id]/edit (existing route)
     - Delete button -> calls delete action with confirmation
   - Load More button if items.length < totalCount
   - Empty state: "No cafe submissions yet" with CTA to /map (submit cafe)
   - Pattern from RESEARCH.md Pattern 4 (load more) and Pattern 5 (progressive disclosure)

2. `src/app/api/dashboard/submissions/route.ts`:
   - GET handler with query params: offset, limit
   - Auth check via createClient().auth.getUser()
   - Query cafe_submissions filtered by user_id
   - Select: id, name, address, status, created_at, rejection_reason
   - Order by created_at desc
   - Apply offset and limit
   - Return JSON array

Delete action: use existing deleteSubmission Server Action from @src/app/actions/submissions.ts
Import and call with submission.id, show toast on success/error via sonner.
  </action>
  <verify>
    - Component renders submissions with correct status badges
    - Pending submissions show Edit/Delete buttons
    - Declined submissions expand to show rejection reason
    - Load More fetches additional submissions
    - Empty state shows when no submissions
  </verify>
  <done>Submissions list displays user's cafes with status and actions (DASH-01)</done>
</task>

<task type="auto">
  <name>Task 2: Create RatingsList and PhotosList components</name>
  <files>
    src/components/dashboard/ratings-list.tsx
    src/components/dashboard/photos-list.tsx
    src/app/api/dashboard/ratings/route.ts
    src/app/api/dashboard/photos/route.ts
  </files>
  <action>
Create ratings and photos list components:

1. `src/components/dashboard/ratings-list.tsx`:
   - 'use client' for load more state
   - Props: ratings (array), totalCount (number), userId (string), translations (object)
   - Each item shows:
     - Cafe name as Link to /cafes/[slug]
     - Overall rating with star icon (e.g., "4★")
     - Created date
   - Clickable row - entire item links to cafe detail
   - Load More button pattern
   - Empty state: "No ratings yet" with CTA "Browse cafes to rate" -> /cafes
   - No edit action (per CONTEXT.md - link to cafe page instead)

2. `src/components/dashboard/photos-list.tsx`:
   - 'use client' for load more and delete state
   - Props: photos (array), totalCount (number), userId (string), translations (object)
   - Grid layout (CSS columns or grid) for thumbnails
   - Each item shows:
     - Thumbnail image (use Supabase storage transform width=200)
     - StatusBadge overlay
     - Upvote count (heart icon + number)
     - Cafe name below
   - For pending photos: Delete button (trash icon)
   - For rejected photos: Show rejection reason on hover (title attribute) or expandable
   - Load More button
   - Empty state: "No photos uploaded" with CTA to /cafes

3. API routes:

   `src/app/api/dashboard/ratings/route.ts`:
   - GET with offset, limit params
   - Auth check
   - Query cafe_ratings by user_id
   - Select: id, overall, created_at, cafe:cafes(id, name, slug)
   - Order by created_at desc
   - Return JSON

   `src/app/api/dashboard/photos/route.ts`:
   - GET with offset, limit params
   - Auth check
   - Query photos by user_id
   - Select: id, storage_path, status, upvote_count, created_at, rejection_reason, cafe:cafes(name)
   - Order by created_at desc
   - Return JSON

Use deletePhoto Server Action from existing actions for photo deletion.
  </action>
  <verify>
    - Ratings list shows cafe names with links and overall scores
    - Photos grid displays thumbnails with status badges
    - Load More works for both components
    - Pending photos have delete button
    - Empty states show appropriate CTAs
  </verify>
  <done>Ratings and photos lists display user contributions (DASH-02, DASH-03)</done>
</task>

<task type="auto">
  <name>Task 3: Integrate lists into dashboard and add remaining translations</name>
  <files>
    src/app/dashboard/page.tsx
    src/lib/i18n/translations.ts
  </files>
  <action>
Integrate list components and complete i18n:

1. Update `src/app/dashboard/page.tsx`:
   - Import SubmissionsList, RatingsList, PhotosList
   - Pass fetched data to each component:
     - SubmissionsList: submissions, totalSubmissions, user.id, translations
     - RatingsList: ratings, totalRatings, user.id, translations
     - PhotosList: photos, totalPhotos, user.id, translations
   - Build translations object from getTranslation calls for each section
   - Ensure sections are properly spaced (space-y-8 or similar)
   - Keep UserStats cards above each list (already in Plan 01)

2. Add remaining translations to `src/lib/i18n/translations.ts` (all 5 languages):

   English (en):
   - 'dashboard.cafes.empty': 'No cafe submissions yet'
   - 'dashboard.cafes.emptyCta': 'Submit a cafe'
   - 'dashboard.ratings.empty': 'No ratings yet'
   - 'dashboard.ratings.emptyCta': 'Browse cafes to rate'
   - 'dashboard.photos.empty': 'No photos uploaded'
   - 'dashboard.photos.emptyCta': 'Add photos to cafes'
   - 'dashboard.loadMore': 'Load More'
   - 'dashboard.remaining': 'remaining'
   - 'dashboard.showReason': 'Show reason'
   - 'dashboard.hideReason': 'Hide reason'
   - 'dashboard.rejectionReason': 'Rejection reason'
   - 'dashboard.actions.edit': 'Edit'
   - 'dashboard.actions.delete': 'Delete'
   - 'dashboard.actions.deleteConfirm': 'Are you sure you want to delete this?'
   - 'dashboard.upvotes': 'upvotes'

   Korean (ko):
   - 'dashboard.cafes.empty': '아직 제출된 카페가 없습니다'
   - 'dashboard.cafes.emptyCta': '카페 제출하기'
   - 'dashboard.ratings.empty': '아직 평가가 없습니다'
   - 'dashboard.ratings.emptyCta': '카페 둘러보기'
   - 'dashboard.photos.empty': '업로드된 사진이 없습니다'
   - 'dashboard.photos.emptyCta': '카페에 사진 추가'
   - 'dashboard.loadMore': '더 보기'
   - 'dashboard.remaining': '개 남음'
   - 'dashboard.showReason': '사유 보기'
   - 'dashboard.hideReason': '사유 숨기기'
   - 'dashboard.rejectionReason': '거절 사유'
   - 'dashboard.actions.edit': '수정'
   - 'dashboard.actions.delete': '삭제'
   - 'dashboard.actions.deleteConfirm': '정말 삭제하시겠습니까?'
   - 'dashboard.upvotes': '추천'

   Add equivalent translations for fr, zh, vi.
  </action>
  <verify>
    - Dashboard page renders all three list components
    - Each section shows UserStats card above its list
    - All text is translated (switch language to verify)
    - Load more loads additional items in each section
    - Empty states appear when user has no contributions
  </verify>
  <done>Dashboard fully functional with all contribution lists and i18n (DASH-01, DASH-02, DASH-03, DASH-04)</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` succeeds
2. Log in as user with contributions:
   - Cafe submissions show with correct status badges
   - Pending submissions have Edit/Delete buttons
   - Declined submissions expand to show rejection reason
   - Ratings show cafe name (clickable) + overall score
   - Photos show thumbnails with status and upvote count
   - Load More works in each section
3. Log in as user with no contributions:
   - Each section shows empty state with CTA
4. Test delete on pending submission:
   - Confirmation prompt appears
   - Item removed after confirmation
   - Toast shows success message
5. Test delete on pending photo:
   - Same flow as submission
6. Click cafe name in ratings list:
   - Navigates to cafe detail page
7. Switch language:
   - All labels, buttons, empty states update
</verification>

<success_criteria>
- DASH-01: User sees submitted cafes with status (pending/approved/declined)
- DASH-02: User sees ratings with cafe names and overall scores
- DASH-03: User sees photos with thumbnails and status
- DASH-04: Stats show counts (from Plan 01) + lists show details
- Load more functionality works for all sections
- Pending items have appropriate actions (edit/delete)
- All text translated for 5 languages
</success_criteria>

<output>
After completion, create `.planning/phases/11-user-dashboard/11-02-SUMMARY.md`
</output>
