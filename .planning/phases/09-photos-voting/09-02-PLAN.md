---
phase: 09-photos-voting
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/components/photos/photo-upload.tsx
  - src/lib/photos/upload.ts
  - src/lib/photos/validation.ts
autonomous: true

must_haves:
  truths:
    - User can select photo via file picker
    - Upload validates file type (JPEG/PNG/WebP)
    - Upload validates file size (max 5MB)
    - Progress bar shows upload percentage
    - 3 photo per cafe limit checked before upload
    - 10 per day rate limit checked before upload
    - Validation errors shown in list format
    - Success/error feedback via toast notifications
  artifacts:
    - path: "src/components/photos/photo-upload.tsx"
      provides: "Photo upload component with progress and validation"
      min_lines: 150
    - path: "src/lib/photos/upload.ts"
      provides: "Upload utility functions"
    - path: "src/lib/photos/validation.ts"
      provides: "File validation logic"
  key_links:
    - from: "PhotoUpload component"
      to: "Supabase Storage"
      via: "uploadToStorage() function"
    - from: "PhotoUpload component"
      to: "photos table"
      via: "insertPhoto() Server Action"
---

<objective>
Create photo upload component with validation, progress tracking, and rate limit enforcement.

Purpose: Provide intuitive photo upload UI with clear feedback and limit enforcement
Output: PhotoUpload component with validation utilities and upload helpers
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-photos-voting/09-CONTEXT.md

## Key Decisions from Context
- Traditional file picker (click button, not drag-drop)
- One photo at a time (no batch)
- Progress bar showing percentage
- Validation errors in list format below upload area
- Invalid files rejected, valid files kept

## Existing Patterns
@src/components/submissions/submission-form.tsx — form patterns with validation
@src/lib/submissions/actions.ts — Server Action patterns
</context>

<tasks>

<task type="auto">
  <name>Create file validation utilities</name>
  <files>src/lib/photos/validation.ts</files>
  <action>
Create validation module for photo uploads:

1. Define constants:
   - MAX_FILE_SIZE = 5 * 1024 * 1024 (5MB)
   - ALLOWED_TYPES = ['image/jpeg', 'image/png', 'image/webp']
   - ALLOWED_EXTENSIONS = ['.jpg', '.jpeg', '.png', '.webp']

2. Create `validatePhotoFile(file: File)` function:
   - Returns `{ valid: boolean; errors: string[] }`
   - Check file size <= MAX_FILE_SIZE
   - Check mime type in ALLOWED_TYPES
   - Return specific error messages:
     - "File must be less than 5MB"
     - "Only JPG, PNG, and WebP images are allowed"

3. Create `formatFileSize(bytes: number)` helper for display

4. Export all for use in component
  </action>
  <verify>Validation functions handle all edge cases correctly</verify>
  <done>Photo validation utilities ready with size and type checks</done>
</task>

<task type="auto">
  <name>Create upload utilities for Supabase Storage</name>
  <files>src/lib/photos/upload.ts</files>
  <action>
Create upload utilities following project patterns:

1. Create `uploadPhotoToStorage()` function:
   - Takes: file: File, cafeId: string, userId: string
   - Generates storage path: `photos/{cafeId}/{userId}/{timestamp}-{random}.ext`
   - Uses Supabase Storage client to upload
   - Returns: { path: string; error?: string }
   - Handle upload errors gracefully

2. Create check functions using database functions:
   - `checkPhotoLimits(cafeId: string)` — calls can_upload_to_cafe() RPC
   - `checkDailyLimit()` — calls can_upload_photo() RPC or get_remaining_uploads()
   - Returns: { canUpload: boolean; reason?: string }

3. Export types:
   - UploadResult, LimitCheckResult

4. Follow error handling patterns from submission Server Actions (return objects, don't throw)
  </action>
  <verify>Upload utilities compile and follow project patterns</verify>
  <done>Storage upload helpers and limit checkers ready</done>
</task>

<task type="auto">
  <name>Create PhotoUpload component with progress and validation</name>
  <files>src/components/photos/photo-upload.tsx</files>
  <action>
Create PhotoUpload component following context requirements:

1. Props interface:
   - cafeId: string
   - onUploadSuccess?: () => void
   - disabled?: boolean

2. State management:
   - selectedFile: File | null
   - uploadProgress: number (0-100)
   - isUploading: boolean
   - errors: string[]
   - limitInfo: { remainingDaily: number; remainingCafe: number }

3. File selection:
   - Hidden file input (accept="image/*")
   - Visible button to trigger file picker
   - onChange handler validates file immediately
   - Show selected file name and size

4. Validation display:
   - Error list below upload area (if validation fails)
   - Clear errors when new file selected

5. Progress bar:
   - Visible during upload
   - Shows percentage
   - Animated fill (use Tailwind transition)

6. Limit display:
   - Show remaining uploads: "X of 3 photos for this cafe"
   - Show daily limit: "Y uploads remaining today"
   - Disable button if limits reached

7. Upload flow:
   - Check limits first (RPC calls)
   - Upload to Supabase Storage
   - Insert record to photos table (pending status)
   - Show success toast via sonner
   - Call onUploadSuccess callback
   - Clear form

8. Styling:
   - Use project UI components (Button, Card)
   - Tailwind for custom styling
   - Dark mode support

9. i18n:
   - Use useI18n() hook for translations
   - Keys: photo.upload.title, photo.upload.select, photo.upload.limits, etc.
  </action>
  <verify>Component renders with all features and handles upload flow</verify>
  <done>PhotoUpload component with validation, progress, and limits</done>
</task>

</tasks>

<verification>
- Component compiles without TypeScript errors
- File validation rejects oversized and wrong-type files
- Progress bar updates during upload
- Limit checks prevent upload when exceeded
- Success toast appears after upload
- Errors display in list format as specified
</verification>

<success_criteria>
- [ ] File picker triggers native file dialog
- [ ] Files validated for type (JPG/PNG/WebP) and size (5MB)
- [ ] Progress bar visible during upload with percentage
- [ ] 3 photo per cafe limit enforced with clear messaging
- [ ] 10 per day rate limit enforced with clear messaging
- [ ] Validation errors shown below upload area in list format
- [ ] Success toast via sonner after upload completes
- [ ] Component follows project UI patterns and i18n
</success_criteria>

<output>
After completion, create `.planning/phases/09-photos-voting/09-02-SUMMARY.md`
</output>
