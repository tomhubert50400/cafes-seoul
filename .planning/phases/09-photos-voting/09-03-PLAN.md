---
phase: 09-photos-voting
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/components/photos/photo-gallery.tsx
  - src/components/photos/photo-card.tsx
  - src/components/photos/vote-button.tsx
  - src/lib/photos/voting.ts
autonomous: true

must_haves:
  truths:
    - Gallery displays photos in masonry layout
    - First 6 photos visible, "Show more" loads more
    - Heart button in top-right corner of each photo
    - Vote count always visible (no hover required)
    - Toggle voting (click to upvote, click again to remove)
    - Optimistic UI updates count immediately
    - Photos sorted by upvote count (highest first)
    - Never display uploader name
  artifacts:
    - path: "src/components/photos/photo-gallery.tsx"
      provides: "Masonry gallery with show more"
      min_lines: 100
    - path: "src/components/photos/photo-card.tsx"
      provides: "Individual photo card with vote UI"
      min_lines: 80
    - path: "src/components/photos/vote-button.tsx"
      provides: "Heart button with optimistic updates"
      min_lines: 60
    - path: "src/lib/photos/voting.ts"
      provides: "Vote toggle Server Action"
  key_links:
    - from: "VoteButton component"
      to: "toggleVote Server Action"
      via: "onClick handler"
    - from: "PhotoGallery component"
      to: "photos table"
      via: "fetchPhotos query"
---

<objective>
Create photo gallery with masonry layout, voting UI, and optimistic updates.

Purpose: Display approved photos with Instagram-style voting interface
Output: Photo gallery components with heart voting and "Show more" pagination
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-photos-voting/09-CONTEXT.md

## Key Decisions from Context
- Masonry layout (Pinterest-style varying heights)
- First 6 photos visible, "Show more" to load more
- Gallery thumbnails only (no lightbox)
- Vote count always visible
- NEVER show uploader name
- Heart button top-right (Instagram-style)
- Toggle behavior (click to upvote, click to remove)
- Optimistic UI (immediate count update)
- Visual feedback: heart animation + number flash

## Existing Patterns
@src/components/cafe-list.tsx — pagination patterns
@src/lib/ratings/actions.ts — Server Action patterns with optimistic UI
</context>

<tasks>

<task type="auto">
  <name>Create vote toggle Server Action</name>
  <files>src/lib/photos/voting.ts</files>
  <action>
Create voting Server Action following project patterns:

1. Create `toggleVote()` Server Action:
   - Params: photoId: string
   - Returns: { success: boolean; upvoteCount: number; hasVoted: boolean; error?: string }

2. Logic:
   - Get current user via getUser()
   - Check if user already voted for this photo
   - If voted: DELETE vote (toggle off)
   - If not voted: INSERT vote (toggle on)
   - Return new upvote_count from photos table (after trigger updates)

3. Error handling:
   - Return error objects (don't throw)
   - Handle "not authenticated" error
   - Handle database errors gracefully

4. Revalidate path after toggle:
   - revalidatePath('/cafes/[slug]') to refresh gallery

5. Export type: ToggleVoteResult
  </action>
  <verify>Server Action handles toggle logic correctly and returns updated counts</verify>
  <done>Vote toggle Server Action with proper error handling</done>
</task>

<task type="auto">
  <name>Create VoteButton component with optimistic updates</name>
  <files>src/components/photos/vote-button.tsx</files>
  <action>
Create VoteButton component with Instagram-style interaction:

1. Props interface:
   - photoId: string
   - initialCount: number
   - initialHasVoted: boolean
   - onVoteChange?: (count: number, hasVoted: boolean) => void

2. State:
   - count: number (optimistic local state)
   - hasVoted: boolean
   - isPending: boolean (during server call)

3. Click handler:
   - Optimistic update: toggle hasVoted, adjust count immediately
   - Call toggleVote Server Action
   - On error: revert to original values
   - On success: sync with server response

4. Visual design:
   - Heart icon (lucide-react Heart)
   - Position: absolute top-2 right-2
   - Filled heart when voted, outline when not
   - Count displayed next to heart
   - Animation: scale pulse + color change when voted

5. Accessibility:
   - aria-label: "X upvotes"
   - aria-pressed for voted state
   - title: "Click to upvote" / "Click to remove upvote"

6. Styling:
   - Semi-transparent background for readability
   - White/light text for contrast on photos
   - Hover state for desktop

7. Visual feedback:
   - Heart briefly scales up then back (0.2s ease-out)
   - Count briefly highlights/glows when changed
   - Use CSS transitions (Tailwind)
  </action>
  <verify>Button toggles votes with optimistic UI and proper animations</verify>
  <done>VoteButton with optimistic updates and Instagram-style UX</done>
</task>

<task type="auto">
  <name>Create PhotoCard component</name>
  <files>src/components/photos/photo-card.tsx</files>
  <action>
Create PhotoCard component displaying individual photo:

1. Props interface:
   - photo: PhotoWithVoteStatus (includes url, upvoteCount, hasVoted, etc.)
   - onVoteChange?: (photoId: string, newCount: number) => void

2. Photo display:
   - Next.js Image component with proper sizing
   - object-fit: cover
   - Aspect ratio varies (masonry-style heights)
   - Rounded corners (rounded-lg)

3. Vote button:
   - Render VoteButton in top-right corner
   - Pass photo.id, photo.upvoteCount, photo.hasVoted
   - Handle onVoteChange callback

4. Status badge (if user's own photo):
   - Show "Pending" badge only to uploader
   - Small badge in top-left corner
   - Different color for pending vs approved

5. No uploader name (privacy):
   - Do NOT display user name, email, or avatar
   - Focus purely on photo and votes

6. Styling:
   - Relative positioning for absolute children
   - Shadow on hover
   - Transition effects

7. Types needed:
   - Define PhotoWithVoteStatus interface in types/photos.ts
   - Include: id, url, upvoteCount, hasVoted, status, isOwnPhoto
  </action>
  <verify>Card displays photo with vote button and status badge</verify>
  <done>PhotoCard component with vote UI and privacy compliance</done>
</task>

<task type="auto">
  <name>Create PhotoGallery component with masonry layout</name>
  <files>src/components/photos/photo-gallery.tsx</files>
  <action>
Create PhotoGallery component with masonry layout and pagination:

1. Props interface:
   - cafeId: string
   - initialPhotos: PhotoWithVoteStatus[] (from server)
   - currentUserId?: string

2. State:
   - photos: PhotoWithVoteStatus[]
   - displayCount: number (start at 6)
   - isLoading: boolean
   - hasMore: boolean

3. Data fetching:
   - Accept initialPhotos from server (SSR)
   - For "Show more": fetch additional photos via API
   - Sort: by upvote_count DESC, then created_at DESC

4. Masonry layout:
   - Use CSS columns or grid with varying heights
   - 2 columns on mobile, 3 on tablet, 4 on desktop
   - Column gap: consistent spacing (gap-4)
   - Photos naturally flow (break-inside: avoid)

5. "Show more" button:
   - Show if photos.length > displayCount
   - Increment displayCount by 6 on click
   - Loading state while fetching

6. Empty state:
   - Message when no photos: "No photos yet. Be the first to upload!"
   - i18n support

7. Photo rendering:
   - Map over photos.slice(0, displayCount)
   - Render PhotoCard for each
   - Pass onVoteChange handler to update local state

8. Vote change handler:
   - Update photo.upvoteCount in local array
   - Update photo.hasVoted for current user
   - Triggers re-render with new counts

9. Styling:
   - Container with proper padding
   - Loading spinner for "Show more"
   - Smooth transitions when photos appear
  </action>
  <verify>Gallery renders masonry grid with show more and vote updates work</verify>
  <done>PhotoGallery with masonry layout, pagination, and voting</done>
</task>

</tasks>

<verification>
- Gallery displays photos in masonry columns
- Vote button toggles with optimistic count update
- "Show more" loads additional photos
- Photos sorted by upvote count
- No uploader names visible
- Heart animation plays on vote
</verification>

<success_criteria>
- [ ] Masonry layout with 2/3/4 columns responsive
- [ ] First 6 photos visible initially
- [ ] "Show more" button loads 6 more at a time
- [ ] Heart button in top-right corner
- [ ] Vote count always visible
- [ ] Toggle voting works (add/remove)
- [ ] Optimistic UI updates count immediately
- [ ] Heart animation and number flash on change
- [ ] Photos sorted by upvote count (highest first)
- [ ] No uploader name displayed anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/09-photos-voting/09-03-SUMMARY.md`
</output>
