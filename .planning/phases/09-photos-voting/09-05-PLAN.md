---
phase: 09-photos-voting
plan: 05
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
  - 09-04
files_modified:
  - src/app/cafes/[slug]/page.tsx
  - src/app/cafes/[slug]/photos-section.tsx
  - messages/en.json
  - messages/ko.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - Photo gallery appears on cafe detail page
    - Upload section shown to authenticated users
    - "Add Photo" button or upload area visible
    - Gallery shows approved photos sorted by votes
    - Pending photos show "Pending" badge to uploader only
    - i18n strings for all photo-related UI
    - Responsive layout (mobile/desktop)
  artifacts:
    - path: "src/app/cafes/[slug]/photos-section.tsx"
      provides: "Integrated photo section for cafe page"
      min_lines: 100
    - path: "src/app/cafes/[slug]/page.tsx"
      provides: "Updated cafe page with photo section"
    - path: "messages/*.json"
      provides: "i18n translations for photos"
  key_links:
    - from: "Cafe detail page"
      to: "PhotoGallery component"
      via: "PhotosSection component"
    - from: "PhotosSection"
      to: "PhotoUpload component"
      via: "conditional render for authenticated users"
---

<objective>
Integrate photo upload and gallery into cafe detail page with i18n support.

Purpose: Provide complete photo experience on cafe pages
Output: Photos section integrated into cafe detail page with translations
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-photos-voting/09-CONTEXT.md

## Existing Cafe Detail Page
@src/app/cafes/[slug]/page.tsx — current cafe detail structure
@src/app/cafes/[slug]/ratings-section.tsx — section component pattern

## i18n Pattern
@messages/en.json — translation structure
@messages/ko.json — Korean translations
@src/lib/i18n/ — i18n configuration

## Key Integration Points
- Photo section after ratings section
- Upload button for authenticated users only
- Gallery with "Show more" pagination
- Pending badge for user's pending photos
</context>

<tasks>

<task type="auto">
  <name>Create PhotosSection component for cafe page</name>
  <files>src/app/cafes/[slug]/photos-section.tsx</files>
  <action>
Create PhotosSection component following ratings-section pattern:

1. Props interface:
   - cafeId: string
   - initialPhotos: PhotoWithVoteStatus[] (from server)
   - currentUser: User | null

2. Section structure:
   - Section header with title "Photos" (i18n)
   - Photo count badge: "X photos" (optional)
   - Upload area (conditional for authenticated users)
   - PhotoGallery component
   - Empty state when no photos

3. Upload area:
   - If user authenticated: show PhotoUpload component
   - If not authenticated: show "Sign in to upload photos" link
   - Use Card or bordered area for upload section

4. PhotoGallery integration:
   - Pass cafeId, initialPhotos, currentUser?.id
   - Gallery handles pagination internally

5. Empty state:
   - Message: "No photos yet. Be the first to share!"
   - i18n key: photos.emptyState
   - Encouraging CTA for upload

6. Styling:
   - Section spacing consistent with ratings-section
   - Border-t separator from previous section
   - Responsive padding

7. Re-fetch after upload:
   - PhotoUpload onUploadSuccess callback
   - Refreshes gallery (could use router.refresh() or state update)
  </action>
  <verify>Component renders with gallery and conditional upload area</verify>
  <done>PhotosSection component ready for cafe page integration</done>
</task>

<task type="auto">
  <name>Integrate photos section into cafe detail page</name>
  <files>src/app/cafes/[slug]/page.tsx</files>
  <action>
Update cafe detail page to include photos section:

1. Add imports:
   - PhotosSection from './photos-section'
   - Photo-related types

2. Fetch photos server-side:
   - Add photos query alongside cafe data
   - Get initial batch of photos (first 6)
   - Include current user's vote status if authenticated
   - Query only approved photos + user's pending photos

3. Page layout update:
   - Add PhotosSection after RatingsSection
   - Maintain visual flow: Info -> Ratings -> Photos

4. Pass required props:
   - cafeId: cafe.id
   - initialPhotos: fetched photos
   - currentUser: user from getUser()

5. Ensure proper error handling:
   - Photos fetch failures don't break page
   - Log errors but show gallery with empty state

6. Add section ID for anchor links:
   - id="photos" on section wrapper
   - Allows direct linking to photos section

7. Test responsive layout:
   - Mobile: full width sections
   - Desktop: maintain two-column layout if applicable
  </action>
  <verify>Cafe page renders with photos section and initial photos loaded</verify>
  <done>Photos integrated into cafe detail page with SSR data</done>
</task>

<task type="auto">
  <name>Add i18n translations for photos feature</name>
  <files>messages/en.json, messages/ko.json, messages/fr.json, messages/zh.json, messages/vi.json</files>
  <action>
Add photo-related translations to all i18n files:

1. English (en.json) — add to existing structure:
   ```json
   "photos": {
     "title": "Photos",
     "upload": {
       "title": "Upload Photo",
       "select": "Select Photo",
       "uploading": "Uploading...",
       "success": "Photo uploaded successfully!",
       "error": "Failed to upload photo",
       "limits": {
         "daily": "{count} uploads remaining today",
         "cafe": "{count} of 3 photos for this cafe",
         "reached": "Upload limit reached"
       }
     },
     "gallery": {
       "emptyState": "No photos yet. Be the first to share!",
       "showMore": "Show more",
       "loading": "Loading...",
       "voteCount": "{count} upvotes"
     },
     "status": {
       "pending": "Pending approval",
       "approved": "Approved",
       "rejected": "Rejected"
     },
     "errors": {
       "fileSize": "File must be less than 5MB",
       "fileType": "Only JPG, PNG, and WebP images are allowed",
       "limitCafe": "You can only upload 3 photos per cafe",
       "limitDaily": "You can upload up to 10 photos per day",
       "deletePendingOnly": "You can only delete pending photos"
     }
   }
   ```

2. Korean (ko.json) translations:
   - Use appropriate Korean terms
   - Keep technical terms consistent with rest of app
   - Example: "Photos" -> "사진", "Upload" -> "업로드"

3. French (fr.json), Chinese (zh.json), Vietnamese (vi.json):
   - Add same structure with translations
   - Use existing translation patterns
   - Can use placeholder translations if needed (to be refined later)

4. Ensure all UI strings use i18n:
   - No hardcoded strings in components
   - Use t('photos.upload.title') pattern

5. Test key pages render without missing translation errors
  </action>
  <verify>All photo UI elements display translated strings</verify>
  <done>i18n translations for photos in all supported languages</done>
</task>

</tasks>

<verification>
- Photos section appears on cafe detail page
- Upload area shown for authenticated users only
- Gallery displays photos with vote counts
- Pending badge visible to uploader
- "Show more" pagination works
- All UI strings translated
- Responsive layout on mobile and desktop
</verification>

<success_criteria>
- [ ] Photos section visible on cafe detail page
- [ ] Upload component shown to authenticated users
- [ ] "Sign in to upload" shown to guests
- [ ] Gallery displays with initial 6 photos
- [ ] "Show more" loads additional photos
- [ ] Pending badge visible only to uploader
- [ ] Photos sorted by upvote count
- [ ] All strings translated in en.json and ko.json
- [ ] French, Chinese, Vietnamese have translations (can be basic)
- [ ] Responsive layout works on mobile/desktop
- [ ] Photos section has anchor id="#photos"
</success_criteria>

<output>
After completion, create `.planning/phases/09-photos-voting/09-05-SUMMARY.md`
</output>
