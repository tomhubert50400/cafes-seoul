---
phase: 06-map-feature
plan: 06
type: execute
wave: 3
depends_on: [06-04, 06-05]
files_modified:
  - src/components/map/cafe-map-skeleton.tsx
  - src/components/map/map-error-boundary.tsx
  - src/components/map/index.ts
  - src/app/map/error.tsx
  - src/app/map/loading.tsx
autonomous: false

must_haves:
  truths:
    - "Map shows loading skeleton while script loads"
    - "Error boundary catches and displays map errors gracefully"
    - "Mobile layout is fully usable (filter drawer, touch targets)"
    - "All 9 rating dimensions can be filtered simultaneously"
    - "Keyboard navigation works for accessibility"
  artifacts:
    - path: "src/components/map/cafe-map-skeleton.tsx"
      provides: "Loading state for map"
    - path: "src/components/map/map-error-boundary.tsx"
      provides: "Error handling for map failures"
    - path: "src/components/map/index.ts"
      provides: "Clean exports for all map components"
  key_links:
    - from: "MapPage"
      to: "Error Boundary"
      via: "error.tsx in app directory"
    - from: "MapPage"
      to: "Loading State"
      via: "loading.tsx in app directory"
---

<objective>
Polish the map feature with loading states, error handling, mobile optimization, and comprehensive testing.

Purpose: Ensure the map feature is production-ready with smooth UX, proper error handling, mobile responsiveness, and accessibility.
Output: Loading states, error boundaries, mobile improvements, and verified functionality across all scenarios.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-map-feature/06-RESEARCH.md
@.planning/phases/06-map-feature/06-04-SUMMARY.md
@.planning/phases/06-map-feature/06-05-SUMMARY.md

**Prerequisites:**
- Plans 06-04 and 06-05 must be complete and verified
- Map feature functional on desktop
- Static maps working on cafe pages

**Polish Requirements:**
- Loading skeleton while Kakao script loads
- Error boundary for map failures
- Mobile-responsive filter drawer
- Keyboard navigation
- Performance optimization
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CafeMapSkeleton component</name>
  <files>src/components/map/cafe-map-skeleton.tsx</files>
  <action>
    Create a loading skeleton for the map:
    
    ```typescript
    export function CafeMapSkeleton() {
      return (
        <div className="h-full w-full bg-muted/20 animate-pulse flex items-center justify-center">
          <div className="text-center">
            <div className="h-8 w-8 bg-muted rounded-full mx-auto" />
            <p className="mt-2 text-sm text-muted-foreground">Loading map...</p>
          </div>
        </div>
      );
    }
    ```
    
    Also update the dynamic import in /map/page.tsx to use this skeleton:
    ```typescript
    const CafeMapWrapper = dynamic(
      () => import('@/components/map/cafe-map-wrapper').then((mod) => mod.CafeMapWrapper),
      { 
        ssr: false,
        loading: () => <CafeMapSkeleton />
      }
    );
    ```
  </action>
  <verify>test -f src/components/map/cafe-map-skeleton.tsx && grep -q "export function CafeMapSkeleton" src/components/map/cafe-map-skeleton.tsx</verify>
  <done>CafeMapSkeleton component created and integrated</done>
</task>

<task type="auto">
  <name>Task 2: Create map error boundary</name>
  <files>src/app/map/error.tsx</files>
  <action>
    Create an error page for the map route:
    
    ```typescript
    'use client';

    import { useEffect } from 'react';
    import { Button } from '@/components/ui/button';
    import { AlertCircle, RefreshCw } from 'lucide-react';

    export default function MapError({
      error,
      reset,
    }: {
      error: Error & { digest?: string };
      reset: () => void;
    }) {
      useEffect(() => {
        console.error('Map error:', error);
      }, [error]);

      return (
        <div className="flex h-[calc(100vh-3.5rem)] items-center justify-center bg-muted/20">
          <div className="text-center max-w-md mx-auto p-6">
            <AlertCircle className="h-12 w-12 text-destructive mx-auto mb-4" />
            <h2 className="text-lg font-semibold mb-2">Failed to load map</h2>
            <p className="text-sm text-muted-foreground mb-4">
              There was an error loading the map. This might be due to a network 
              issue or a problem with the map service.
            </p>
            <div className="space-x-2">
              <Button onClick={reset} variant="default">
                <RefreshCw className="h-4 w-4 mr-2" />
                Try again
              </Button>
              <Button onClick={() => window.location.href = '/'} variant="outline">
                Go home
              </Button>
            </div>
          </div>
        </div>
      );
    }
    ```
  </action>
  <verify>test -f src/app/map/error.tsx && grep -q "export default function MapError" src/app/map/error.tsx</verify>
  <done>Error boundary created for map route</done>
</task>

<task type="auto">
  <name>Task 3: Create map loading state</name>
  <files>src/app/map/loading.tsx</files>
  <action>
    Create a loading page for the map route:
    
    ```typescript
    import { Loader2 } from 'lucide-react';

    export default function MapLoading() {
      return (
        <div className="flex h-[calc(100vh-3.5rem)] items-center justify-center bg-muted/20">
          <div className="text-center">
            <Loader2 className="h-8 w-8 animate-spin mx-auto text-primary" />
            <p className="mt-2 text-sm text-muted-foreground">Loading map...</p>
          </div>
        </div>
      );
    }
    ```
  </action>
  <verify>test -f src/app/map/loading.tsx && grep -q "export default function MapLoading" src/app/map/loading.tsx</verify>
  <done>Loading state created for map route</done>
</task>

<task type="auto">
  <name>Task 4: Create map components index file</name>
  <files>src/components/map/index.ts</files>
  <action>
    Create a clean exports file for all map components:
    
    ```typescript
    // Map components
    export { MapProvider } from './map-provider';
    export { CafeMap } from './cafe-map';
    export { CafeMarker } from './cafe-marker';
    export { CafeInfoWindow } from './cafe-info-window';
    export { CafeStaticMap } from './cafe-static-map';
    export { MapWithFilters } from './map-with-filters';
    export { CafeMapWrapper } from './cafe-map-wrapper';
    export { MapFilters } from './map-filters';
    export { RatingSlider } from './rating-slider';
    export { FeatureToggle } from './feature-toggle';
    export { CafeMapSkeleton } from './cafe-map-skeleton';
    ```
    
    This allows clean imports:
    ```typescript
    import { CafeMap, MapFilters, CafeStaticMap } from '@/components/map';
    ```
  </action>
  <verify>test -f src/components/map/index.ts && grep -q "export { MapProvider }" src/components/map/index.ts</verify>
  <done>Map components index file created with all exports</done>
</task>

<task type="auto">
  <name>Task 5: Mobile optimization improvements</name>
  <files>src/components/map/map-with-filters.tsx</files>
  <action>
    Review and improve mobile UX:
    
    1. Ensure filter button is easily tappable (min 44px touch target)
    2. Check that filter drawer opens smoothly
    3. Verify map takes full height on mobile
    4. Test that info windows are readable on small screens
    
    Update CafeInfoWindow if needed for mobile:
    ```typescript
    // Add responsive classes to info window
    <div className="relative min-w-[200px] max-w-[260px] md:min-w-[240px] md:max-w-[280px] ...">
    ```
    
    Ensure MapFilters has proper padding on mobile:
    ```typescript
    // In MapWithFilters mobile SheetContent
    <SheetContent side="left" className="w-[300px] sm:w-80 p-0">
    ```
  </action>
  <verify>grep -q "w-\[300px\]" src/components/map/map-with-filters.tsx || grep -q "w-80" src/components/map/map-with-filters.tsx</verify>
  <done>Mobile optimizations applied</done>
</task>

<task type="auto">
  <name>Task 6: Performance verification</name>
  <files>src/components/map/cafe-map.tsx</files>
  <action>
    Verify performance optimizations:
    
    1. Check that CafeMarker uses React.memo (done in 06-04)
    2. Verify filterCafes is wrapped in useMemo:
    ```typescript
    const visibleCafes = useMemo(() => {
      if (!filters) return cafes;
      return filterCafes(cafes, filters);
    }, [cafes, filters]);
    ```
    
    3. Ensure callbacks use useCallback:
    ```typescript
    const handleMarkerClick = useCallback((cafe: CafeSummary) => {
      setSelectedCafe(cafe);
    }, []);
    ```
    
    4. Run Lighthouse performance check (if possible) or manual test:
    - Open /map with 100+ cafes
    - Zoom in/out rapidly
    - Apply filters quickly
    - Verify no lag or jank
  </action>
  <verify>grep -q "useMemo" src/components/map/cafe-map.tsx && grep -q "useCallback" src/components/map/cafe-map.tsx</verify>
  <done>Performance optimizations verified</done>
</task>

<task type="auto">
  <name>Task 7: Final testing checklist</name>
  <files>src/components/map/cafe-map.tsx</files>
  <action>
    Create and run through a comprehensive test checklist:
    
    ```markdown
    ## Map Feature Testing Checklist
    
    ### Desktop (Chrome, Safari, Firefox)
    - [ ] Map loads and shows Kakao tiles
    - [ ] All cafes appear as markers
    - [ ] Zoom out → markers cluster
    - [ ] Zoom in → clusters break into individual markers
    - [ ] Click marker → info window opens
    - [ ] Click "View Details" → navigates to cafe page
    - [ ] Click X or map → info window closes
    - [ ] Filter sidebar visible
    - [ ] Adjust seating slider → markers filter instantly
    - [ ] Check "Has WiFi" → only wifi cafes remain
    - [ ] Multiple filters combine (AND logic)
    - [ ] Clear all → all markers restored
    - [ ] Active filter count displayed correctly
    
    ### Mobile (iOS Safari, Chrome Android)
    - [ ] Map loads and fills screen
    - [ ] Filter button visible in top-left
    - [ ] Tap filter button → drawer opens smoothly
    - [ ] Filters work in drawer
    - [ ] Close drawer → filters applied
    - [ ] Tap marker → info window opens
    - [ ] Info window readable (not too small)
    - [ ] Can close info window
    - [ ] Pinch to zoom works
    - [ ] Pan/drag map works
    
    ### Static Maps (Cafe Detail Pages)
    - [ ] Static map shows on all cafe pages
    - [ ] Map centered on correct location
    - [ ] Marker visible
    - [ ] Map not interactive (can't zoom/drag)
    - [ ] Address displayed below map
    
    ### Error Handling
    - [ ] Remove API key → error state shows
    - [ ] Error has "Try again" button
    - [ ] Slow network → loading state visible
    
    ### Accessibility
    - [ ] Tab through filter controls
    - [ ] Keyboard can select markers (if supported)
    - [ ] Screen reader announces filter changes
    ```
    
    Fix any issues found during testing.
  </action>
  <verify>echo "Manual testing checklist created"</verify>
  <done>Testing checklist created and verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Final verification - comprehensive testing</name>
  <what-built>
    Complete map feature with polish:
    - Loading skeletons for better UX
    - Error boundaries for graceful failures
    - Mobile-optimized filter drawer
    - Performance optimizations (memo, useCallback)
    - Clean component exports
    - Comprehensive test coverage
  </what-built>
  <how-to-verify>
    Complete full testing:
    
    **Desktop:**
    1. Visit /map → map loads with loading state then shows cafes
    2. Test all 9 rating sliders (seating, wifi, food, etc.)
    3. Test all 5 feature toggles
    4. Verify clustering works (zoom out)
    5. Click markers and navigate to cafes
    6. Clear filters and verify all markers return
    
    **Mobile:**
    1. Use browser mobile viewport (or actual phone)
    2. Tap filter button → drawer should open
    3. Apply filters → drawer closes, map updates
    4. Tap markers → info window readable
    5. Test pinch-zoom and pan
    
    **Cafe Detail Pages:**
    1. Visit 3+ different cafe pages
    2. Verify static map shows in sidebar
    3. Confirm map is not interactive
    
    **Error Cases:**
    1. Temporarily remove API key from .env.local
    2. Refresh /map → should show error state
    3. Restore API key
  </how-to-verify>
  <resume-signal>Type "approved" when all tests pass, or list any remaining issues</resume-signal>
</task>

</tasks>

<verification>
1. Loading skeleton shows while map loads
2. Error boundary catches and displays errors
3. Mobile filter drawer works smoothly
4. All 9 rating dimensions filterable
5. Performance optimizations in place (memo, useCallback)
6. Clean exports via index.ts
7. Comprehensive testing completed
</verification>

<success_criteria>
- [ ] Loading skeleton for map
- [ ] Error boundary for map failures
- [ ] Mobile filter drawer with smooth animations
- [ ] All 9 rating dimensions filterable
- [ ] All 5 boolean features toggleable
- [ ] Performance: no lag with 100+ cafes
- [ ] Static maps on all cafe detail pages
- [ ] Clean component exports
- [ ] Desktop testing complete
- [ ] Mobile testing complete
- [ ] Error handling tested
</success_criteria>

<output>
After completion, create `.planning/phases/06-map-feature/06-06-SUMMARY.md` with:
- Polish features added
- Performance optimizations
- Testing results
- Known limitations
</output>
