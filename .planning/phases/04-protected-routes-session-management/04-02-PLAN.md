---
phase: 04-protected-routes-session-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/auth/user-menu.tsx
  - src/components/header.tsx
  - src/app/layout.tsx
  - src/lib/i18n/translations.ts
autonomous: true

must_haves:
  truths:
    - "Logged-in users see avatar dropdown menu in header"
    - "Avatar shows user's profile image or initials fallback"
    - "Dropdown contains: Profile, My Reviews, Settings, Language, Logout"
    - "Logged-out users see Login and Signup buttons"
    - "Layout fetches user server-side and passes to Header"
  artifacts:
    - path: "src/components/auth/user-menu.tsx"
      provides: "Avatar dropdown menu for authenticated users"
      uses: "@radix-ui/react-dropdown-menu, @radix-ui/react-avatar"
    - path: "src/components/header.tsx"
      provides: "Updated Header with UserMenu integration"
      changes: "Replace LogoutButton with UserMenu when user exists"
    - path: "src/app/layout.tsx"
      provides: "Root layout with user fetching"
      changes: "Add supabase auth.getUser() call, pass user to Header"
    - path: "src/lib/i18n/translations.ts"
      provides: "Translations for user menu items"
      adds: "nav.profile, nav.myReviews, nav.settings, nav.logout"
  key_links:
    - from: "layout.tsx"
      to: "Header"
      via: "user prop"
      pattern: "<Header user={user} />"
    - from: "Header"
      to: "UserMenu"
      via: "conditional rendering"
      pattern: "{user ? <UserMenu ... /> : <Login/Signup buttons />}"
---

<objective>
Create auth-aware header that shows avatar dropdown for logged-in users and login/signup buttons for logged-out users. Layout fetches user server-side and passes to Header component.

Purpose: Provide clear visual indication of authentication state and easy access to user account actions.
Output: UserMenu component, updated Header, layout with user fetching, and translations.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protected-routes-session-management/04-CONTEXT.md
@.planning/phases/04-protected-routes-session-management/04-RESEARCH.md
@src/components/header.tsx
@src/components/ui/dropdown-menu.tsx
@src/components/ui/avatar.tsx
@src/components/auth/logout-button.tsx
@src/lib/i18n/translations.ts
@src/lib/constants/routes.ts
@src/app/layout.tsx
@src/lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Create UserMenu component with avatar dropdown</name>
  <files>src/components/auth/user-menu.tsx</files>
  <action>
    Create a new UserMenu component at src/components/auth/user-menu.tsx:

    1. Import required components:
       - DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator from @/components/ui/dropdown-menu
       - Avatar, AvatarImage, AvatarFallback from @/components/ui/avatar
       - User from @/types/user (use User type)
       - Link from next/link
       - useI18n from @/lib/i18n
       - logout action from @/app/actions/auth
       - ROUTES from @/lib/constants/routes
       - User icon from lucide-react

    2. Define UserMenuProps interface with user: User

    3. Create helper function getInitials(email: string): string that returns first two chars of email before @, uppercase

    4. Component structure:
       - Use DropdownMenu as wrapper
       - DropdownMenuTrigger: Button with Avatar (circular, size="sm")
         - AvatarImage: src={user.avatarUrl || undefined}
         - AvatarFallback: {getInitials(user.email)}
       - DropdownMenuContent: align="end", className="w-56"
         - DropdownMenuItem with User icon -> Link to ROUTES.PROFILE
         - DropdownMenuItem -> Link to ROUTES.PROFILE_REVIEWS
         - DropdownMenuItem -> Link to ROUTES.PROFILE_SETTINGS
         - DropdownMenuSeparator
         - DropdownMenuItem with logout action (form with action={logout})

    5. Use translations keys:
       - 'nav.profile'
       - 'nav.myReviews'
       - 'nav.settings'
       - 'auth.logout'

    6. Mark as 'use client' since it uses client-side hooks and actions
  </action>
  <verify>
    cat src/components/auth/user-menu.tsx | head -5
  </verify>
  <done>UserMenu component exists with avatar dropdown and all menu items</done>
</task>

<task type="auto">
  <name>Update Header to use UserMenu</name>
  <files>src/components/header.tsx</files>
  <action>
    Update the Header component to integrate UserMenu:

    1. Import UserMenu from @/components/auth/user-menu
    2. Update HeaderProps interface - user should be User type (not just { email?: string }), allow null
    3. Replace the existing conditional rendering:
       CURRENT: {user ? (<LogoutButton />) : (<Login/Signup buttons />)}
       NEW: {user ? (<UserMenu user={user} />) : (<Login/Signup buttons />)}
    4. Remove LogoutButton import since it's now inside UserMenu
    5. Import User type from @/types/user

    Keep all other Header functionality the same:
    - Logo and site name
    - Navigation items (Cafes, Map, Districts)
    - LanguageSwitcher (always visible)
    - Mobile responsiveness (hidden on mobile nav)
  </action>
  <verify>
    grep -n "UserMenu" src/components/header.tsx
  </verify>
  <done>Header conditionally renders UserMenu when user is logged in</done>
</task>

<task type="auto">
  <name>Add user fetching to root layout</name>
  <files>src/app/layout.tsx</files>
  <action>
    Update the root layout to fetch user server-side and pass to Header:

    1. Import createClient from @/lib/supabase/server
    2. Import Header from @/components/header
    3. In the Layout component (before return):
       - const supabase = await createClient()
       - const { data: { user } } = await supabase.auth.getUser()
    4. Pass user to Header component:
       - <Header user={user} />

    Note: Header import needs to be added (it's not currently imported). Remove the children-only structure and wrap with Header.

    The layout should be:
    <body>
      <I18nProvider>
        <Header user={user} />
        {children}
        <Toaster />
      </I18nProvider>
    </body>
  </action>
  <verify>
    grep -n "Header" src/app/layout.tsx
  </verify>
  <done>Layout fetches user and passes to Header component</done>
</task>

<task type="auto">
  <name>Add translations for user menu</name>
  <files>src/lib/i18n/translations.ts</files>
  <action>
    Add translation keys for user menu items in all 5 languages (en, ko, fr, zh, vi):

    Add to the "Header & Navigation" section after 'nav.signup':
    - 'nav.profile': 'Profile' / '프로필' / 'Profil' / '个人资料' / 'Hồ sơ'
    - 'nav.myReviews': 'My Reviews' / '내 리뷰' / 'Mes avis' / '我的评论' / 'Đánh giá của tôi'
    - 'nav.settings': 'Settings' / '설정' / 'Paramètres' / '设置' / 'Cài đặt'

    The 'auth.logout' key already exists, so no need to add it.

    Keep existing translations intact, only add the new keys.
  </action>
  <verify>
    grep -n "nav.profile" src/lib/i18n/translations.ts
  </verify>
  <done>Translations added for profile, my reviews, and settings in all 5 languages</done>
</task>

</tasks>

<verification>
- [ ] UserMenu component exists at src/components/auth/user-menu.tsx
- [ ] UserMenu uses DropdownMenu and Avatar from Radix UI
- [ ] Avatar shows initials fallback when no profile image
- [ ] Header conditionally shows UserMenu (logged in) or Login/Signup buttons (logged out)
- [ ] Layout fetches user via supabase.auth.getUser()
- [ ] Header receives user prop from layout
- [ ] Translations exist for nav.profile, nav.myReviews, nav.settings in all languages
</verification>

<success_criteria>
Header displays avatar dropdown with Profile, My Reviews, Settings, and Logout for authenticated users. Logged-out users see Login and Signup buttons. Layout fetches user server-side for optimal performance.
</success_criteria>

<output>
After completion, create `.planning/phases/04-protected-routes-session-management/04-02-SUMMARY.md`
</output>
