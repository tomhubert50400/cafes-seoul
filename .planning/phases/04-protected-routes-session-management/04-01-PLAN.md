---
phase: 04-protected-routes-session-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/middleware.ts
autonomous: true

must_haves:
  truths:
    - "Middleware uses 'next' parameter instead of 'redirect' for return URL"
    - "Protected routes (/profile, /favorites) redirect unauthenticated users to /login"
    - "Login page preserves intended destination via ?next=/path query param"
    - "Logged-in users are redirected away from /login and /signup to homepage"
    - "Supabase session persistence is enabled - sessions persist until logout (AUTH-08)"
  artifacts:
    - path: "src/lib/supabase/middleware.ts"
      provides: "Session refresh and route protection"
      changes: "Change 'redirect' to 'next' param, preserve protected paths logic, document session persistence"
  key_links:
    - from: "middleware.ts protected paths check"
      to: "Login page with next param"
      via: "NextResponse.redirect to /login?next=/path"
    - from: "middleware.ts"
      to: "Supabase cookie-based sessions"
      via: "supabase.auth.getSession() refresh"
      note: "Sessions persist until logout by default"
---

<objective>
Update middleware to use 'next' parameter for return URL consistency, ensuring protected routes redirect unauthenticated users to login with the intended destination preserved.

Purpose: Standardize the redirect parameter across the auth flow and ensure route protection works correctly.
Output: Updated middleware.ts using 'next' instead of 'redirect' query parameter.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protected-routes-session-management/04-RESEARCH.md
@src/middleware.ts
@src/lib/supabase/middleware.ts
@src/app/auth/callback/route.ts
</context>

<tasks>

<task type="auto">
  <name>Update middleware redirect parameter</name>
  <files>src/lib/supabase/middleware.ts</files>
  <action>
    Update the middleware.ts file to change the redirect parameter from 'redirect' to 'next' for consistency with the OAuth callback and standard Next.js conventions:

    1. Locate the protected paths redirect logic (around line 48)
    2. Change `url.searchParams.set('redirect', request.nextUrl.pathname)` to `url.searchParams.set('next', request.nextUrl.pathname)`
    
    The middleware already has:
    - Protected paths: ['/profile', '/favorites']
    - Auth paths: ['/login', '/signup']
    - Logic to redirect unauthenticated users from protected routes
    - Logic to redirect logged-in users away from auth pages
    
    Only change the query parameter name from 'redirect' to 'next'. All other logic remains the same.
  </action>
  <verify>
    grep -n "searchParams.set('next'" src/lib/supabase/middleware.ts
  </verify>
  <done>Middleware uses 'next' parameter for redirect URL, not 'redirect'</done>
</task>

<task type="auto">
  <name>Verify and document session persistence (AUTH-08)</name>
  <files>src/lib/supabase/middleware.ts</files>
  <action>
    Verify that Supabase session persistence is enabled and document it in the code:

    1. Confirm middleware.ts uses @supabase/ssr createServerClient with cookie options
    2. Verify that getSession() is called on every request (this triggers automatic token refresh)
    3. Add a comment at the top of the file explaining session persistence:
       
       ```
       /**
        * Session Persistence (AUTH-08):
        * Supabase sessions persist until logout by default via secure HTTP-only cookies.
        * The middleware automatically refreshes expired access tokens on cold starts.
        * Sessions survive browser refresh, tab close/reopen, and device restart.
        */
       ```
    
    4. Ensure cookie options include:
       - httpOnly: true (prevents XSS)
       - secure: true in production
       - sameSite: 'lax' (CSRF protection)
       
    The default Supabase @supabase/ssr configuration already satisfies AUTH-08.
    The "Remember me" checkbox in the UI is for user expectation management only.
  </action>
  <verify>
    grep -n "Session Persistence\|persist until logout" src/lib/supabase/middleware.ts
  </verify>
  <done>Session persistence documented in middleware, AUTH-08 verified as satisfied by default Supabase behavior</done>
</task>

</tasks>

<verification>
- [ ] Middleware file contains `searchParams.set('next', ...)` not `searchParams.set('redirect', ...)`
- [ ] Protected paths (/profile, /favorites) are still defined
- [ ] Auth paths (/login, /signup) are still defined
- [ ] Unauthenticated users redirect to /login with ?next=/path
- [ ] Logged-in users redirect to / from auth pages
- [ ] Session persistence comment exists in middleware.ts
- [ ] AUTH-08 marked as satisfied by default Supabase cookie behavior
</verification>

<success_criteria>
Middleware consistently uses 'next' parameter for return URLs, matching OAuth callback convention and enabling proper redirect flow after authentication. Session persistence (AUTH-08) is documented as satisfied by default Supabase @supabase/ssr cookie-based session management.
</success_criteria>

<output>
After completion, create `.planning/phases/04-protected-routes-session-management/04-01-SUMMARY.md`
</output>
