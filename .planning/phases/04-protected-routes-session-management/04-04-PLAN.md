---
phase: 04-protected-routes-session-management
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/auth/login-form.tsx
  - src/app/(auth)/login/page-client.tsx
  - src/lib/i18n/translations.ts
autonomous: true

must_haves:
  truths:
    - "Login form includes 'Remember me' checkbox with label"
    - "Remember me preference persists to localStorage"
    - "Login page stores next URL in sessionStorage when present"
    - "Next URL survives multiple login attempts"
    - "Translations exist for remember me in all 5 languages"
  artifacts:
    - path: "src/components/auth/login-form.tsx"
      provides: "Login form with remember me checkbox"
      changes: "Add Checkbox component, localStorage logic for rememberMe preference"
    - path: "src/app/(auth)/login/page-client.tsx"
      provides: "Next URL storage in sessionStorage"
      changes: "Add useEffect to read next param and store in sessionStorage"
    - path: "src/lib/i18n/translations.ts"
      provides: "Remember me translations"
      adds: "auth.login.rememberMe key in all 5 languages"
  key_links:
    - from: "login-form.tsx"
      to: "localStorage"
      via: "rememberMe preference storage"
    - from: "page-client.tsx"
      to: "sessionStorage"
      via: "authNextUrl storage for redirect after login"
---

<objective>
Add "Remember me" functionality to login form and implement session storage for next URL persistence across login attempts. Include all necessary translations.

Purpose: Improve UX by respecting user session preferences and ensuring redirect works correctly after multiple login attempts.
Output: Updated login form with remember me checkbox, sessionStorage integration for next URL, and translations.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protected-routes-session-management/04-CONTEXT.md
@.planning/phases/04-protected-routes-session-management/04-RESEARCH.md
@src/components/auth/login-form.tsx
@src/app/(auth)/login/page-client.tsx
@src/lib/i18n/translations.ts
@src/components/ui/checkbox.tsx
</context>

<tasks>

<task type="auto">
  <name>Add "Remember me" to login form</name>
  <files>src/components/auth/login-form.tsx</files>
  <action>
    Update the login form to add "Remember me" functionality:

    1. Add state for rememberMe: boolean (default true based on localStorage or true as default)

    2. Use useEffect to load remember me preference from localStorage on mount:
       - const stored = localStorage.getItem('rememberMe')
       - setRememberMe(stored === null ? true : stored === 'true')

    3. Add Checkbox from @/components/ui/checkbox between password field and submit button
       - Label: "Remember me" / "로그인 상태 유지" (use translation key 'auth.login.rememberMe')
       - Checked state bound to rememberMe
       - onCheckedChange: update state AND save to localStorage

    4. IMPORTANT: Add a comment explaining that the remember me preference is just for UX - Supabase sessions persist until logout by default via secure cookies. Store the preference in localStorage as 'rememberMe'.

    5. Make sure to handle typeof window !== 'undefined' for localStorage access to avoid SSR issues.

    6. Style the checkbox and label appropriately - label should be clickable and positioned to the right of the checkbox.
  </action>
  <verify>
    grep -n "rememberMe\|localStorage" src/components/auth/login-form.tsx
  </verify>
  <done>Login form has remember me checkbox that persists to localStorage</done>
</task>

<task type="auto">
  <name>Add session storage for next URL</name>
  <files>src/app/(auth)/login/page-client.tsx</files>
  <action>
    Update the login page client to store next URL in sessionStorage:

    1. Read the file to understand current structure
    2. Add useEffect to:
       - Check for next param in URL (?next=/profile)
       - If present, store in sessionStorage as 'authNextUrl'
       - This survives multiple login attempts (email -> error -> try again)

    3. The OAuth buttons already handle next param via server action, but for email/password login, we need to preserve the next URL across form submissions.

    4. The actual redirect after successful email/password login is handled by the server action (auth.ts), which currently redirects to '/'. We'll update this in a future plan to read from sessionStorage.

    5. Add a comment explaining: "Store next URL for redirect after successful login - survives page reloads and multiple attempts"

    For now, just store the next URL when the login page loads with a next param.
  </action>
  <verify>
    grep -n "sessionStorage\|authNextUrl" src/app/(auth)/login/page-client.tsx
  </verify>
  <done>Login page stores next URL in sessionStorage when present</done>
</task>

<task type="auto">
  <name>Add remember me translations</name>
  <files>src/lib/i18n/translations.ts</files>
  <action>
    Add translation key for remember me checkbox in all 5 languages:

    Add to the Auth section (near auth.login.submit):
    - 'auth.login.rememberMe': 'Remember me' / '로그인 상태 유지' / 'Se souvenir de moi' / '记住我' / 'Ghi nhớ đăng nhập'

    Note: The translations for the login form itself should already exist from Phase 2. Only add the rememberMe key if it doesn't exist.
  </action>
  <verify>
    grep -n "auth.login.rememberMe" src/lib/i18n/translations.ts
  </verify>
  <done>Remember me translation exists in all 5 languages</done>
</task>

</tasks>

<verification>
- [ ] Login form has "Remember me" checkbox
- [ ] Remember me preference stores in localStorage
- [ ] Login page stores next URL in sessionStorage when ?next= param present
- [ ] Translations exist for auth.login.rememberMe in all languages
- [ ] Comments explain that session persistence is handled by Supabase (AUTH-08)
</verification>

<success_criteria>
Login form includes working "Remember me" checkbox that persists to localStorage. Next URL persists across login attempts via sessionStorage. User understands that Supabase handles actual session persistence, and "Remember me" is for UX preference only.
</success_criteria>

<output>
After completion, create `.planning/phases/04-protected-routes-session-management/04-04-SUMMARY.md`
</output>
