---
phase: 20-vibe-search-assistant
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/hooks/use-map-filters.ts
  - src/components/map/preset-badges.tsx
autonomous: true

must_haves:
  truths:
    - "useMapFilters hook exposes applyPreset() that resets to defaults then applies preset filters"
    - "useMapFilters hook exposes matchedPreset that reactively computes the matching preset"
    - "PresetBadges component renders a horizontal scrollable bar of pill badges"
    - "Active preset badge has distinct visual styling (filled primary color)"
    - "All badges meet 44px WCAG AAA touch target minimum"
  artifacts:
    - path: "src/hooks/use-map-filters.ts"
      provides: "Extended hook with applyPreset and matchedPreset"
      exports: ["useMapFilters"]
    - path: "src/components/map/preset-badges.tsx"
      provides: "Horizontal scrollable preset badge bar component"
      exports: ["PresetBadges"]
  key_links:
    - from: "src/hooks/use-map-filters.ts"
      to: "src/lib/filter-presets.ts"
      via: "imports FILTER_PRESETS and getMatchedPreset"
      pattern: "import.*FILTER_PRESETS.*from.*filter-presets"
    - from: "src/components/map/preset-badges.tsx"
      to: "src/lib/filter-presets.ts"
      via: "imports FILTER_PRESETS for rendering badges"
      pattern: "import.*FILTER_PRESETS.*from.*filter-presets"
    - from: "src/components/map/preset-badges.tsx"
      to: "useI18n"
      via: "translates preset labels using t()"
      pattern: "useI18n|t\\("
---

<objective>
Extend the useMapFilters hook with preset application and matching, and create the PresetBadges UI component.

Purpose: The hook extension centralizes preset logic alongside existing filter state management. The PresetBadges component provides the visual UI that users interact with to select vibes. Together they form the reusable building blocks that Plan 03 wires into both the map and list pages.

Output: Extended `src/hooks/use-map-filters.ts` with `applyPreset()` and `matchedPreset`. New `src/components/map/preset-badges.tsx` with horizontal scrollable badge bar.
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-vibe-search-assistant/20-RESEARCH.md
@.planning/phases/20-vibe-search-assistant/20-01-SUMMARY.md

@src/hooks/use-map-filters.ts
@src/lib/filter-presets.ts
@src/types/map.ts
@src/lib/i18n/translations.ts
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend useMapFilters hook with applyPreset and matchedPreset</name>
  <files>src/hooks/use-map-filters.ts</files>
  <action>
Extend the existing `useMapFilters` hook in `src/hooks/use-map-filters.ts`. Do NOT change existing function signatures or break existing consumers.

Add two new capabilities:

1. **applyPreset(presetId: string)** - A useCallback that:
   - Finds the preset by ID from FILTER_PRESETS
   - If not found, returns early (no-op)
   - Resets filters to DEFAULT_FILTERS first (replace behavior, not merge)
   - Then spreads the preset's filters on top: `{ ...DEFAULT_FILTERS, ...preset.filters }`
   - Uses setFilters to apply atomically

2. **matchedPreset** - A useMemo that:
   - Calls `getMatchedPreset(filters)` from `src/lib/filter-presets.ts`
   - Returns `FilterPreset | null`
   - Depends on `[filters]`

Add these imports at the top:
```typescript
import { FILTER_PRESETS, getMatchedPreset } from '@/lib/filter-presets';
import type { FilterPreset } from '@/types/map';
```

Add to the return object:
```typescript
return {
  filters,
  updateFilter,
  clearFilters,
  clearFilter,
  applyPreset,      // NEW
  matchedPreset,    // NEW
  hasActiveFilters: hasActive,
  activeFilterCount: activeCount,
};
```

IMPORTANT: Keep the existing DEFAULT_FILTERS const and all existing functions unchanged. Only ADD the two new items.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Verify the hook still exports all original properties plus the two new ones.
  </verify>
  <done>
- useMapFilters returns applyPreset (function) and matchedPreset (FilterPreset | null)
- applyPreset resets to defaults then applies preset filters (replace behavior)
- matchedPreset reactively updates when filters change
- All existing hook consumers continue to work without modification
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PresetBadges horizontal scrollable component</name>
  <files>src/components/map/preset-badges.tsx</files>
  <action>
Create `src/components/map/preset-badges.tsx` as a client component ('use client').

**Props interface:**
```typescript
interface PresetBadgesProps {
  onPresetSelect: (presetId: string) => void;
  matchedPresetId?: string | null;
  className?: string;
}
```

**Implementation:**

1. Import: `FILTER_PRESETS` from `@/lib/filter-presets`, `useI18n` from `@/lib/i18n`, `cn` from `@/lib/utils`, and lucide-react icons (Laptop, Heart, Zap).

2. Create an icon map to resolve string icon names from presets to actual components:
```typescript
const ICON_MAP: Record<string, LucideIcon> = {
  Laptop,
  Heart,
  Zap,
};
```

3. Render a scroll container with CSS scroll-snap:
```
<div className={cn("w-full", className)}>
  <div className="flex gap-2 overflow-x-auto pb-2 scrollbar-hide snap-x snap-mandatory">
    {FILTER_PRESETS.map(...)}
  </div>
</div>
```

4. Each badge is a `<button>` with:
   - `snap-start shrink-0` for scroll snapping
   - `min-h-[44px]` for WCAG AAA touch target
   - `px-4 py-2 rounded-full` for pill shape
   - `border-2 font-medium text-sm transition-colors` base styles
   - Active state (when `matchedPresetId === preset.id`): `border-primary bg-primary text-primary-foreground`
   - Inactive state: `border-border bg-background hover:border-primary/50 hover:bg-accent`
   - `aria-pressed={isActive}` for accessibility
   - `outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` for keyboard focus

5. Badge content: Icon (h-4 w-4) + translated label via `t(preset.labelKey)`

6. onClick calls `onPresetSelect(preset.id)`

Do NOT use CVA for this -- the variants are simple enough for inline cn() conditionals. Keep it straightforward.

Ensure the `scrollbar-hide` utility class works with Tailwind 4. If not available, add `[&::-webkit-scrollbar]:hidden [-ms-overflow-style:none] [scrollbar-width:none]` as fallback.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Verify the component renders 3 buttons (one per preset) with proper aria-pressed attribute.
  </verify>
  <done>
- PresetBadges component exports from src/components/map/preset-badges.tsx
- Renders 3 pill-shaped badges in a horizontal scrollable container
- Active badge has filled primary styling, inactive has outlined styling
- All badges are min 44px height for WCAG AAA touch targets
- Badges use i18n translations via useI18n()
- Each badge has aria-pressed for accessibility
- Scroll container uses CSS scroll-snap (no JS scroll management)
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. useMapFilters hook returns applyPreset and matchedPreset alongside all existing properties
3. PresetBadges component renders 3 badges with icons and translated labels
4. No breaking changes to existing MapWithFilters or MapFiltersPanel consumers
</verification>

<success_criteria>
- Hook extended without breaking existing consumers
- PresetBadges component is self-contained and reusable
- Both components are ready for integration into map and list pages
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-vibe-search-assistant/20-02-SUMMARY.md`
</output>
