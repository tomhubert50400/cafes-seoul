---
phase: 03-oauth-integration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/auth/oauth-buttons.tsx
  - src/components/auth/login-form.tsx
  - src/components/auth/signup-form.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
autonomous: false

must_haves:
  truths:
    - "OAuth buttons appear below email form with 'or' divider on login page"
    - "OAuth buttons appear below email form with 'or' divider on signup page"
    - "Kakao button appears first with yellow background"
    - "Google button appears second with outline style"
    - "OAuth errors display inline on auth pages"
    - "Clicking OAuth button redirects to provider"
  artifacts:
    - path: "src/components/auth/oauth-buttons.tsx"
      provides: "Reusable OAuth buttons component"
      exports: ["OAuthButtons"]
      min_lines: 50
    - path: "src/components/auth/login-form.tsx"
      provides: "Login form with OAuth buttons"
      contains: "OAuthButtons"
    - path: "src/components/auth/signup-form.tsx"
      provides: "Signup form with OAuth buttons"
      contains: "OAuthButtons"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page with error param handling"
      contains: "searchParams"
  key_links:
    - from: "src/components/auth/oauth-buttons.tsx"
      to: "loginWithOAuth"
      via: "server action call"
      pattern: "loginWithOAuth\\("
    - from: "src/components/auth/oauth-buttons.tsx"
      to: "window.location.href"
      via: "redirect to provider"
      pattern: "window\\.location\\.href.*url"
    - from: "src/app/(auth)/login/page.tsx"
      to: "LoginForm"
      via: "error prop passing"
      pattern: "error.*searchParams"
---

<objective>
Create OAuth buttons component and integrate into login/signup forms with error handling from URL params.

Purpose: Provides the user-facing OAuth UI - branded buttons following provider guidelines, proper placement below email form, and inline error display for OAuth failures.

Output: OAuthButtons component, updated login/signup forms with OAuth integration, and error query param handling on auth pages.
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-oauth-integration/03-CONTEXT.md
@.planning/phases/03-oauth-integration/03-RESEARCH.md
@.planning/phases/03-oauth-integration/03-01-SUMMARY.md

# Files to modify
@src/components/auth/login-form.tsx
@src/components/auth/signup-form.tsx
@src/app/(auth)/login/page.tsx
@src/app/(auth)/signup/page.tsx
@src/lib/i18n/translations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OAuth buttons component</name>
  <files>src/components/auth/oauth-buttons.tsx</files>
  <action>
Create reusable OAuth buttons component following CONTEXT.md decisions:

1. 'use client' directive at top
2. Import: useState, Button from ui/button, useI18n, loginWithOAuth from actions/auth
3. Create `OAuthButtons` component that:
   - Manages loading state: `useState<'google' | 'kakao' | null>(null)`
   - Manages error state: `useState<string | null>(null)`
   - Has `handleOAuthLogin(provider)` function that:
     - Sets loading state
     - Clears error
     - Calls loginWithOAuth(provider)
     - If error returned: set error state, clear loading
     - If url returned: `window.location.href = result.url`
   - Renders:
     - Divider with "or" text (from t('auth.oauth.divider'))
     - Error display (if error): `bg-destructive/10 p-3 text-sm text-destructive`
     - Kakao button FIRST (user decision):
       - `bg-[#FEE500] hover:bg-[#FEE500]/90 text-[#000000] font-medium`
       - Full width, outline variant base
       - Text: t('auth.oauth.kakao') or t('auth.oauth.loading') when loading
     - Google button SECOND:
       - Outline variant, full width
       - Text: t('auth.oauth.google') or t('auth.oauth.loading') when loading
     - Both buttons disabled when any loading

**Divider styling:**
```tsx
<div className="relative my-4">
  <div className="absolute inset-0 flex items-center">
    <div className="w-full border-t" />
  </div>
  <div className="relative flex justify-center text-xs uppercase">
    <span className="bg-background px-2 text-muted-foreground">
      {t('auth.oauth.divider')}
    </span>
  </div>
</div>
```

Export the component.
  </action>
  <verify>
File exists, exports OAuthButtons, contains Kakao button before Google button, has loading and error states.
  </verify>
  <done>
OAuthButtons component renders Kakao-first, Google-second buttons with brand colors, loading states, and error display.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate OAuth into login and signup forms</name>
  <files>
    src/components/auth/login-form.tsx
    src/components/auth/signup-form.tsx
  </files>
  <action>
Update both form components to include OAuth buttons:

**login-form.tsx:**
1. Import OAuthButtons from './oauth-buttons'
2. Add optional `oauthError?: string` prop to LoginForm
3. After the existing form content (after SubmitButton), add:
   - `<OAuthButtons />`
4. If oauthError prop provided, display it at top of form (before email field) in error style:
   - `bg-destructive/10 p-3 text-sm text-destructive rounded-md mb-4`

**signup-form.tsx:**
1. Import OAuthButtons from './oauth-buttons'
2. Add optional `oauthError?: string` prop to SignupForm
3. After the existing form content (after SubmitButton), add:
   - `<OAuthButtons />`
4. If oauthError prop provided, display it at top of form in error style

The OAuth buttons appear BELOW the submit button, which is BELOW the email/password form - this matches the "below email/password form with divider" requirement.
  </action>
  <verify>
Both forms import and render OAuthButtons. Both accept oauthError prop. TypeScript compiles.
  </verify>
  <done>
Login and signup forms include OAuth buttons below email/password form with error display capability.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add error query param handling to auth pages</name>
  <files>
    src/app/(auth)/login/page.tsx
    src/app/(auth)/signup/page.tsx
  </files>
  <action>
Update auth pages to read and pass error from URL query params:

**login/page.tsx:**
1. Add searchParams to page props: `{ searchParams }: { searchParams: Promise<{ error?: string }> }`
2. Await searchParams to get error: `const { error } = await searchParams`
3. Pass error to LoginForm: `<LoginForm oauthError={error} />`

**signup/page.tsx:**
1. Add searchParams to page props: `{ searchParams }: { searchParams: Promise<{ error?: string }> }`
2. Await searchParams to get error: `const { error } = await searchParams`
3. Pass error to SignupForm: `<SignupForm oauthError={error} />`

Note: In Next.js 15+, searchParams is a Promise that must be awaited. The page component should be async.
  </action>
  <verify>
Both pages read error from searchParams and pass to form components. Pages are async functions. TypeScript compiles.
  </verify>
  <done>
Auth pages display OAuth errors from URL query params inline in form area.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete OAuth integration: callback route, server action, buttons component, form integration, and error handling.
  </what-built>
  <how-to-verify>
**Prerequisites:**
- Google OAuth configured in Supabase Dashboard (Authentication -> Providers -> Google)
- Kakao OAuth configured in Supabase Dashboard (Authentication -> Providers -> Kakao)
- Redirect URIs added to both provider consoles

**Test OAuth flow:**
1. Start dev server: `npm run dev`
2. Navigate to http://localhost:3000/login
3. Verify: OAuth buttons appear BELOW email/password form with "or" divider
4. Verify: Kakao button (yellow) appears FIRST, Google button (outline) appears SECOND
5. Click "Continue with Kakao" - should redirect to Kakao login page
6. Cancel on Kakao page - should return to /login with "Login cancelled" message displayed inline
7. Click "Continue with Google" - should redirect to Google login page
8. Complete Google login - should redirect to home page, logged in
9. Test signup page has same OAuth buttons layout

**Test error handling:**
10. Navigate to http://localhost:3000/login?error=Test%20error
11. Verify: Error message displays inline in form area

**Test account linking (if existing email/password account):**
12. If you have an email/password account, try OAuth with same email
13. Should auto-link (same user, can login with either method)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. OAuthButtons component exists and renders correctly
2. Login form includes OAuthButtons below submit button
3. Signup form includes OAuthButtons below submit button
4. Login page reads error from searchParams
5. Signup page reads error from searchParams
6. `npm run build` succeeds
7. Visual: Kakao yellow button first, Google outline button second
8. Visual: "or" divider between form and OAuth buttons
9. Functional: OAuth flow redirects to provider and back
</verification>

<success_criteria>
- OAuth buttons visible on both login and signup pages
- Kakao button first with #FEE500 background
- Google button second with outline style
- OAuth errors display inline when present in URL
- Full OAuth flow works (redirect to provider -> callback -> logged in)
- Account linking works for matching verified emails
</success_criteria>

<output>
After completion, create `.planning/phases/03-oauth-integration/03-02-SUMMARY.md`
</output>
