---
phase: 02-email-password-auth
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/components/auth/login-form.tsx
  - src/components/auth/signup-form.tsx
  - src/components/auth/password-strength-meter.tsx
  - src/lib/hooks/use-debounced-value.ts
  - src/components/auth/logout-button.tsx
autonomous: true

must_haves:
  truths:
    - "User can navigate to /login and see a login form"
    - "User can navigate to /signup and see a signup form with password strength meter"
    - "Login form calls login Server Action on submit"
    - "Signup form calls signup Server Action on submit"
    - "Both forms show validation errors inline below fields"
    - "Both forms have password visibility toggle"
    - "Auth pages have minimal header (logo only)"
    - "Logged-in user can see and click a logout button on auth pages"
  artifacts:
    - path: "src/app/(auth)/layout.tsx"
      provides: "Auth layout with minimal header"
      min_lines: 15
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page"
      min_lines: 10
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Signup page"
      min_lines: 10
    - path: "src/components/auth/login-form.tsx"
      provides: "Login form component with React Hook Form"
      min_lines: 50
    - path: "src/components/auth/signup-form.tsx"
      provides: "Signup form component with password strength"
      min_lines: 60
    - path: "src/components/auth/password-strength-meter.tsx"
      provides: "Password strength visual indicator"
      min_lines: 25
    - path: "src/lib/hooks/use-debounced-value.ts"
      provides: "Debounce hook for password strength calculation"
      min_lines: 10
    - path: "src/components/auth/logout-button.tsx"
      provides: "Logout button component calling logout Server Action"
      min_lines: 15
  key_links:
    - from: "src/components/auth/login-form.tsx"
      to: "src/app/actions/auth.ts"
      via: "login action import"
      pattern: "import.*login.*from.*actions/auth"
    - from: "src/components/auth/signup-form.tsx"
      to: "src/app/actions/auth.ts"
      via: "signup action import"
      pattern: "import.*signup.*from.*actions/auth"
    - from: "src/components/auth/signup-form.tsx"
      to: "src/components/auth/password-strength-meter.tsx"
      via: "component import"
      pattern: "import.*PasswordStrengthMeter"
    - from: "src/components/auth/logout-button.tsx"
      to: "src/app/actions/auth.ts"
      via: "logout action import"
      pattern: "import.*logout.*from.*actions/auth"
---

<objective>
Create auth UI: route group layout, login page, signup page with forms, and logout button.

Purpose: Deliver the user-facing auth pages that call Server Actions and provide proper UX.
Output: Auth route group with layout, login/signup pages, form components, password strength meter, logout button.
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-email-password-auth/02-RESEARCH.md
@.planning/phases/02-email-password-auth/02-CONTEXT.md
@src/app/actions/auth.ts
@src/lib/validations/auth.ts
@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/ui/card.tsx
@src/lib/i18n/translations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth route group with layout and login page</name>
  <files>
    src/app/(auth)/layout.tsx
    src/app/(auth)/login/page.tsx
    src/components/auth/login-form.tsx
  </files>
  <action>
Create the (auth) route group with a minimal layout and login page.

**src/app/(auth)/layout.tsx:**
- Minimal header with logo only (no navigation links)
- Centered content area
- Logo links to home (/)
- Use the same CoffeeIcon from header.tsx

**src/app/(auth)/login/page.tsx:**
- Server Component that renders LoginForm
- Centered card layout
- Title: "Log in" / "로그인" (use i18n key)
- Link to signup: "Don't have an account? Sign up"

**src/components/auth/login-form.tsx:**
- Client Component ('use client')
- Import login action: `import { login } from '@/app/actions/auth'`
- Use React Hook Form with zodResolver(loginSchema)
- mode: 'onBlur' for validation
- Fields: email (type="email"), password (type="password" with toggle)
- Password visibility toggle using Eye/EyeOff icons from lucide-react
- Submit button: disabled during submission, shows "Logging in..." text
- Server error display below form
- Handle showResend response: if login returns showResend=true, show "Resend verification email" button
- Call resendVerification action when resend clicked

Form flow:
1. User fills form, onBlur validates format
2. User submits, formState.isSubmitting becomes true
3. Call login Server Action with FormData
4. If errors returned, set them to form with form.setError()
5. If showResend returned, display resend verification button
6. If success, redirect happens server-side

Use shadcn/ui components: Button, Input, Card, CardHeader, CardContent.
Use useI18n hook for translations (add auth.* keys usage, translations added in 02-04).
  </action>
  <verify>
1. `npm run dev` - navigate to /login, page renders
2. Form displays email and password fields
3. Password toggle works (shows/hides password)
4. Empty submit shows validation errors
5. TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
- (auth) route group created with layout.tsx
- /login page renders with login form
- Login form uses React Hook Form with Zod validation
- Login form imports login action from '@/app/actions/auth'
- Password visibility toggle works
- Submit button shows loading state
- Layout has minimal header with logo only
  </done>
</task>

<task type="auto">
  <name>Task 2: Create signup page with password strength meter</name>
  <files>
    src/app/(auth)/signup/page.tsx
    src/components/auth/signup-form.tsx
    src/components/auth/password-strength-meter.tsx
    src/lib/hooks/use-debounced-value.ts
  </files>
  <action>
Create the signup page and password strength meter component.

**src/app/(auth)/signup/page.tsx:**
- Server Component that renders SignupForm
- Same card layout as login
- Title: "Create account" / "회원가입"
- Link to login: "Already have an account? Log in"

**src/components/auth/signup-form.tsx:**
- Client Component ('use client')
- Import signup action: `import { signup } from '@/app/actions/auth'`
- Import PasswordStrengthMeter: `import { PasswordStrengthMeter } from '@/components/auth/password-strength-meter'`
- Use React Hook Form with zodResolver(signupSchema)
- mode: 'onBlur' for validation
- Fields: email, password with toggle
- Password strength meter below password field (only shows when password has content)
- Submit button: disabled during submission, shows "Creating account..." text
- On success: redirect happens server-side, show toast "Check your email" on home page

For toast: use sonner's toast() function. Import from 'sonner'.
Since redirect happens server-side, the toast needs to be triggered on the destination page.
Option: Pass ?message=check-email in redirect URL, read on home page and show toast.

Simpler approach: The signup action redirects to /?registered=true, and the home page shows toast if that param exists.

**src/components/auth/password-strength-meter.tsx:**
- Client Component
- Props: password (string)
- Uses zxcvbn from zxcvbn-ts to calculate strength (score 0-4)
- Visual: 5 bars that fill based on score
- Colors: 0-1 red (weak), 2 yellow (fair), 3 blue (good), 4 green (strong)
- Label text: Weak, Weak, Fair, Good, Strong (matching score)
- Only renders if password is not empty
- Debounce calculation by 300ms to avoid performance issues
- Import debounce hook: `import { useDebouncedValue } from '@/lib/hooks/use-debounced-value'`

**src/lib/hooks/use-debounced-value.ts:**
Create the debounce hook used by password strength meter:
```typescript
import { useState, useEffect } from 'react'

export function useDebouncedValue<T>(value: T, delay: number): T {
  const [debouncedValue, setDebouncedValue] = useState(value)

  useEffect(() => {
    const timer = setTimeout(() => setDebouncedValue(value), delay)
    return () => clearTimeout(timer)
  }, [value, delay])

  return debouncedValue
}
```

Password strength meter example from research:
```typescript
import { zxcvbn } from '@zxcvbn-ts/core'
import { useEffect, useState } from 'react'
import { useDebouncedValue } from '@/lib/hooks/use-debounced-value'

export function PasswordStrengthMeter({ password }: { password: string }) {
  const debouncedPassword = useDebouncedValue(password, 300)
  const [strength, setStrength] = useState(0)

  useEffect(() => {
    if (debouncedPassword) {
      const result = zxcvbn(debouncedPassword)
      setStrength(result.score)
    } else {
      setStrength(0)
    }
  }, [debouncedPassword])

  if (!password) return null

  const colors = ['bg-red-500', 'bg-red-500', 'bg-yellow-500', 'bg-blue-500', 'bg-green-500']
  const labels = ['Weak', 'Weak', 'Fair', 'Good', 'Strong']

  return (
    <div className="mt-2">
      <div className="flex gap-1 h-1">
        {[...Array(5)].map((_, i) => (
          <div
            key={i}
            className={`flex-1 rounded ${i <= strength ? colors[strength] : 'bg-muted'}`}
          />
        ))}
      </div>
      <p className="text-xs text-muted-foreground mt-1">{labels[strength]}</p>
    </div>
  )
}
```
  </action>
  <verify>
1. `npm run dev` - navigate to /signup, page renders
2. Form displays email and password fields
3. Password strength meter appears when typing password
4. Strength meter updates as password changes (debounced)
5. Empty submit shows validation errors
6. TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
- /signup page renders with signup form
- Signup form uses React Hook Form with Zod validation
- Signup form imports signup action from '@/app/actions/auth'
- Signup form imports PasswordStrengthMeter from '@/components/auth/password-strength-meter'
- Password strength meter shows visual feedback
- Strength meter is debounced (no lag while typing)
- useDebouncedValue hook created in src/lib/hooks/
- Submit button shows loading state
- Link to login page works
  </done>
</task>

<task type="auto">
  <name>Task 3: Create logout button component</name>
  <files>
    src/components/auth/logout-button.tsx
    src/app/(auth)/layout.tsx
  </files>
  <action>
Create a logout button component and add it to the auth layout for testing logout functionality.

**src/components/auth/logout-button.tsx:**
- Client Component ('use client')
- Import logout action: `import { logout } from '@/app/actions/auth'`
- Simple button that calls logout Server Action on click
- Use form action pattern: wrap button in form with action={logout}
- Button text: "Log out" / "로그아웃" (use i18n key)
- Style: ghost variant, small size

```typescript
'use client'

import { logout } from '@/app/actions/auth'
import { Button } from '@/components/ui/button'
import { useI18n } from '@/lib/i18n/client'

export function LogoutButton() {
  const { t } = useI18n()

  return (
    <form action={logout}>
      <Button type="submit" variant="ghost" size="sm">
        {t('auth.logout')}
      </Button>
    </form>
  )
}
```

**Update src/app/(auth)/layout.tsx:**
- Add LogoutButton to the header, positioned to the right of the logo
- Only show if user is logged in (check via server-side getUser())
- This provides a way to test logout during Phase 2 verification

Layout should conditionally render:
- If logged in: show LogoutButton in header
- If not logged in: header shows logo only

Use createClient from '@/lib/supabase/server' to check auth state.
  </action>
  <verify>
1. `npm run dev` - navigate to /login while logged in
2. LogoutButton visible in header
3. Click logout - user is logged out and redirected to /login
4. When logged out, logout button not visible
5. TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
- LogoutButton component created at src/components/auth/logout-button.tsx
- LogoutButton imports logout action from '@/app/actions/auth'
- Auth layout conditionally shows LogoutButton when user is logged in
- Clicking logout calls the logout Server Action and redirects to /login
- AUTH-07 requirement (logout) is now testable in Phase 2
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. `npm run dev` and visit /login - form renders and functions
2. Visit /signup - form renders with password strength meter
3. Both pages have minimal header (logo only when logged out, logo + logout when logged in)
4. Password visibility toggle works on both forms
5. Validation errors appear on blur
6. When logged in, logout button appears and works
7. `npm run build` passes
</verification>

<success_criteria>
- [ ] (auth) route group with layout created
- [ ] /login page with LoginForm component
- [ ] /signup page with SignupForm component
- [ ] Password strength meter shows 5-bar visual with labels
- [ ] Both forms use React Hook Form + Zod
- [ ] Password visibility toggle on both forms
- [ ] Loading states on submit buttons
- [ ] LogoutButton component created and functional
- [ ] Logout button visible when logged in on auth pages
- [ ] Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-email-password-auth/02-03-SUMMARY.md`
</output>
