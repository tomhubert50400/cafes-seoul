---
phase: 19-style-and-mobile-responsive-check
plan: 05
type: execute
wave: 3
depends_on: ["19-02", "19-03", "19-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All pages pass visual inspection at 320px, 375px, 768px, and 1024px widths"
    - "No horizontal scrollbar on any page at any tested width"
    - "User can navigate the entire app on a mobile device"
  artifacts: []
  key_links: []
---

<objective>
Run the dev server and verify all responsive fixes across every page in the application. Build the project to confirm no errors.

Purpose: This is the final verification step. All previous plans have made responsive fixes, and now we confirm everything works together by running the build and documenting the verification results.
Output: Confirmed build passes, verification notes for user to do final visual check.
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-style-and-mobile-responsive-check/19-01-SUMMARY.md
@.planning/phases/19-style-and-mobile-responsive-check/19-02-SUMMARY.md
@.planning/phases/19-style-and-mobile-responsive-check/19-03-SUMMARY.md
@.planning/phases/19-style-and-mobile-responsive-check/19-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and final responsive sweep</name>
  <files></files>
  <action>
    1. Run `npm run build` and ensure the project compiles with zero errors
    2. If there are TypeScript or build errors from the responsive changes in plans 01-04, fix them
    3. Run `npm run lint` if a lint script exists, fix any linting errors introduced
    4. Start the dev server with `npm run dev` in the background
    5. Use the dev server to check that every page route loads without errors:
       - / (home)
       - /cafes (listing)
       - /map
       - /roulette
       - /login
       - /signup
       - /admin (if accessible)
    6. If any pages throw runtime errors, fix the issues in the relevant files
  </action>
  <verify>Run `npm run build` - must complete with zero errors. All page routes load without runtime errors.</verify>
  <done>Project builds successfully. No TypeScript errors. All page routes load without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Responsive fixes across all pages: viewport metadata, header/footer, home page, cafes listing, cafe detail, map, auth pages, dashboard, profile, settings, roulette, and admin panel.</what-built>
  <how-to-verify>
    Start the dev server (`npm run dev`) and open Chrome DevTools (F12). Toggle device mode (Ctrl+Shift+M).

    Test at these widths: 320px, 375px, 768px, 1024px

    **Check each page for:**
    - No horizontal scrollbar
    - Text is readable (not too small, not overflowing)
    - Buttons and links are tappable (not too small or overlapping)
    - Images are contained (not stretching beyond viewport)
    - Forms are usable (inputs accessible, buttons reachable)

    **Page checklist:**
    1. Home page (/) - hero section, search bar, badges
    2. Cafes listing (/cafes) - filters, grid, pagination
    3. Cafe detail (/cafes/[any-slug]) - images, ratings, reviews
    4. Map (/map) - map fills screen, controls accessible
    5. Roulette (/roulette) - spin button, animation, result card
    6. Login (/login) - form centered, inputs sized
    7. Signup (/signup) - form centered, all fields
    8. Dashboard (/dashboard) - stats cards, content
    9. Profile (/profile) - tabs navigable
    10. Profile Settings (/profile/settings) - forms, toggles
    11. Submit (/submit) - form fields, upload area
    12. Admin (/admin) - sidebar/nav, tables scrollable

    **Also test with French language** (longest translations) to verify no text overflow.
  </how-to-verify>
  <resume-signal>Type "approved" if all pages look good, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. All pages load without runtime errors
3. Human verification confirms no visual issues across all tested viewports
</verification>

<success_criteria>
- Build passes with zero errors
- Human confirms all pages render correctly on mobile viewports
- No horizontal scrollbar on any page
- All interactive elements are accessible on touch devices
</success_criteria>

<output>
After completion, create `.planning/phases/19-style-and-mobile-responsive-check/19-05-SUMMARY.md`
</output>
