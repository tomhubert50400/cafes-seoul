---
phase: 19-style-and-mobile-responsive-check
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/app/roulette/page.tsx
  - src/components/roulette/roulette-client.tsx
  - src/components/roulette/roulette-idle.tsx
  - src/components/roulette/roulette-spinner.tsx
  - src/components/roulette/roulette-result.tsx
  - src/components/roulette/roulette-filter-sheet.tsx
  - src/app/admin/layout.tsx
  - src/components/admin/admin-sidebar.tsx
  - src/components/admin/cafes-table.tsx
  - src/components/admin/submissions-table.tsx
  - src/components/admin/photos-table.tsx
autonomous: true

must_haves:
  truths:
    - "Roulette spinner animation works on mobile without overflow"
    - "Roulette result card is fully readable on mobile"
    - "Roulette filter sheet is usable on mobile touch devices"
    - "Admin sidebar collapses or is hidden on mobile"
    - "Admin tables are horizontally scrollable on mobile"
  artifacts:
    - path: "src/components/roulette/roulette-spinner.tsx"
      provides: "Mobile-friendly roulette animation"
      contains: "overflow-hidden"
    - path: "src/components/roulette/roulette-result.tsx"
      provides: "Responsive result card"
    - path: "src/components/admin/admin-sidebar.tsx"
      provides: "Responsive admin navigation"
  key_links:
    - from: "src/components/roulette/roulette-client.tsx"
      to: "src/components/roulette/roulette-spinner.tsx"
      via: "Phase state machine"
      pattern: "RouletteSpinner"
    - from: "src/app/admin/layout.tsx"
      to: "src/components/admin/admin-sidebar.tsx"
      via: "Layout composition"
      pattern: "AdminSidebar"
---

<objective>
Fix responsive layout for the Roulette feature (newest feature, likely untested on mobile) and the Admin panel (tables and sidebar).

Purpose: The Roulette feature is NEW and was likely built without thorough mobile testing. The spinner animation, result card, and filter sheet all need mobile verification. The Admin panel has tables that typically break on mobile.
Output: Roulette works smoothly on mobile. Admin panel is usable on mobile with scrollable tables.
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-style-and-mobile-responsive-check/19-RESEARCH.md
@src/app/roulette/page.tsx
@src/components/roulette/roulette-client.tsx
@src/components/roulette/roulette-idle.tsx
@src/components/roulette/roulette-spinner.tsx
@src/components/roulette/roulette-result.tsx
@src/components/roulette/roulette-filter-sheet.tsx
@src/app/admin/layout.tsx
@src/components/admin/admin-sidebar.tsx
@src/components/admin/cafes-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Roulette feature responsive layout</name>
  <files>src/app/roulette/page.tsx, src/components/roulette/roulette-client.tsx, src/components/roulette/roulette-idle.tsx, src/components/roulette/roulette-spinner.tsx, src/components/roulette/roulette-result.tsx, src/components/roulette/roulette-filter-sheet.tsx</files>
  <action>
    **Roulette Page (src/app/roulette/page.tsx):**
    1. Already uses `max-w-2xl mx-auto px-4 py-8` which is good for mobile
    2. Reduce padding on mobile: `px-4 py-4 md:py-8`

    **Roulette Client (src/components/roulette/roulette-client.tsx):**
    1. Title section uses `text-3xl` - change to `text-2xl md:text-3xl` for mobile
    2. The filter bar with `flex items-center justify-between gap-2` is fine
    3. The cafe count text has `shrink-0` which is good

    **Roulette Idle (src/components/roulette/roulette-idle.tsx):**
    1. Read the file first
    2. The "Spin" button should be large and prominent on mobile: ensure it has `min-h-[48px]` or similar for easy tapping
    3. Any decorative elements or animations: verify they don't cause overflow
    4. The "no cafes match" empty state: ensure text wraps properly

    **Roulette Spinner (src/components/roulette/roulette-spinner.tsx):**
    CRITICAL - This is the most likely to break on mobile:
    1. The spinner uses a fixed ITEM_WIDTH of 120px with 50 reel items. The framer-motion animation scrolls horizontally. This is contained in `overflow-hidden` which is correct.
    2. Verify the container `w-full` properly constrains the reel viewport
    3. The fade edges use `w-12 sm:w-16` which is already responsive - good
    4. The center indicator arrows are absolutely positioned at `left-1/2` which should work
    5. Each reel item has `flex-shrink-0` with fixed 120px width - this is intentional for the reel effect. Verify the overflow-hidden on the reel viewport contains everything
    6. On very narrow screens (320px), the reel items (120px each) should still be visible through the overflow-hidden viewport. The animation should work identically since it's CSS-transform based
    7. Make sure the reel item cafe name truncation `truncate w-full` works properly at 120px width

    **Roulette Result (src/components/roulette/roulette-result.tsx):**
    1. The result card already uses responsive padding `px-4 py-5 sm:p-6` - good
    2. The cafe image uses `aspect-video w-full` which is responsive - good
    3. The action buttons grid uses `grid grid-cols-2 gap-3` - verify this doesn't cause text overflow on mobile. The buttons already have `min-w-0` and `truncate` which is good
    4. The static map is `h-[200px]` which works on mobile
    5. Footer buttons stack vertically with `flex flex-col` which is good
    6. Check the Kakao directions button: long translated text in some languages may overflow. Verify `truncate` handles this

    **Roulette Filter Sheet (src/components/roulette/roulette-filter-sheet.tsx):**
    1. Read the file to understand if it uses a Sheet/Drawer component
    2. If it's a Sheet: ensure it opens from the bottom on mobile (`side="bottom"` or full-width from side)
    3. Ensure the sheet content is scrollable: `overflow-y-auto max-h-[80vh]`
    4. Filter controls (selects, toggles, checkboxes): ensure adequate touch targets (min 44px)
    5. Apply/Clear buttons should be sticky at bottom or prominent
  </action>
  <verify>Run `npm run build` to confirm no errors. At 320px: roulette idle state shows spin button prominently, spinner animation runs without overflow, result card is readable with tappable buttons, filter sheet opens and is usable.</verify>
  <done>Roulette spinner animation is contained within viewport at 320px. Result card renders cleanly on mobile. Filter sheet is scrollable and controls are tappable. No overflow on the roulette page at any viewport width.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Admin panel responsive layout</name>
  <files>src/app/admin/layout.tsx, src/components/admin/admin-sidebar.tsx, src/components/admin/admin-nav.tsx, src/components/admin/cafes-table.tsx, src/components/admin/submissions-table.tsx, src/components/admin/photos-table.tsx, src/components/admin/admin-stats.tsx</files>
  <action>
    **Admin Layout (src/app/admin/layout.tsx):**
    1. Currently uses `flex` with AdminSidebar + main content. The sidebar may push content off-screen on mobile.
    2. The main content already has `min-w-0 overflow-x-hidden p-4 pt-16 md:p-6 md:pt-6` - the `pt-16` on mobile suggests the sidebar might be a fixed-position top bar on mobile. Read AdminSidebar to verify.

    **Admin Sidebar (src/components/admin/admin-sidebar.tsx):**
    1. Read the file to understand current responsive behavior
    2. If it's a persistent sidebar: on mobile it should either:
       a. Collapse to a hamburger menu/drawer, OR
       b. Move to a horizontal nav bar at the top
    3. If it already has mobile handling (the `pt-16` in layout suggests it does): verify it works
    4. If it's a full sidebar on all screens: add responsive classes to hide on mobile and show a mobile nav alternative
    5. Ensure nav links have adequate touch targets

    **Admin Nav (src/components/admin/admin-nav.tsx):**
    1. Read to understand if this is the mobile nav alternative
    2. Ensure nav items are tappable with min 44px height

    **Admin Tables (cafes-table.tsx, submissions-table.tsx, photos-table.tsx):**
    1. Read each table file
    2. Tables are notoriously bad on mobile. The standard fix: wrap each table in a container with `overflow-x-auto`:
       ```tsx
       <div className="overflow-x-auto -mx-4 px-4 md:mx-0 md:px-0">
         <table className="min-w-[600px] w-full">...</table>
       </div>
       ```
    3. The `-mx-4 px-4` pattern allows the table to extend to screen edges on mobile for maximum width, while maintaining padding on the scroll container
    4. Alternatively, if tables use a Table component from UI library, wrap the component
    5. Ensure table cells don't use fixed widths that are too wide
    6. For action columns (approve/reject buttons): ensure buttons are not too small. On mobile, consider icon-only buttons with tooltips

    **Admin Stats (src/components/admin/admin-stats.tsx):**
    1. Read the file
    2. Stats cards should use responsive grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4`
  </action>
  <verify>Run `npm run build` to confirm no errors. At 320px: admin sidebar/nav is accessible, tables scroll horizontally without breaking layout, stats cards stack on mobile, action buttons are tappable.</verify>
  <done>Admin sidebar is accessible on mobile (collapsed/drawer or horizontal nav). Tables are horizontally scrollable on mobile. Stats cards stack on mobile. All admin action buttons meet touch target requirements.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. Roulette at 320px: all three phases (idle, spinning, result) render without overflow
3. Roulette spinner animation is smooth and contained
4. Roulette filter sheet opens and is usable on mobile
5. Admin at 320px: navigation accessible, tables scrollable, stats cards stack
</verification>

<success_criteria>
- Roulette spinner animation contained in overflow-hidden viewport at all screen widths
- Roulette result card readable on mobile with tappable action buttons
- Roulette filter controls have adequate touch targets
- Admin sidebar/nav is accessible on mobile
- Admin tables horizontally scrollable without breaking page layout
- No horizontal scrollbar on roulette or admin pages at 320px
</success_criteria>

<output>
After completion, create `.planning/phases/19-style-and-mobile-responsive-check/19-04-SUMMARY.md`
</output>
