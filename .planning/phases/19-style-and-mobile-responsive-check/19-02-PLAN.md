---
phase: 19-style-and-mobile-responsive-check
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/components/home/hero-section.tsx
  - src/app/cafes/page.tsx
  - src/components/search-filters.tsx
  - src/components/cafe-card.tsx
  - src/components/cafe-list.tsx
  - src/components/cafe-detail/cafe-detail-content.tsx
  - src/app/cafes/[slug]/page.tsx
  - src/app/map/page.tsx
  - src/components/map/map-filters.tsx
  - src/components/map/cafe-detail-panel.tsx
autonomous: true

must_haves:
  truths:
    - "Home hero section is readable and search bar is usable at 320px width"
    - "Cafe listing grid adapts from 1 column on mobile to 2-3 columns on larger screens"
    - "Search filters wrap properly on mobile without horizontal overflow"
    - "Cafe detail page content is readable on mobile with no overflow"
    - "Map page fills viewport height on mobile with accessible filter controls"
  artifacts:
    - path: "src/components/home/hero-section.tsx"
      provides: "Responsive hero with mobile-friendly search"
      contains: "max-w"
    - path: "src/components/search-filters.tsx"
      provides: "Mobile-friendly filter layout"
      contains: "flex-wrap"
    - path: "src/components/map/map-filters.tsx"
      provides: "Mobile-friendly map filter controls"
  key_links:
    - from: "src/app/cafes/page.tsx"
      to: "src/components/search-filters.tsx"
      via: "SearchFilters component"
      pattern: "SearchFilters"
    - from: "src/app/map/page.tsx"
      to: "src/components/map/map-filters.tsx"
      via: "Map filter UI"
      pattern: "map-filters"
---

<objective>
Fix responsive layout issues on all public-facing pages: Home, Cafes listing, Cafe detail, and Map.

Purpose: These are the most-visited pages. Users on mobile phones must be able to browse cafes, use search/filters, read cafe details, and use the map without layout issues.
Output: All public pages render correctly at 320px, 640px, 768px, and 1024px+ widths.
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-style-and-mobile-responsive-check/19-RESEARCH.md
@src/components/home/hero-section.tsx
@src/app/cafes/page.tsx
@src/components/search-filters.tsx
@src/components/cafe-card.tsx
@src/components/cafe-list.tsx
@src/components/cafe-detail/cafe-detail-content.tsx
@src/app/map/page.tsx
@src/components/map/map-filters.tsx
@src/components/map/cafe-detail-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Home page hero and Cafes listing responsive layout</name>
  <files>src/components/home/hero-section.tsx, src/app/cafes/page.tsx, src/components/search-filters.tsx, src/components/cafe-card.tsx, src/components/cafe-list.tsx, src/components/cafes/page-header.tsx</files>
  <action>
    **Hero Section (src/components/home/hero-section.tsx):**
    1. The hero is mostly responsive already. Verify and fix:
       - Reduce vertical padding on mobile: change `py-20` to `py-12 md:py-20 lg:py-32` (currently `py-20 md:py-32`)
       - Search bar form: the `flex max-w-xl gap-2` may cause the search button to be too small on narrow screens. Change to stack on very narrow screens: `flex flex-col sm:flex-row max-w-xl gap-2` so on mobile the search input and button stack vertically with full width
       - Search button: add `w-full sm:w-auto` to ensure it's full width when stacked
       - Popular neighborhoods badges: already use `flex-wrap` - verify they don't overflow at 320px. Add `px-2` to the container if needed for padding
    2. Ensure search input has `h-12 text-base` for good mobile touch targets (already present)

    **Cafes Page (src/app/cafes/page.tsx):**
    1. The header section "flex items-start justify-between" can cause issues when the page title is long in some languages. Add `gap-4` and ensure the Add Cafe button wraps properly: `flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 mb-8`
    2. The "Add Cafe" button text may be too long on mobile. Consider: keep as-is since it's short, but ensure it has `shrink-0` (already present)

    **Search Filters (src/components/search-filters.tsx):**
    1. The Select triggers have fixed `w-[140px]` which may not fit well on very narrow screens. Change to `w-full sm:w-[140px]` so they stack full-width on mobile
    2. The filter row uses `flex flex-wrap` which is good, but on mobile the selects should be full width. Wrap the filter row in a responsive layout: on mobile use `grid grid-cols-1 gap-2` and on sm+ use the existing `flex flex-wrap items-center gap-2`
    3. Alternatively, keep the flex-wrap but change selects to not have a fixed width on mobile: `w-full min-w-[140px] sm:w-[140px]`

    **Cafe Card (src/components/cafe-card.tsx):**
    1. Read the file first to understand current layout
    2. Ensure card images use `w-full` and `object-cover` with proper aspect ratio
    3. Ensure cafe name text uses `truncate` or `line-clamp-2` to prevent overflow
    4. Ensure rating display and badges don't overflow the card width

    **Cafe List (src/components/cafe-list.tsx):**
    1. Read the file to check the grid setup
    2. Ensure grid is responsive: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
    3. Add `overflow-hidden` to the grid container if not already present
  </action>
  <verify>Run `npm run build` to confirm no errors. Check in browser DevTools at 320px width: hero search bar is usable, cafes listing shows 1 column, filters don't overflow, cafe cards render without text overflow.</verify>
  <done>Hero search stacks vertically on mobile. Cafe grid is 1 column on mobile, 2 on sm, 3 on lg. Search filters don't cause horizontal overflow. Cafe cards don't have text overflow.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Cafe detail page and Map page responsive layout</name>
  <files>src/app/cafes/[slug]/page.tsx, src/components/cafe-detail/cafe-detail-content.tsx, src/app/map/page.tsx, src/components/map/map-filters.tsx, src/components/map/cafe-detail-panel.tsx</files>
  <action>
    **Cafe Detail (src/components/cafe-detail/cafe-detail-content.tsx):**
    1. Read the file first to understand the full layout
    2. Common issues to fix:
       - Image gallery: ensure images use `w-full max-w-full` with `object-cover`
       - Rating dimensions grid: should be 1 column on mobile, 2+ columns on larger screens. Look for grid layouts and ensure responsive columns: `grid grid-cols-1 sm:grid-cols-2 gap-4`
       - Address and contact info: ensure long text uses `break-words` or `truncate`
       - Review text: ensure user-generated content uses `break-words` to prevent overflow from long URLs or words
       - Any fixed-width elements: replace with responsive `max-w-full` or `w-full`
    3. Ensure the main content container has proper mobile padding: `px-4`

    **Cafe Detail Page (src/app/cafes/[slug]/page.tsx):**
    1. Read the file to check layout structure
    2. Ensure the page container uses `max-w-6xl mx-auto px-4` consistently
    3. Check for any two-column layouts that should stack on mobile

    **Map Page (src/app/map/page.tsx):**
    1. The map already fills viewport height with `calc(100vh - 56px)` which is good
    2. The floating "Add Cafe" button at `bottom-6 right-6` is fine for mobile
    3. On mobile, the text "Add Cafe" is hidden with `hidden sm:inline` - verify the button with just the Plus icon is still large enough as a touch target. If the button is too small without text, add explicit sizing: `h-12 w-12 sm:h-auto sm:w-auto`

    **Map Filters (src/components/map/map-filters.tsx):**
    1. Read the file to understand the filter panel layout
    2. If filters are in a sidebar: on mobile they should be in a drawer/sheet or collapsible panel, not blocking the map
    3. If filters are overlay controls: ensure they don't cover the entire map on mobile
    4. Ensure filter controls have adequate touch target sizes (min 44px recommended for map controls)

    **Cafe Detail Panel on Map (src/components/map/cafe-detail-panel.tsx):**
    1. Read the file to understand the info panel layout
    2. On mobile: the detail panel should not exceed viewport height. Add `max-h-[60vh] overflow-y-auto` if needed
    3. Ensure the panel has proper padding and the close button is easily tappable (min 44px)
  </action>
  <verify>Run `npm run build` to confirm no errors. Check cafe detail page at 320px: content readable, no horizontal overflow, images responsive. Check map page at 320px: map fills screen, filter controls accessible, detail panel scrollable.</verify>
  <done>Cafe detail content renders properly on mobile with responsive images and grids. Map fills viewport on mobile with accessible controls. Map detail panel is scrollable and doesn't exceed viewport.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. Home hero at 320px: title readable, search bar usable (stacked or single-line), badges wrap
3. Cafes listing at 320px: single column grid, filters don't overflow, cards render cleanly
4. Cafe detail at 320px: images responsive, rating grid stacks, text doesn't overflow
5. Map at 320px: fills viewport, floating button is tappable, filters accessible
</verification>

<success_criteria>
- No horizontal scrollbar on Home, Cafes, Cafe Detail, or Map pages at 320px
- Cafe listing grid is 1 column on mobile, 2 on tablet, 3 on desktop
- Search filters don't cause overflow and are usable on mobile
- Cafe detail content is readable on mobile
- Map controls and panels are usable on mobile touch devices
</success_criteria>

<output>
After completion, create `.planning/phases/19-style-and-mobile-responsive-check/19-02-SUMMARY.md`
</output>
