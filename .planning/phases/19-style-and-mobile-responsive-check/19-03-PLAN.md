---
phase: 19-style-and-mobile-responsive-check
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page-client.tsx
  - src/app/(auth)/signup/page-client.tsx
  - src/app/dashboard/page.tsx
  - src/app/dashboard/layout.tsx
  - src/app/profile/layout.tsx
  - src/app/profile/settings/page.tsx
  - src/components/settings/settings-tabs.tsx
  - src/components/settings/security-section.tsx
  - src/components/settings/notifications-section.tsx
  - src/app/submit/page.tsx
  - src/app/(auth)/verify-email/page-client.tsx
  - src/app/profile/page.tsx
  - src/app/profile/reviews/page.tsx
  - src/app/profile/favorites/page.tsx
  - src/app/profile/submissions/page.tsx
autonomous: true

must_haves:
  truths:
    - "Auth pages (login, signup) render centered and usable on mobile"
    - "Dashboard page content is readable on mobile without overflow"
    - "Profile tab navigation wraps properly on narrow screens"
    - "Settings forms are usable on mobile with proper input sizing"
    - "Cafe submission form is usable on mobile"
  artifacts:
    - path: "src/app/(auth)/login/page-client.tsx"
      provides: "Mobile-friendly login form"
    - path: "src/app/profile/layout.tsx"
      provides: "Responsive profile tab navigation"
      contains: "flex-wrap"
    - path: "src/components/settings/settings-tabs.tsx"
      provides: "Mobile-friendly settings layout"
  key_links:
    - from: "src/app/profile/layout.tsx"
      to: "src/app/profile/settings/page.tsx"
      via: "Tab navigation"
      pattern: "TabsTrigger"
---

<objective>
Fix responsive layout issues on auth pages (login, signup), user dashboard, profile pages (tabs, settings, reviews, favorites), and the cafe submission form.

Purpose: Logged-in users interact with these pages for account management. Profile tabs, settings forms, and the submission form must be usable on mobile phones.
Output: All authenticated user pages render correctly at 320px, 640px, 768px, and 1024px+ widths.
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-style-and-mobile-responsive-check/19-RESEARCH.md
@src/app/(auth)/layout.tsx
@src/app/(auth)/login/page-client.tsx
@src/app/(auth)/signup/page-client.tsx
@src/app/dashboard/page.tsx
@src/app/dashboard/layout.tsx
@src/app/profile/layout.tsx
@src/app/profile/settings/page.tsx
@src/components/settings/settings-tabs.tsx
@src/app/submit/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix auth pages and dashboard responsive layout</name>
  <files>src/app/(auth)/layout.tsx, src/app/(auth)/login/page-client.tsx, src/app/(auth)/signup/page-client.tsx, src/app/(auth)/verify-email/page-client.tsx, src/app/dashboard/page.tsx, src/app/dashboard/layout.tsx</files>
  <action>
    **Auth Layout (src/app/(auth)/layout.tsx):**
    1. The auth layout is already well-structured with `flex-1 flex items-center justify-center p-4`
    2. Verify the minimal header doesn't overflow on mobile (site name "Seoul Cafes" is always shown - consider hiding on very narrow screens or it's probably fine since it's short)
    3. Ensure the content area has `w-full max-w-md` or similar constraint so forms don't stretch too wide

    **Login Page Client (src/app/(auth)/login/page-client.tsx):**
    1. Read the file to understand the form layout
    2. Ensure the login form card/container has: `w-full max-w-md mx-auto` for proper mobile width
    3. Ensure form inputs have `h-11 text-base` for adequate touch targets and to prevent iOS zoom on focus (inputs with text-base or larger prevent auto-zoom)
    4. Ensure OAuth buttons (Google, Kakao) are full width on mobile: `w-full`
    5. Ensure submit button is full width: `w-full`
    6. Check spacing between elements: `space-y-4` minimum for touch-friendly spacing

    **Signup Page Client (src/app/(auth)/signup/page-client.tsx):**
    1. Read and apply same fixes as login page
    2. If signup has more fields, ensure they stack vertically on mobile
    3. Ensure password strength meter (zxcvbn) doesn't overflow on mobile

    **Verify Email Page (src/app/(auth)/verify-email/page-client.tsx):**
    1. Read the file - this is typically a simple message page
    2. Ensure text content is centered and readable on mobile

    **Dashboard Layout (src/app/dashboard/layout.tsx):**
    1. Currently uses `container mx-auto py-8 px-4` - the `container` class may have different behavior in Tailwind v4. Verify it works or change to explicit `max-w-6xl mx-auto`
    2. Ensure consistent with other layouts

    **Dashboard Page (src/app/dashboard/page.tsx):**
    1. Read the file to understand the stats/contributions layout
    2. Stats cards: ensure they use responsive grid `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
    3. Any tables or lists: ensure they don't cause horizontal overflow. For tables, wrap in `overflow-x-auto`
    4. Contribution activity: ensure cards/items have proper mobile padding
  </action>
  <verify>Run `npm run build` to confirm no errors. Check login/signup pages at 320px: form fills width properly, inputs are tappable, OAuth buttons visible. Check dashboard at 320px: stats cards stack, no horizontal overflow.</verify>
  <done>Auth forms render centered on mobile with full-width inputs and buttons. Dashboard stats stack on mobile. No overflow on any auth or dashboard page at 320px.</done>
</task>

<task type="auto">
  <name>Task 2: Fix profile pages, settings, and submission form responsive layout</name>
  <files>src/app/profile/layout.tsx, src/app/profile/page.tsx, src/app/profile/reviews/page.tsx, src/app/profile/favorites/page.tsx, src/app/profile/submissions/page.tsx, src/app/profile/settings/page.tsx, src/components/settings/settings-tabs.tsx, src/components/settings/security-section.tsx, src/components/settings/notifications-section.tsx, src/app/submit/page.tsx</files>
  <action>
    **Profile Layout (src/app/profile/layout.tsx):**
    1. The TabsList already has `flex-wrap gap-2` which is good for mobile wrapping
    2. However, with 5 tabs (Overview, Reviews, Favorites, Submissions, Settings), they may wrap awkwardly. Consider:
       - On mobile: make tabs scrollable horizontally with `overflow-x-auto flex-nowrap` OR keep flex-wrap but ensure proper visual separation
       - Recommended: use `overflow-x-auto` with `scrollbar-hide` (custom CSS) and `flex-nowrap` on mobile, `flex-wrap` on larger screens: `flex-nowrap overflow-x-auto md:flex-wrap md:overflow-x-visible`
    3. Each TabsTrigger should have adequate touch target: `min-h-[40px]` minimum
    4. The page title "Profile" with `text-3xl` may be too large on mobile. Change to `text-2xl md:text-3xl`

    **Profile Overview Page (src/app/profile/page.tsx):**
    1. Read to understand layout
    2. Ensure user info section (avatar, name, bio) stacks on mobile
    3. Stats grid should be responsive

    **Profile Reviews, Favorites, Submissions Pages:**
    1. Read each file
    2. Reviews: ensure review cards stack properly on mobile, sort/filter controls wrap
    3. Favorites: ensure favorite cards use responsive grid
    4. Submissions: ensure submission status cards/list renders on mobile. If there's a table, wrap in `overflow-x-auto`

    **Settings Page + Components:**
    1. Read src/app/profile/settings/page.tsx and src/components/settings/settings-tabs.tsx
    2. Settings tabs (if there are sub-tabs like Account, Security, Notifications): ensure they work on mobile
    3. Security section: password change form should have full-width inputs on mobile
    4. Notifications section: toggle switches should have adequate touch targets and labels should not overflow
    5. All form inputs: `h-11 text-base` for mobile-friendly sizing
    6. Form buttons: `w-full sm:w-auto` pattern for mobile full-width, desktop auto-width

    **Cafe Submission Form (src/app/submit/page.tsx):**
    1. Read the file to understand the form layout
    2. Ensure form fields stack vertically on mobile
    3. Any two-column layouts should collapse to single column: `grid grid-cols-1 md:grid-cols-2 gap-4`
    4. Photo upload area: ensure it's large enough to tap on mobile (min 44px)
    5. Submit/Cancel buttons: stack on mobile with `flex flex-col-reverse sm:flex-row gap-2`
  </action>
  <verify>Run `npm run build` to confirm no errors. Check profile layout at 320px: tabs are navigable (scrollable or wrapped). Check settings at 320px: forms usable, toggle switches tappable. Check submission form at 320px: all fields accessible, no overflow.</verify>
  <done>Profile tabs are navigable on mobile. Settings forms have full-width inputs on mobile. Toggle switches are tappable. Submission form fields stack vertically on mobile. No overflow on any page at 320px.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. Login page at 320px: centered form, full-width inputs, OAuth buttons visible
3. Dashboard at 320px: stats cards stack, no overflow
4. Profile at 320px: tabs navigable (scrollable or wrapped), content readable
5. Settings at 320px: forms usable, toggles tappable
6. Submission form at 320px: fields stack, upload area tappable
</verification>

<success_criteria>
- Auth forms render properly centered on mobile with full-width inputs
- Dashboard stats use responsive grid
- Profile tabs are navigable on 320px screens
- Settings forms and notification toggles are usable on mobile
- Submission form stacks fields on mobile
- No horizontal overflow on any authenticated page at 320px
</success_criteria>

<output>
After completion, create `.planning/phases/19-style-and-mobile-responsive-check/19-03-SUMMARY.md`
</output>
