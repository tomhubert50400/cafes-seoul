---
phase: 05-auth-ui-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/auth/form-loading-overlay.tsx
  - src/components/auth/login-form.tsx
  - src/components/auth/signup-form.tsx
  - src/lib/i18n/translations.ts
autonomous: true

must_haves:
  truths:
    - "Loading state appears as semi-transparent overlay over entire form"
    - "Loading overlay appears after 200ms delay to prevent flash on fast responses"
    - "Submit button text changes to action-specific loading text during submission"
    - "Cancel button appears during loading to allow aborting slow requests"
    - "Form is visually disabled during loading (not interactive)"
  artifacts:
    - path: "src/components/auth/form-loading-overlay.tsx"
      provides: "Reusable form loading overlay component"
      exports: ["FormLoadingOverlay"]
    - path: "src/components/auth/login-form.tsx"
      provides: "Integrated loading overlay with cancel functionality"
      contains: ["FormLoadingOverlay", "isLoading", "onCancel"]
    - path: "src/components/auth/signup-form.tsx"
      provides: "Integrated loading overlay with cancel functionality"
      contains: ["FormLoadingOverlay", "isLoading", "onCancel"]
  key_links:
    - from: "login-form.tsx"
      to: "form-loading-overlay.tsx"
      via: "import and render"
      pattern: "<FormLoadingOverlay"
    - from: "signup-form.tsx"
      to: "form-loading-overlay.tsx"
      via: "import and render"
      pattern: "<FormLoadingOverlay"
---

<objective>
Create form-level loading states with semi-transparent overlay, 200ms delay trigger, action-specific button text, and cancel functionality for slow requests.

Purpose: Provide clear feedback during auth operations while allowing users to abort slow requests.
Output: Form loading overlay component integrated into both login and signup forms.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-auth-ui-ux-polish/05-CONTEXT.md
@src/components/auth/login-form.tsx
@src/components/auth/signup-form.tsx
@src/components/ui/button.tsx
@src/components/ui/sonner.tsx

## Current State
Forms currently have basic button-level loading state using useFormStatus's `pending` boolean. The SubmitButton component shows spinner and disabled state, but:
- No form-level overlay
- No delay before showing loading (immediate)
- No cancel button
- Button text changes but only shows generic "Logging in..." / "Creating account..."

## Requirements from Context
- **Visual:** Form-level semi-transparent overlay with spinner (not just button-level)
- **Trigger timing:** Loading state starts after 200ms delay (prevents flash on fast responses)
- **Button text:** Changes to action-specific text during loading (e.g., "Signing in...", "Creating account...")
- **Interactivity:** Cancel button appears during loading operations (allows users to abort long requests)
</context>

<tasks>

<task type="auto">
  <name>Create Form Loading Overlay Component</name>
  <files>src/components/auth/form-loading-overlay.tsx</files>
  <action>
    Create a new reusable FormLoadingOverlay component:
    
    ```tsx
    interface FormLoadingOverlayProps {
      isLoading: boolean
      message: string
      onCancel?: () => void
      cancelLabel?: string
    }
    ```
    
    Features:
    1. Semi-transparent backdrop overlay (bg-background/80 backdrop-blur-sm)
    2. Centered loading spinner with message text below
    3. Cancel button (secondary/outline style) that appears when onCancel prop provided
    4. Smooth fade-in/fade-out transitions using CSS or framer-motion
    5. Accessible: aria-busy="true" on form when loading
    
    Styling:
    - Overlay: absolute inset-0, z-50
    - Spinner: animate-spin, size-8 or size-10
    - Message: text-muted-foreground, text-sm
    - Cancel button: mt-4, variant="outline", size="sm"
    
    The component should be positioned relative to the form container.
  </action>
  <verify>
    Check the component:
    - Exports FormLoadingOverlay interface and component
    - Uses Tailwind classes for styling
    - Has proper TypeScript types
    - Accepts onCancel callback prop
    - File compiles without errors
  </verify>
  <done>
    Reusable FormLoadingOverlay component created with semi-transparent backdrop, spinner, message, and optional cancel button.
  </done>
</task>

<task type="auto">
  <name>Integrate Loading Overlay into Login Form</name>
  <files>src/components/auth/login-form.tsx</files>
  <action>
    Update login form to use the new loading overlay with 200ms delay:
    
    1. Add state management:
       - `isLoading` boolean state
       - `showOverlay` boolean state (for delay)
       - `loadingTimeout` ref to store setTimeout id
       - `abortController` ref for cancel functionality
    
    2. Create startLoading function:
       - Set isLoading to true immediately
       - Set 200ms timeout to set showOverlay to true
       - Store timeout id in ref for cleanup
    
    3. Create stopLoading function:
       - Clear the timeout if it exists
       - Set both isLoading and showOverlay to false
    
    4. Create handleCancel function:
       - Abort the abortController if exists
       - Call stopLoading
    
    5. Wrap form submission:
       - Create new AbortController on submit start
       - Call startLoading()
       - Pass signal to form action or handle cancellation
       - Call stopLoading() when done (success or error)
    
    6. Update SubmitButton:
       - Accept isLoading prop instead of using useFormStatus
       - Button text: "Signing in..." (use t('auth.login.loading') - add new key)
       - Disabled when isLoading
    
    7. Add FormLoadingOverlay inside form, conditionally rendered when showOverlay is true
    
    8. Update form onSubmit handler to manage the loading lifecycle
  </action>
  <verify>
    Check the form:
    - has isLoading and showOverlay state
    - has 200ms delay implementation
    - has abort controller for cancellation
    - FormLoadingOverlay imported and used
    - Cancel button appears during loading
    - Button text changes to "Signing in..."
  </verify>
  <done>
    Login form has form-level loading overlay with 200ms delay, cancel button, and action-specific loading text.
  </done>
</task>

<task type="auto">
  <name>Integrate Loading Overlay into Signup Form</name>
  <files>src/components/auth/signup-form.tsx</files>
  <action>
    Update signup form with the same loading overlay integration as login form:
    
    1. Add same state management (isLoading, showOverlay, loadingTimeout ref, abortController ref)
    2. Create startLoading and stopLoading functions with 200ms delay
    3. Create handleCancel function
    4. Wrap form submission with loading lifecycle
    5. Update SubmitButton to show "Creating account..." during loading (add t('auth.signup.loading'))
    6. Add FormLoadingOverlay conditionally rendered
    
    Pattern should match login-form.tsx exactly for consistency.
  </action>
  <verify>
    Check the form has:
    - 200ms delay implementation
    - Cancel button during loading
    - "Creating account..." button text
    - FormLoadingOverlay properly integrated
  </verify>
  <done>
    Signup form has consistent loading overlay with 200ms delay and cancel functionality.
  </done>
</task>

<task type="auto">
  <name>Add Loading State Translation Keys</name>
  <files>src/lib/i18n/translations.ts</files>
  <action>
    Add new translation keys for loading states in all 5 languages:
    
    New keys:
    - `auth.login.loading`: "Signing in..."
    - `auth.signup.loading`: "Creating account..."
    - `auth.loading.cancel`: "Cancel"
    
    For each language:
    - EN: "Signing in...", "Creating account...", "Cancel"
    - KO: "로그인 중...", "계정 생성 중...", "취소"
    - FR: "Connexion en cours...", "Création du compte...", "Annuler"
    - ZH: "登录中...", "创建账户中...", "取消"
    - VI: "Đang đăng nhập...", "Đang tạo tài khoản...", "Hủy"
  </action>
  <verify>
    grep -n "auth.login.loading\|auth.signup.loading\|auth.loading.cancel" src/lib/i18n/translations.ts
    Should find all three keys in all 5 language sections.
  </verify>
  <done>
    All loading state text is translatable with appropriate localized strings in 5 languages.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run TypeScript check to verify no errors
2. Verify FormLoadingOverlay component compiles
3. Check both forms have consistent loading behavior
</verification>

<success_criteria>
- Form-level semi-transparent overlay appears during loading
- Overlay has 200ms delay before showing (prevents flash)
- Cancel button appears in overlay during loading
- Login button shows "Signing in..." during loading
- Signup button shows "Creating account..." during loading
- Cancel functionality works (stops the loading state)
- All text uses i18n translations
</success_criteria>

<output>
After completion, create `.planning/phases/05-auth-ui-ux-polish/05-02-SUMMARY.md`
</output>
