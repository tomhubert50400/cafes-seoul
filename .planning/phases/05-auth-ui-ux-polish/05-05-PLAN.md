---
phase: 05-auth-ui-ux-polish
plan: 05
type: execute
wave: 3
depends_on: ['05-01']
files_modified:
  - src/components/auth/password-strength-meter.tsx
  - src/components/auth/signup-form.tsx
  - src/lib/i18n/translations.ts
autonomous: true

must_haves:
  truths:
    - "Password strength meter only appears after 3+ characters typed"
    - "Strength meter shows visual indicator (colors + label) of password strength"
    - "Strength meter provides specific guidance on improving password"
    - "Meter updates smoothly as user types (with debounce)"
    - "Strength criteria explained inline (length, variety, etc.)"
  artifacts:
    - path: "src/components/auth/password-strength-meter.tsx"
      provides: "Enhanced password strength indicator"
      exports: ["PasswordStrengthMeter"]
      contains: ["minLength", "strength calculation", "criteria list"]
    - path: "src/components/auth/signup-form.tsx"
      provides: "Conditional display of strength meter"
      contains: ["password.length >= 3", "PasswordStrengthMeter"]
  key_links:
    - from: "signup-form.tsx"
      to: "password-strength-meter.tsx"
      via: "conditional render based on length"
      pattern: "password\\.length >= 3"
    - from: "password-strength-meter.tsx"
      to: "i18n translations"
      via: "strength labels and criteria"
      pattern: "t\\('auth\\.password\."
---

<objective>
Enhance password strength meter with delayed appearance (3+ chars), detailed criteria guidance, and smooth animations.

Purpose: Help users create secure passwords without overwhelming them with feedback too early.
Output: Improved password strength component with conditional display and detailed guidance.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-auth-ui-ux-polish/05-CONTEXT.md
@src/components/auth/password-strength-meter.tsx
@src/components/auth/signup-form.tsx
@src/lib/i18n/translations.ts

## Current State
- PasswordStrengthMeter component exists and is used in signup form
- Currently shows immediately when password field has focus
- Basic strength calculation (weak/medium/strong) based on length and complexity
- Visual bar indicator with color coding

## Requirements from Context
- **Password strength:** Indicator appears after 3+ characters typed (not immediately on focus)
- Must guide users to create secure passwords with clear criteria
- Should feel smooth with subtle animations
</context>

<tasks>

<task type="auto">
  <name>Enhance Password Strength Meter Component</name>
  <files>src/components/auth/password-strength-meter.tsx</files>
  <action>
    Update the PasswordStrengthMeter component with enhanced features:
    
    1. Props interface:
       ```typescript
       interface PasswordStrengthMeterProps {
         password: string
         t: (key: string) => string
       }
       ```
    
    2. Strength calculation algorithm:
       - Score 0-100 based on:
         - Length (8+ chars = 30 points, 12+ = 40 points)
         - Uppercase letters (10 points)
         - Lowercase letters (10 points)
         - Numbers (15 points)
         - Special characters (15 points)
         - Mixed variety bonus (10 points for 3+ character types)
       - Thresholds:
         - 0-40: Weak (red)
         - 41-70: Fair (yellow/orange)
         - 71-90: Good (blue)
         - 91-100: Strong (green)
    
    3. Visual elements:
       - Segmented progress bar (4 segments) instead of continuous
       - Each segment fills based on score threshold
       - Color transitions: red -> orange -> blue -> green
       - Strength label below bar (e.g., "Weak", "Fair", "Good", "Strong")
       - Smooth transitions using CSS or framer-motion
    
    4. Criteria checklist:
       - Show 4-5 criteria with check/x icons:
         - At least 8 characters
         - Contains uppercase letter
         - Contains lowercase letter
         - Contains number
         - Contains special character
       - Met criteria show checkmark (green)
       - Unmet criteria show circle/x (gray)
       - List appears below strength bar
    
    5. Accessibility:
       - aria-label describing current strength
       - role="progressbar" with aria-valuenow
    
    Use the translation function for all labels and criteria text.
  </action>
  <verify>
    Check password-strength-meter.tsx:
    - Has scoring algorithm (0-100)
    - Has 4 strength levels with colors
    - Shows segmented progress bar
    - Displays criteria checklist with icons
    - Uses t() for all text
    - Has proper ARIA attributes
  </verify>
  <done>
    Enhanced password strength meter with detailed scoring, segmented visual, and criteria checklist.
  </done>
</task>

<task type="auto">
  <name>Implement Conditional Display in Signup Form</name>
  <files>src/components/auth/signup-form.tsx</files>
  <action>
    Update signup form to only show password strength meter after 3+ characters:
    
    1. Update the PasswordStrengthMeter rendering:
       ```tsx
       {password.length >= 3 && (
         <PasswordStrengthMeter password={password} t={t} />
       )}
       ```
    
    2. Add smooth entrance animation:
       - Wrap PasswordStrengthMeter in a div with animation classes
       - Use framer-motion or CSS transitions for fade-in + slide-down
       - Animation: opacity 0->1, translateY -10px->0
       - Duration: 200ms, ease-out
    
    3. Update PasswordStrengthMeter props:
       - Pass t function as prop
       - Ensure component receives translation function
    
    4. Consider debounce:
       - Password strength calculation already debounced at 300ms
       - Keep existing debounce behavior
    
    5. Position:
       - Keep below password field and error message
       - Add mt-2 for spacing
    
    The meter should not appear when password is empty or has 1-2 characters.
  </action>
  <verify>
    Check signup form:
    - Only renders PasswordStrengthMeter when password.length >= 3
    - Has smooth entrance animation
    - Passes t function as prop
  </verify>
  <done>
    Password strength meter conditionally appears after 3+ characters with smooth animation.
  </done>
</task>

<task type="auto">
  <name>Add Password Strength Translation Keys</name>
  <files>src/lib/i18n/translations.ts</files>
  <action>
    Add comprehensive translation keys for password strength in all 5 languages:
    
    New keys under auth.password namespace:
    - `auth.password.strength.weak`: "Weak"
    - `auth.password.strength.fair`: "Fair"
    - `auth.password.strength.good`: "Good"
    - `auth.password.strength.strong`: "Strong"
    - `auth.password.criteria.length`: "At least 8 characters"
    - `auth.password.criteria.uppercase`: "Contains uppercase letter"
    - `auth.password.criteria.lowercase`: "Contains lowercase letter"
    - `auth.password.criteria.number`: "Contains number"
    - `auth.password.criteria.special`: "Contains special character"
    - `auth.password.label`: "Password strength"
    - `auth.password.hint`: "Make it strong"
    
    Translations for all 5 languages:
    
    English (en):
    - Weak, Fair, Good, Strong
    - Criteria as listed above
    
    Korean (ko):
    - 약함, 보통, 좋음, 강함
    - 8자 이상, 대문자 포함, 소문자 포함, 숫자 포함, 특수문자 포함
    - 비밀번호 강도, 강하게 만드세요
    
    French (fr):
    - Faible, Moyen, Bon, Fort
    - Au moins 8 caractères, Contient une majuscule, etc.
    
    Chinese (zh):
    - 弱, 一般, 良好, 强
    - 至少8个字符, 包含大写字母, 等
    
    Vietnamese (vi):
    - Yếu, Trung bình, Tốt, Mạnh
    - Ít nhất 8 ký tự, Có chữ hoa, 等
  </action>
  <verify>
    grep for auth.password keys in translations.ts - should find all keys in all 5 language sections.
  </verify>
  <done>
    All password strength labels and criteria are translatable in 5 languages.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Verify TypeScript compiles
2. Test that meter only appears after 3 characters
3. Check all strength levels and criteria display correctly
4. Verify smooth animations work
</verification>

<success_criteria>
- Password strength meter only appears when password has 3+ characters
- Strength meter shows segmented progress bar with 4 levels (Weak/Fair/Good/Strong)
- Colors transition appropriately (red/orange/blue/green)
- Criteria checklist shows below meter with check/x icons
- Meter entrance has smooth animation (fade + slide)
- All text uses i18n translations
- Accessibility attributes present
</success_criteria>

<output>
After completion, create `.planning/phases/05-auth-ui-ux-polish/05-05-SUMMARY.md`
</output>
