---
phase: 05-auth-ui-ux-polish
plan: 06
type: execute
wave: 4
depends_on: ['05-01', '05-02', '05-03', '05-04', '05-05']
files_modified:
  - src/components/auth/login-form.tsx
  - src/components/auth/signup-form.tsx
  - src/app/(auth)/layout.tsx
autonomous: false

must_haves:
  truths:
    - "All auth forms have smooth, subtle animations throughout"
    - "Focus management works correctly (tab order, auto-focus on errors)"
    - "Auth flows provide clear feedback at every step"
    - "All auth functionality works end-to-end in verification"
    - "No console errors or warnings during auth operations"
  artifacts:
    - path: "src/app/(auth)/layout.tsx"
      provides: "Auth layout with animations"
      contains: ["motion wrapper", "page transitions"]
    - path: "src/components/auth/login-form.tsx"
      provides: "Polished login with all UX enhancements"
      contains: ["animations", "focus management", "toasts"]
    - path: "src/components/auth/signup-form.tsx"
      provides: "Polished signup with all UX enhancements"
      contains: ["animations", "focus management", "toasts"]
  key_links:
    - from: "layout.tsx"
      to: "motion components"
      via: "page transition wrapper"
      pattern: "<motion.div"
---

<objective>
Final polish pass: add smooth animations throughout auth flows, verify keyboard navigation, ensure auth layout has proper motion wrappers, and perform comprehensive testing checkpoint.

Purpose: Ensure all auth UI/UX enhancements work together seamlessly for a production-ready experience.
Output: Fully polished auth experience with all feedback mechanisms integrated and verified.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-auth-ui-ux-polish/05-CONTEXT.md
@src/components/auth/login-form.tsx
@src/components/auth/signup-form.tsx
@src/app/(auth)/layout.tsx

## Previous Plans Summary
- Plan 01: Enhanced validation with blur-triggered errors and focus management
- Plan 02: Form loading overlays with 200ms delay and cancel buttons
- Plan 03: Toast infrastructure (configuration, utilities, translations, root integration)
- Plan 04: Toast integration into auth forms and verify-email page
- Plan 05: Password strength meter with conditional display

## Current State
- Individual enhancements implemented but need integration verification
- Animations need to be consistent across components
- Auth layout may need motion wrapper for page transitions
- Keyboard navigation needs final verification
- Screen reader support needs verification

## Requirements from Context
- **Motion:** Always animate but keep subtle (do not honor reduced motion system preferences)
- **Focus management:** On validation error, automatically focus the first invalid field
- **Keyboard:** Tab navigation optimization (logical order, skip decorative elements)
- **Screen readers:** Use live regions sparingly (announce critical changes only)
</context>

<tasks>

<task type="auto">
  <name>Add Smooth Animations to Auth Forms</name>
  <files>
    src/components/auth/login-form.tsx
    src/components/auth/signup-form.tsx
  </files>
  <action>
    Add subtle, smooth animations throughout auth forms:
    
    **Error message animations:**
    - Add fade-in + slide-down animation when errors appear
    - CSS: `animate-in fade-in slide-in-from-top-2 duration-200`
    - Use framer-motion or CSS transitions
    
    **Button state transitions:**
    - Smooth color/opacity changes on hover, focus, disabled states
    - Transition duration: 150ms
    
    **Loading overlay:**
    - Ensure overlay has fade-in animation (already from Plan 02)
    - Smooth backdrop blur transition
    
    **Input focus states:**
    - Smooth ring/border color transition on focus
    - Duration: 150ms
    
    **Password strength meter:**
    - Already has entrance animation from Plan 05
    - Ensure smooth bar fills
    
    **Success/error toasts:**
    - Sonner handles animations, verify they're smooth
    
    Apply consistent animation patterns using:
    - Tailwind's animate-* utilities
    - Or framer-motion for complex sequences
    - Duration: 150-300ms (snappy but noticeable)
    - Easing: ease-out for entrances, ease-in for exits
    
    Keep animations subtle - not jarring or distracting.
  </action>
  <verify>
    Check forms have:
    - Error message entrance animations
    - Smooth input focus transitions
    - Consistent animation timing
  </verify>
  <done>
    All auth forms have subtle, smooth animations for state changes.
  </done>
</task>

<task type="auto">
  <name>Optimize Keyboard Navigation</name>
  <files>
    src/components/auth/login-form.tsx
    src/components/auth/signup-form.tsx
  </files>
  <action>
    Ensure optimal keyboard navigation in auth forms:
    
    1. Tab order verification:
       - Email field -> Password field -> Remember me (login only) -> Submit button -> OAuth buttons
       - Tab index should follow visual order naturally
       - No positive tabIndex values needed (use natural order)
    
    2. Focus visibility:
       - All interactive elements have visible focus states
       - Focus ring is prominent enough
       - No focus trap issues
    
    3. Skip logic:
       - Password visibility toggle should be in tab order
       - Cancel button (when loading) should be focusable
       - OAuth buttons should be focusable
    
    4. Auto-focus behavior:
       - On page load: focus email field (optional, maybe not - accessibility concern)
       - On error: first invalid field gets focus (already from Plan 01)
       - After submit: loading state manages focus
    
    5. Escape key:
       - Escape should cancel loading if overlay is showing
       - Add keydown handler for Escape key during loading
    
    6. ARIA attributes:
       - Verify aria-invalid on inputs with errors
       - Verify aria-describedby links inputs to error messages
       - Verify aria-label on icon-only buttons (password toggle, cancel)
    
    Make sure the form is fully navigable without a mouse.
  </action>
  <verify>
    Manual verification needed for keyboard nav, but code should have:
    - No positive tabIndex
    - Visible focus states
    - ARIA attributes
    - Escape key handler for loading
  </verify>
  <done>
    Auth forms optimized for keyboard navigation with visible focus states and logical tab order.
  </done>
</task>

<task type="auto">
  <name>Enhance Auth Layout with Motion</name>
  <files>src/app/(auth)/layout.tsx</files>
  <action>
    Add smooth page transitions to auth layout:
    
    1. Import framer-motion:
       - `import { motion } from 'framer-motion'`
    
    2. Wrap main content in motion container:
       ```tsx
       <motion.div
         initial={{ opacity: 0, y: 10 }}
         animate={{ opacity: 1, y: 0 }}
         exit={{ opacity: 0, y: -10 }}
         transition={{ duration: 0.2, ease: "easeOut" }}
       >
         {children}
       </motion.div>
       ```
    
    3. Consider using AnimatePresence for page transitions:
       - Wrap in AnimatePresence if not already done
       - Ensure mode="wait" for smooth exit/enter
    
    4. Keep animations subtle:
       - Short duration (200ms)
       - Small movement (10px)
       - Smooth easing
    
    5. Verify layout structure:
       - Auth header/logo at top
       - Motion wrapper around content
       - Proper spacing and responsive layout
  </action>
  <verify>
    Check auth layout.tsx:
    - Has framer-motion imports
    - Wraps content in motion container
    - Has smooth page transition animations
  </verify>
  <done>
    Auth layout has smooth page transition animations.
  </done>
</task>

<task type="auto">
  <name>Verify Screen Reader Support</name>
  <files>
    src/components/auth/login-form.tsx
    src/components/auth/signup-form.tsx
    src/components/auth/form-loading-overlay.tsx
  </files>
  <action>
    Add screen reader optimizations:
    
    1. Form announcements:
       - Add aria-live="polite" region for success messages (subtle)
       - Error messages already show inline with aria-invalid
    
    2. Loading state:
       - Add aria-busy="true" to form when loading
       - Loading overlay should have aria-live="polite" with status message
       - "Signing in..." should be announced
    
    3. Password strength:
       - Add aria-label to strength meter describing current level
       - Announce strength changes (optional - might be too verbose)
    
    4. Toast announcements:
       - Sonner toasts should have proper aria-live regions
       - Critical errors (login failure) should use assertive
    
    5. Focus announcements:
       - When auto-focusing first invalid field, screen reader should announce
       - Error message should be linked with aria-describedby
    
    Keep live regions minimal - only announce critical changes.
  </action>
  <verify>
    Check for:
    - aria-busy on form during loading
    - aria-live regions where appropriate
    - aria-describedby linking inputs to errors
  </verify>
  <done>
    Screen reader support added for loading states and critical announcements.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Comprehensive Auth UX Verification</name>
  <files>All auth-related files</files>
  <what-built>
    Complete auth UI/UX polish phase including:
    - Enhanced client-side validation with blur timing and error clearing
    - Form loading overlays with 200ms delay and cancel buttons
    - Toast notifications for all auth feedback (errors, success)
    - Email verification page with email display
    - Password strength meter with criteria checklist
    - Smooth animations throughout
    - Keyboard navigation optimization
    - Screen reader support
  </what-built>
  <how-to-verify>
    Test all auth flows thoroughly:
    
    1. **Login Flow:**
       - Visit /login
       - Leave email field empty, tab out → error should appear
       - Start typing in email → error should clear immediately
       - Enter invalid email format → error on blur
       - Enter wrong password → toast error should appear (not inline)
       - Click submit → loading overlay appears after 200ms
       - Cancel button should appear in overlay (test it works)
       - Login success → "Welcome back!" toast, then redirect
    
    2. **Signup Flow:**
       - Visit /signup
       - Type 2 chars in password → no strength meter
       - Type 3rd char → strength meter appears with animation
       - Check all strength criteria update correctly
       - Submit with errors → errors clear on type
       - Successful signup → redirect to /verify-email?email=xxx
       - Verify page shows email address clearly
       - Resend button works
    
    3. **Logout Flow:**
       - Logout → "Logged out successfully" toast → redirect to login
    
    4. **Keyboard Navigation:**
       - Tab through all fields without mouse
       - Press Enter to submit
       - Press Escape during loading to cancel
       - Verify focus states are visible
    
    5. **Visual Polish:**
       - Check all animations are smooth
       - Verify toast progress bar shows countdown
       - Check password strength colors are clear
       - Verify loading overlay looks good
    
    6. **i18n:**
       - Switch languages, verify all new text translates
    
    Report any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" if all flows work smoothly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After completing automation tasks:
1. Run full TypeScript check
2. Run build to verify no compilation errors
3. Check console for any warnings/errors
4. Complete checkpoint verification
</verification>

<success_criteria>
- Auth layout has smooth page transition animations
- All forms have smooth, subtle animations
- Keyboard navigation works for entire auth flow
- Screen readers properly announce loading and errors
- All auth flows verified working end-to-end
- No console errors or warnings
- User approves the UX after testing
</success_criteria>

<output>
After completion, create `.planning/phases/05-auth-ui-ux-polish/05-06-SUMMARY.md`
</output>
