---
phase: 05-auth-ui-ux-polish
plan: 03
type: execute
wave: 2
depends_on: ['05-01', '05-02']
files_modified:
  - src/components/ui/sonner.tsx
  - src/components/auth/auth-toast.tsx
  - src/components/auth/login-form.tsx
  - src/components/auth/signup-form.tsx
  - src/components/auth/logout-button.tsx
  - src/app/(auth)/verify-email/page.tsx
  - src/lib/i18n/translations.ts
autonomous: true

must_haves:
  truths:
    - "Server errors display as toast notifications with slide-in animation"
    - "Toasts auto-dismiss with visible progress bar showing countdown"
    - "Multiple errors combine into single toast with expandable details"
    - "Login success shows celebration toast then redirects"
    - "Signup success shows inline verification message with email address"
    - "Logout shows success toast confirmation"
  artifacts:
    - path: "src/components/auth/auth-toast.tsx"
      provides: "Auth-specific toast utilities with progress bar"
      exports: ["showAuthError", "showAuthSuccess", "AuthToastProvider"]
    - path: "src/components/ui/sonner.tsx"
      provides: "Updated toast configuration with progress bar"
      contains: ["duration", "progress", "closeButton"]
    - path: "src/app/(auth)/verify-email/page.tsx"
      provides: "Email verification success page with email display"
      contains: ["email param", "check your email", "resend option"]
  key_links:
    - from: "login-form.tsx"
      to: "auth-toast.tsx"
      via: "import showAuthError/showAuthSuccess"
      pattern: "showAuthError\\(state\\.message\\)"
    - from: "signup-form.tsx"
      to: "verify-email page"
      via: "redirect with email param"
      pattern: "redirect\\('/verify-email'\\)"
---

<objective>
Implement comprehensive toast notification system for auth feedback: server errors as toasts with progress bars, success celebrations, and inline verification messages.

Purpose: Provide clear, non-intrusive feedback for all auth operations with visual polish.
Output: Auth toast utilities, updated toast configuration, and integrated toast calls in all auth flows.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-auth-ui-ux-polish/05-CONTEXT.md
@src/components/ui/sonner.tsx
@src/components/auth/login-form.tsx
@src/components/auth/signup-form.tsx
@src/components/auth/logout-button.tsx
@src/lib/i18n/translations.ts

## Current State
- Sonner toaster exists in src/components/ui/sonner.tsx with basic styling
- Forms currently show server errors as inline red divs (not toasts)
- No success toast feedback for login/logout
- Signup shows generic "Check your email" inline but doesn't show the email address
- Sonner has progress bar support but not configured

## Requirements from Context
- **Server errors:** Display as toast notifications (slide-in at screen edge)
- **Dismissal:** Auto-dismiss with progress bar showing countdown (users can also manually close)
- **Multiple errors:** Combine into single toast (e.g., "3 errors found" with expandable details)
- **After login:** Show success toast with brief celebration, then redirect to destination
- **After signup:** Different flows by method —
  - Email signup: Show "verification email sent" message inline on page (display email address)
  - OAuth signup: Redirect to home with success toast
- **Success style:** Brief celebration (subtle animation) for major actions like signup
- **After logout:** Toast confirmation "Logged out successfully"
</context>

<tasks>

<task type="auto">
  <name>Enhance Sonner Toast Configuration</name>
  <files>src/components/ui/sonner.tsx</files>
  <action>
    Update the Sonner toaster configuration for auth use cases:
    
    1. Add progress bar support:
       - Add `progress` class styling to the style prop
       - Ensure progress bar is visible (--progress-bar-height, --progress-bar-bg)
    
    2. Configure default durations:
       - Error toasts: 6000ms (longer to read)
       - Success toasts: 4000ms
       - Loading toasts: infinite (until dismissed)
    
    3. Add close button:
       - Set `closeButton: true` to allow manual dismissal
    
    4. Position:
       - Set `position: 'top-right'` for auth toasts
    
    5. Styling enhancements:
       - Ensure toast width is appropriate (not too narrow)
       - Add shadow for depth
       - Match theme colors
    
    The Toaster component should accept props that override defaults for flexibility.
  </action>
  <verify>
    Check sonner.tsx has:
    - progress styling in CSS custom properties
    - closeButton enabled
    - position set to top-right
    - Appropriate duration defaults
  </verify>
  <done>
    Sonner toaster configured with progress bars, close buttons, and auth-appropriate defaults.
  </done>
</task>

<task type="auto">
  <name>Create Auth Toast Utilities</name>
  <files>src/components/auth/auth-toast.tsx</files>
  <action>
    Create auth-specific toast utility functions:
    
    ```typescript
    // Functions to export:
    - showAuthError(message: string, t: TranslationFn): void
    - showAuthSuccess(message: string, t: TranslationFn): void
    - showAuthLoading(message: string, t: TranslationFn): string (returns toast id)
    - dismissAuthToast(toastId: string): void
    - showAuthErrors(errors: Record<string, string[]>, t: TranslationFn): void
    ```
    
    Features:
    1. showAuthError:
       - Uses sonner's `error` type (red styling)
       - Shows error icon
       - 6 second duration with progress bar
       - Close button visible
    
    2. showAuthSuccess:
       - Uses sonner's `success` type (green styling)
       - Shows success icon
       - Optional subtle animation (scale bounce on appear)
       - 4 second duration
    
    3. showAuthErrors (for multiple validation errors):
       - Combines multiple field errors into one toast
       - Title: "X errors found" or localized equivalent
       - Description: Expandable list of errors
       - Error styling
    
    4. showAuthLoading:
       - Shows loading spinner icon
       - Returns toast id for later dismissal/update
       - No auto-dismiss (infinite duration)
    
    All functions should accept the translation function for i18n support.
  </action>
  <verify>
    Check auth-toast.tsx:
    - Exports all required functions
    - Uses sonner's toast function correctly
    - Has proper TypeScript types
    - Accepts translation function parameter
  </verify>
  <done>
    Auth toast utility module with error, success, loading, and multi-error functions.
  </done>
</task>

<task type="auto">
  <name>Integrate Toast Feedback into Login Form</name>
  <files>src/components/auth/login-form.tsx</files>
  <action>
    Update login form to use toast notifications instead of inline errors:
    
    1. Import toast utilities:
       - `import { showAuthError, showAuthSuccess } from './auth-toast'`
    
    2. Replace inline server error display:
       - Remove the red error div that shows state?.message
       - Instead call `showAuthError(state.message, t)` when state?.message exists
    
    3. Add success toast on login:
       - When login succeeds (state?.success or check for successful login), show success toast
       - Message: "Welcome back!" or localized equivalent
       - Use showAuthSuccess with subtle celebration
       - Toast should show before redirect
    
    4. Update useEffect for state changes:
       - Watch for state changes in useEffect
       - Show error toast when errors arrive
       - Show success toast on success
    
    5. Remove inline error display div (or keep only for OAuth errors)
    
    Note: OAuth errors from URL should still show inline at top of form (not as toast since user just landed).
  </action>
  <verify>
    Check login form:
    - Imports showAuthError and showAuthSuccess
    - Calls showAuthError for server errors
    - Shows success toast on login
    - No longer shows inline red error div for server errors
  </verify>
  <done>
    Login form shows server errors and success messages as toasts instead of inline.
  </done>
</task>

<task type="auto">
  <name>Integrate Toast Feedback into Signup Form</name>
  <files>src/components/auth/signup-form.tsx</files>
  <action>
    Update signup form to use toast notifications for errors:
    
    1. Import showAuthError and showAuthSuccess from auth-toast
    
    2. Replace inline server error display with toast:
       - Remove red error div for state?.message
       - Call showAuthError when server errors arrive
    
    3. Success handling:
       - Email signup: Redirect to verify-email page with email param (not toast)
       - Keep existing redirect logic
    
    4. Update useEffect for state changes
    
    The verify-email page will handle showing the inline success message.
  </action>
  <verify>
    Check signup form:
    - Uses showAuthError for server errors
    - No inline error div for server messages
    - Still redirects to verify-email page on success
  </verify>
  <done>
    Signup form shows errors as toasts and redirects to verification page on success.
  </done>
</task>

<task type="auto">
  <name>Create Email Verification Success Page</name>
  <files>src/app/(auth)/verify-email/page.tsx</files>
  <action>
    Create a new verify-email page that displays after successful signup:
    
    1. Page structure:
       - Auth layout wrapper (minimal header)
       - Centered card with success styling
    
    2. Display:
       - Checkmark icon (large, success color)
       - Title: "Check your email" (localized)
       - Message: "We sent a verification link to:"
       - Email address (bold, prominent) from search params
       - Instructions: "Click the link in your email to activate your account"
       - Resend button (triggers resendVerification action)
       - Back to signup link
    
    3. Functionality:
       - Get email from searchParams: `?email=user@example.com`
       - Handle missing email gracefully
       - Resend button with loading state
       - Show toast on resend success/error
    
    4. Styling:
       - Success/celebration styling (green accents)
       - Clear visual hierarchy
       - Mobile responsive
    
    This replaces the inline "Check your email" message in signup form with a dedicated page.
  </action>
  <verify>
    Check verify-email page:
    - Renders at /verify-email route
    - Displays email from URL param
    - Has resend functionality
    - Shows celebration/success styling
    - Uses i18n translations
  </verify>
  <done>
    Email verification page displays user's email address with success styling and resend option.
  </done>
</task>

<task type="auto">
  <name>Add Logout Toast Feedback</name>
  <files>src/components/auth/logout-button.tsx</files>
  <action>
    Update logout button to show success toast:
    
    1. Import showAuthSuccess from auth-toast
    
    2. In the logout action handler:
       - Show "Logged out successfully" toast before/during redirect
       - Use showAuthSuccess with brief duration
    
    3. The toast should appear before the page redirects to login
    
    4. Add i18n key: `auth.logout.success`
  </action>
  <verify>
    Check logout-button.tsx:
    - Imports and uses showAuthSuccess
    - Shows toast on successful logout
  </verify>
  <done>
    Logout button shows "Logged out successfully" toast before redirect.
  </done>
</task>

<task type="auto">
  <name>Add Toast Translation Keys</name>
  <files>src/lib/i18n/translations.ts</files>
  <action>
    Add all necessary translation keys for toast messages in 5 languages:
    
    New keys needed:
    - `auth.toast.error.title`: "Error"
    - `auth.toast.error.multiple`: "{count} errors found"
    - `auth.toast.success.login`: "Welcome back!"
    - `auth.toast.success.signup`: "Account created!"
    - `auth.toast.success.logout`: "Logged out successfully"
    - `auth.toast.success.resend`: "Verification email sent"
    - `auth.verify.title`: "Check your email"
    - `auth.verify.message`: "We sent a verification link to:"
    - `auth.verify.instructions`: "Click the link in your email to activate your account"
    - `auth.verify.resend`: "Resend email"
    - `auth.verify.back`: "Back to sign up"
    
    Translate to all 5 languages (en, ko, fr, zh, vi).
  </action>
  <verify>
    grep for new keys in translations.ts - should find all keys in all 5 languages.
  </verify>
  <done>
    All toast and verification page text is translatable in 5 languages.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Verify TypeScript compiles without errors
2. Check all auth forms use toast notifications
3. Verify verify-email page renders correctly
4. Test toast durations and progress bar visibility
</verification>

<success_criteria>
- Server errors appear as toasts (not inline) in login and signup forms
- Toasts have visible progress bars showing auto-dismiss countdown
- Toasts can be manually closed
- Login success shows "Welcome back!" toast with celebration styling
- Signup redirects to /verify-email with email displayed
- Logout shows "Logged out successfully" toast
- All text uses i18n translations
- Progress bar is visible on toasts
</success_criteria>

<output>
After completion, create `.planning/phases/05-auth-ui-ux-polish/05-03-SUMMARY.md`
</output>
