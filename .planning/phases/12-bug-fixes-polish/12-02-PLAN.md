---
phase: 12-bug-fixes-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/submissions/cafe-submission-form.tsx
  - src/lib/validations/submission.ts
autonomous: true

must_haves:
  truths:
    - "User adding a cafe does not see latitude/longitude input fields"
    - "Changing language tab for name also changes language tab for address"
    - "Form submission works without coordinates"
  artifacts:
    - path: "src/components/submissions/cafe-submission-form.tsx"
      provides: "Simplified submission form without coordinates"
  key_links:
    - from: "src/components/submissions/cafe-submission-form.tsx"
      to: "src/lib/validations/submission.ts"
      via: "zodResolver import"
      pattern: "submissionSchema"
---

<objective>
Simplify the cafe submission form by removing coordinates fields and unifying language selection.

Purpose: Users submit cafes with address only (coordinates can be geocoded later by admin), and language tab selection applies to both name and address fields for consistent UX.
Output: Updated cafe-submission-form.tsx with cleaner, simpler form.
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-bug-fixes-polish/12-RESEARCH.md
@src/components/submissions/cafe-submission-form.tsx
@src/lib/validations/submission.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove coordinates section from submission form</name>
  <files>src/components/submissions/cafe-submission-form.tsx</files>
  <action>
Remove the entire "Location Section (Optional)" block (lines 342-407):
- Delete the MapPin icon import from lucide-react
- Delete the latitude Controller block (lines 355-378)
- Delete the longitude Controller block (lines 380-403)
- Delete the location help text paragraph
- Delete the entire wrapping div with "Location Section" comment

Remove from defaultValues in useForm (lines 73-76):
- Remove `latitude: initialData?.latitude,`
- Remove `longitude: initialData?.longitude,`

Note: Keep the Zod schema unchanged - the fields remain optional in the schema for API compatibility, they just won't be shown in the form UI.
  </action>
  <verify>
1. Search cafe-submission-form.tsx for 'latitude' - should find 0 matches
2. Search cafe-submission-form.tsx for 'longitude' - should find 0 matches
3. Search cafe-submission-form.tsx for 'MapPin' - should find 0 matches
4. `npm run build` passes
  </verify>
  <done>Coordinates fields are not visible in the add cafe form.</done>
</task>

<task type="auto">
  <name>Task 2: Unify language tab selection for name and address</name>
  <files>src/components/submissions/cafe-submission-form.tsx</files>
  <action>
Currently, name and address each have independent Tabs components. The name section uses controlled state (`activeLanguageTab`), but the address section uses `defaultValue="en"`.

Change the address Tabs to use the same `activeLanguageTab` state:

1. Find the address Tabs component (around line 248 after removing coordinates):
   ```typescript
   <Tabs defaultValue="en">
   ```

2. Change it to use the shared state:
   ```typescript
   <Tabs value={activeLanguageTab} onValueChange={(v) => setActiveLanguageTab(v as 'en' | 'ko')}>
   ```

This synchronizes both tab sets - when user switches to Korean for name, address also switches to Korean, providing unified UX.
  </action>
  <verify>
1. Search cafe-submission-form.tsx for 'defaultValue="en"' in Tabs - should find 0 matches
2. Count occurrences of `activeLanguageTab` - should appear in both name and address Tabs
3. `npm run build` passes
  </verify>
  <done>Language tab selection is unified - selecting Korean for name also selects Korean for address.</done>
</task>

</tasks>

<verification>
1. Run `npm run build` - no TypeScript or build errors
2. Run `npm run lint` - no lint errors in modified files
3. Manual verification (for UAT):
   - Add cafe form shows name, address, phone fields only
   - No latitude/longitude fields visible
   - Clicking Korean tab for name also switches address to Korean tab
   - Form submits successfully without coordinates
</verification>

<success_criteria>
- [ ] Coordinates section completely removed from form UI
- [ ] Both name and address tabs controlled by same state
- [ ] Form renders and submits correctly
- [ ] Build passes with no errors
- [ ] Requirements FORM-01, FORM-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/12-bug-fixes-polish/12-02-SUMMARY.md`
</output>
