---
phase: 12-bug-fixes-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ratings/rating-form.tsx
  - src/components/auth/user-menu.tsx
autonomous: true

must_haves:
  truths:
    - "Rating cancel button shows translated text in all languages"
    - "Admin users see admin panel link in profile dropdown"
    - "Language picker is not visible in profile dropdown"
  artifacts:
    - path: "src/components/ratings/rating-form.tsx"
      provides: "Rating form with correct i18n key"
      contains: "t('rating.cancel')"
    - path: "src/components/auth/user-menu.tsx"
      provides: "User dropdown with admin link, no language picker"
      contains: "ROUTES.ADMIN"
  key_links:
    - from: "src/components/ratings/rating-form.tsx"
      to: "translations.ts"
      via: "useI18n hook"
      pattern: "rating\\.cancel"
---

<objective>
Fix i18n translation key bug and update profile dropdown navigation.

Purpose: Users see properly translated cancel button text instead of raw key, admin users can access admin panel from dropdown, and unnecessary language picker is removed from dropdown.
Output: Updated rating-form.tsx and user-menu.tsx with fixes applied.
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-bug-fixes-polish/12-RESEARCH.md
@src/components/ratings/rating-form.tsx
@src/components/auth/user-menu.tsx
@src/lib/i18n/translations.ts
@src/app/admin/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix rating cancel button i18n key</name>
  <files>src/components/ratings/rating-form.tsx</files>
  <action>
Change line 397 from `{t('common.cancel')}` to `{t('rating.cancel')}`.

The translation key `rating.cancel` already exists in translations.ts for all 5 languages:
- EN: 'Cancel'
- KO: '취소'
- FR: 'Annuler'
- ZH: '取消'
- VI: 'Hủy'

The bug occurred because `common.cancel` does not exist as a translation key, so the raw key was displayed.
  </action>
  <verify>
1. Search translations.ts for `rating.cancel` - should find entries for all languages
2. Search rating-form.tsx for `common.cancel` - should find 0 matches after fix
3. `npm run build` passes without errors
  </verify>
  <done>Rating cancel button displays translated text (not "common.cancel") in all 5 languages.</done>
</task>

<task type="auto">
  <name>Task 2: Add admin link and remove language picker from user dropdown</name>
  <files>src/components/auth/user-menu.tsx</files>
  <action>
Two changes to user-menu.tsx:

1. **Add admin link** (after Settings menu item, before language submenu):
   - Add state to track admin status
   - Fetch admin status on mount using Supabase client
   - Query profiles table for role === 'admin' (same pattern as admin/layout.tsx)
   - Conditionally render admin link with Shield icon when user is admin
   - Link to '/admin' route
   - Add translation key usage: `t('nav.admin')` (key already exists)

2. **Remove language picker submenu**:
   - Delete the entire DropdownMenuSub block (lines 115-134)
   - Remove unused imports: DropdownMenuSub, DropdownMenuSubTrigger, DropdownMenuSubContent, DropdownMenuPortal, Globe
   - Remove unused state/vars: currentLanguage (line 40)

Add these imports:
- `import { Shield } from 'lucide-react'`
- `import { createClient } from '@/lib/supabase/client'`

Add useEffect to fetch admin status:
```typescript
const [isAdmin, setIsAdmin] = useState(false);

useEffect(() => {
  const checkAdmin = async () => {
    const supabase = createClient();
    const { data: profile } = await supabase
      .from('profiles')
      .select('role')
      .eq('id', user.id)
      .single();
    setIsAdmin(profile?.role === 'admin');
  };
  checkAdmin();
}, [user.id]);
```

Add admin menu item (conditionally rendered):
```typescript
{isAdmin && (
  <DropdownMenuItem asChild>
    <Link
      href="/admin"
      className="flex cursor-pointer items-center gap-2"
    >
      <Shield className="h-4 w-4" />
      {t('nav.admin')}
    </Link>
  </DropdownMenuItem>
)}
```
  </action>
  <verify>
1. `npm run build` passes without errors
2. Search user-menu.tsx for 'Globe' - should find 0 matches
3. Search user-menu.tsx for 'DropdownMenuSub' - should find 0 matches
4. Search user-menu.tsx for 'Shield' - should find 1+ matches
5. Search user-menu.tsx for 'isAdmin' - should find matches
  </verify>
  <done>Admin users see admin panel link in dropdown. Language picker is removed from dropdown. Non-admin users do not see admin link.</done>
</task>

</tasks>

<verification>
1. Run `npm run build` - no TypeScript or build errors
2. Run `npm run lint` - no lint errors in modified files
3. Manual verification (for UAT):
   - Rating modal cancel button shows "Cancel"/"취소"/etc based on language
   - Admin user dropdown shows "Admin" link
   - Non-admin user dropdown does not show "Admin" link
   - No language picker in dropdown menu
</verification>

<success_criteria>
- [ ] Rating form uses `rating.cancel` translation key
- [ ] User menu conditionally shows admin link for admin users
- [ ] User menu does not contain language picker submenu
- [ ] Build passes with no errors
- [ ] Requirements I18N-01, NAV-01, NAV-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/12-bug-fixes-polish/12-01-SUMMARY.md`
</output>
