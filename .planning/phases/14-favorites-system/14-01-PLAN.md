---
phase: 14-favorites-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/1401_user_favorites.sql
  - src/lib/supabase/favorites.ts
  - src/lib/actions/favorites.ts
  - src/types/favorites.ts
autonomous: true

must_haves:
  truths:
    - "Favorites table exists with user_id, cafe_id, created_at columns"
    - "Composite unique constraint prevents duplicate favorites"
    - "RLS policies allow users to manage only their own favorites"
    - "Server Action can toggle favorite status for authenticated user"
  artifacts:
    - path: "supabase/migrations/1401_user_favorites.sql"
      provides: "Favorites table schema with RLS"
      contains: "CREATE TABLE user_favorites"
    - path: "src/lib/supabase/favorites.ts"
      provides: "Database query functions"
      exports: ["toggleFavorite", "getUserFavorites", "isCafeFavorited"]
    - path: "src/lib/actions/favorites.ts"
      provides: "Server Actions for favorites"
      exports: ["toggleFavoriteAction", "getFavoritesAction"]
    - path: "src/types/favorites.ts"
      provides: "TypeScript types"
      exports: ["UserFavorite", "FavoriteWithCafe"]
  key_links:
    - from: "src/lib/actions/favorites.ts"
      to: "src/lib/supabase/favorites.ts"
      via: "import toggleFavorite"
      pattern: "import.*from.*supabase/favorites"
    - from: "src/lib/supabase/favorites.ts"
      to: "user_favorites table"
      via: "Supabase query"
      pattern: "\\.from\\(['\"]user_favorites['\"]\\)"
---

<objective>
Create the database schema, data layer, and server actions for the favorites system.

Purpose: Establish the foundation that all favorites UI will build upon - database table with proper constraints, query functions, and authenticated server actions.
Output: Migration file, Supabase query module, Server Action module, TypeScript types
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-favorites-system/14-RESEARCH.md

# Existing patterns to follow
@src/lib/actions/ratings.ts
@src/lib/supabase/ratings.ts
@supabase/migrations/0801_cafe_ratings.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create favorites database migration</name>
  <files>supabase/migrations/1401_user_favorites.sql</files>
  <action>
Create migration file with:

1. **Table schema:**
```sql
CREATE TABLE user_favorites (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  cafe_id UUID NOT NULL REFERENCES cafes(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  CONSTRAINT user_favorites_unique UNIQUE (user_id, cafe_id)
);
```

2. **Indexes for performance:**
- `idx_user_favorites_user_id` on user_id (for fetching user's favorites)
- `idx_user_favorites_cafe_id` on cafe_id (for checking if cafe is favorited)

3. **RLS Policies:**
- Enable RLS on table
- SELECT policy: Users can view their own favorites (`auth.uid() = user_id`)
- INSERT policy: Users can insert their own favorites (`auth.uid() = user_id`)
- DELETE policy: Users can delete their own favorites (`auth.uid() = user_id`)

Follow pattern from `0801_cafe_ratings.sql` for RLS structure.
  </action>
  <verify>
Run `npx supabase db push` to apply migration. Check Supabase dashboard that:
- Table exists with correct columns
- Unique constraint on (user_id, cafe_id) exists
- RLS is enabled with 3 policies
  </verify>
  <done>Migration applied, table created with composite unique constraint and RLS policies</done>
</task>

<task type="auto">
  <name>Task 2: Create TypeScript types and Supabase queries</name>
  <files>src/types/favorites.ts, src/lib/supabase/favorites.ts</files>
  <action>
**src/types/favorites.ts:**
```typescript
export interface UserFavorite {
  id: string;
  userId: string;
  cafeId: string;
  createdAt: string;
}

export interface FavoriteWithCafe {
  id: string;
  cafeId: string;
  createdAt: string;
  cafe: {
    id: string;
    name: Record<string, string>;
    slug: string;
    address: Record<string, string>;
    districtId: string;
    primaryImageUrl: string | null;
    overallRating: number | null;
    totalRatings: number;
  };
}
```

**src/lib/supabase/favorites.ts:**

1. `toggleFavorite(supabase, userId, cafeId)`:
   - Check if favorite exists with `.single()` query
   - If exists: delete it, return `{ success: true, isFavorited: false }`
   - If not exists: insert it, return `{ success: true, isFavorited: true }`
   - Handle errors appropriately
   - Follow pattern from RESEARCH.md Pattern 2

2. `getUserFavorites(supabase, userId)`:
   - Select from user_favorites with cafe join
   - Join cafes table for: name, slug, address, district_id, overall_rating, total_ratings
   - Join cafe_images for primary image (is_primary = true)
   - Order by created_at DESC
   - Transform to FavoriteWithCafe[]

3. `isCafeFavorited(supabase, userId, cafeId)`:
   - Quick check if record exists
   - Return boolean

4. `getUserFavoriteIds(supabase, userId)`:
   - Return array of cafe IDs that user has favorited
   - Used for efficient batch checking
  </action>
  <verify>
TypeScript compilation passes: `npx tsc --noEmit`
Types are exported correctly from both files
  </verify>
  <done>Types defined, query functions implemented with proper Supabase patterns</done>
</task>

<task type="auto">
  <name>Task 3: Create Server Actions for favorites</name>
  <files>src/lib/actions/favorites.ts</files>
  <action>
Create server actions following pattern from `src/lib/actions/ratings.ts`:

1. `toggleFavoriteAction(cafeId: string)`:
   - Add 'use server' directive
   - Verify authentication with `getUser()`
   - Call `toggleFavorite()` from supabase module
   - On success, call `revalidatePath()` for:
     - `/profile/favorites`
     - `/cafes`
     - `/cafes/[slug]` (fetch slug from cafe)
     - `/map`
   - Return `{ success: boolean, isFavorited?: boolean, error?: string }`
   - Follow pattern from RESEARCH.md Pattern 4

2. `getFavoritesAction()`:
   - Verify authentication
   - Call `getUserFavorites()`
   - Return favorites array with cafe data

3. `checkFavoriteAction(cafeId: string)`:
   - Verify authentication
   - Call `isCafeFavorited()`
   - Return boolean

4. `getFavoriteIdsAction()`:
   - Verify authentication
   - Call `getUserFavoriteIds()`
   - Return array of cafe IDs (for batch checking in lists)

Include proper error handling and TypeScript return types.
  </action>
  <verify>
TypeScript compilation passes: `npx tsc --noEmit`
Server action can be imported in client component
  </verify>
  <done>Server Actions created with authentication, toggle logic, and path revalidation</done>
</task>

</tasks>

<verification>
1. Migration applied successfully to Supabase
2. TypeScript compiles without errors
3. Test toggle action manually:
   - Create test favorite via Supabase dashboard SQL
   - Verify RLS blocks other users from viewing it
   - Delete test data
</verification>

<success_criteria>
- [ ] user_favorites table exists with composite unique constraint
- [ ] RLS policies enforce user-only access
- [ ] toggleFavorite correctly adds/removes favorites
- [ ] revalidatePath called for all affected routes
- [ ] TypeScript types properly defined
</success_criteria>

<output>
After completion, create `.planning/phases/14-favorites-system/14-01-SUMMARY.md`
</output>
