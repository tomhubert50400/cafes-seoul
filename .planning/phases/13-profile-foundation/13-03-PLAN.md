---
phase: 13-profile-foundation
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - src/components/reviews/my-reviews-list.tsx
  - src/components/reviews/review-stats.tsx
  - src/app/profile/reviews/page.tsx
autonomous: false

must_haves:
  truths:
    - "User can see all cafes they've rated in the My Reviews tab"
    - "User can sort reviews by date or rating score"
    - "User can filter reviews by minimum overall score"
    - "User can see review stats (total count, average ratings)"
    - "Stats update dynamically when filters change"
  artifacts:
    - path: "src/components/reviews/my-reviews-list.tsx"
      provides: "Client component with sort/filter state and review list"
      exports: ["MyReviewsList"]
      min_lines: 100
    - path: "src/components/reviews/review-stats.tsx"
      provides: "Stats footer with totals and averages"
      exports: ["ReviewStats"]
      min_lines: 40
    - path: "src/app/profile/reviews/page.tsx"
      provides: "Server Component page with data fetching"
      min_lines: 30
  key_links:
    - from: "src/app/profile/reviews/page.tsx"
      to: "@/lib/actions/ratings"
      via: "getMyRatingsWithImages call"
      pattern: "getMyRatingsWithImages"
    - from: "src/components/reviews/my-reviews-list.tsx"
      to: "src/components/reviews/review-card.tsx"
      via: "ReviewCard component usage"
      pattern: "<ReviewCard"
    - from: "src/components/reviews/my-reviews-list.tsx"
      to: "src/components/reviews/reviews-empty-state.tsx"
      via: "ReviewsEmptyState component usage"
      pattern: "<ReviewsEmptyState"
---

<objective>
Create the MyReviewsList client component with sort/filter controls, the ReviewStats footer component, and integrate everything into the reviews page.

Purpose: Users need to view, sort, and filter their review history with real-time stats. This plan brings together all the components into a working page.

Output: Complete My Reviews tab with sort/filter functionality and stats display.
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-profile-foundation/13-CONTEXT.md
@.planning/phases/13-profile-foundation/13-RESEARCH.md
@.planning/phases/13-profile-foundation/13-01-SUMMARY.md
@.planning/phases/13-profile-foundation/13-02-SUMMARY.md
@src/types/ratings.ts
@src/components/reviews/review-card.tsx
@src/components/reviews/reviews-empty-state.tsx
@src/components/ui/select.tsx
@src/components/ui/slider.tsx
@src/app/profile/submissions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReviewStats component</name>
  <files>src/components/reviews/review-stats.tsx</files>
  <action>
Create `src/components/reviews/review-stats.tsx`:

```typescript
'use client';

import { useI18n } from '@/lib/i18n';
import type { UserRatingWithImage, OptionalRatingDimension } from '@/types/ratings';

interface ReviewStatsProps {
  /** All user reviews (unfiltered) */
  allReviews: UserRatingWithImage[];
  /** Currently displayed reviews (after filtering) */
  filteredReviews: UserRatingWithImage[];
}

export function ReviewStats({ allReviews, filteredReviews }: ReviewStatsProps) {
  const { t, language } = useI18n();

  const totalCount = allReviews.length;
  const shownCount = filteredReviews.length;

  // Calculate average overall rating from filtered reviews
  const avgOverall = shownCount > 0
    ? (filteredReviews.reduce((sum, r) => sum + r.overall, 0) / shownCount).toFixed(1)
    : '0';

  // Calculate per-dimension averages (only for dimensions that have ratings)
  const dimensions: OptionalRatingDimension[] = [
    'drinks', 'wifi', 'priceValue', 'quietness',
    'seating', 'comfort', 'food', 'lighting', 'outlets'
  ];

  const dimensionAverages = dimensions.map(dim => {
    const ratedReviews = filteredReviews.filter(r => r[dim] > 0);
    if (ratedReviews.length === 0) return null;
    const avg = ratedReviews.reduce((sum, r) => sum + r[dim], 0) / ratedReviews.length;
    return { dim, avg: avg.toFixed(1), count: ratedReviews.length };
  }).filter(Boolean) as Array<{ dim: OptionalRatingDimension; avg: string; count: number }>;

  const getDimensionLabel = (dim: OptionalRatingDimension): string => {
    const labels: Record<OptionalRatingDimension, { ko: string; en: string }> = {
      drinks: { ko: '음료', en: 'Drinks' },
      wifi: { ko: '와이파이', en: 'Wi-Fi' },
      priceValue: { ko: '가성비', en: 'Value' },
      quietness: { ko: '조용함', en: 'Quiet' },
      seating: { ko: '좌석', en: 'Seats' },
      comfort: { ko: '편안함', en: 'Comfort' },
      food: { ko: '음식', en: 'Food' },
      lighting: { ko: '조명', en: 'Light' },
      outlets: { ko: '콘센트', en: 'Outlets' },
    };
    return language === 'ko' ? labels[dim].ko : labels[dim].en;
  };

  if (totalCount === 0) return null;

  return (
    <div className="mt-8 rounded-lg bg-muted/50 p-4 text-sm text-muted-foreground">
      <div className="flex flex-wrap items-center gap-x-6 gap-y-2">
        {/* Count display */}
        <div>
          {shownCount !== totalCount ? (
            <span>
              {t('reviews.stats.shown')} <strong className="text-foreground">{shownCount}</strong> {t('reviews.stats.of')} {totalCount}
            </span>
          ) : (
            <span>
              {t('reviews.stats.total')}: <strong className="text-foreground">{totalCount}</strong>
            </span>
          )}
        </div>

        {/* Average overall */}
        <div className="border-l pl-6">
          {t('reviews.stats.avgOverall')}: <strong className="text-foreground">{avgOverall}</strong>
        </div>

        {/* Per-dimension averages (compact) */}
        {dimensionAverages.length > 0 && (
          <div className="hidden sm:flex items-center gap-3 border-l pl-6">
            {dimensionAverages.slice(0, 5).map(({ dim, avg }) => (
              <span key={dim}>
                {getDimensionLabel(dim)}: <strong className="text-foreground">{avg}</strong>
              </span>
            ))}
          </div>
        )}
      </div>
    </div>
  );
}
```

Key implementation details:
- Footer placement (subtle, not prominent header per CONTEXT.md)
- Shows "X of Y" when filtering, just total otherwise
- Average overall score calculated from filtered reviews
- Per-dimension averages shown compactly (first 5 on desktop, hidden on mobile)
- All averages update dynamically when filters change
  </action>
  <verify>
1. Run `npx tsc --noEmit` - no TypeScript errors
2. Component calculates averages correctly
3. Shows filtered count when filters active
  </verify>
  <done>
- ReviewStats component created at `src/components/reviews/review-stats.tsx`
- Shows total/filtered count
- Shows average overall rating
- Shows per-dimension averages (desktop only for space)
- Updates dynamically with filtered data
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MyReviewsList with sort/filter controls</name>
  <files>src/components/reviews/my-reviews-list.tsx</files>
  <action>
Create `src/components/reviews/my-reviews-list.tsx`:

```typescript
'use client';

import { useState, useMemo } from 'react';
import { useI18n } from '@/lib/i18n';
import { ReviewCard } from './review-card';
import { ReviewsEmptyState } from './reviews-empty-state';
import { ReviewStats } from './review-stats';
import { Slider } from '@/components/ui/slider';
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select';
import type { UserRatingWithImage } from '@/types/ratings';

type SortOption = 'rating-high' | 'rating-low' | 'date-new' | 'date-old';

interface MyReviewsListProps {
  reviews: UserRatingWithImage[];
  popularCafes?: Array<{ slug: string; name: string }>;
}

export function MyReviewsList({ reviews, popularCafes = [] }: MyReviewsListProps) {
  const { t } = useI18n();

  // Sort/filter state - default: highest rated first per CONTEXT.md
  const [sortBy, setSortBy] = useState<SortOption>('rating-high');
  const [minScore, setMinScore] = useState(1);

  // Apply sorting and filtering client-side
  const filteredReviews = useMemo(() => {
    return reviews
      .filter(r => r.overall >= minScore)
      .sort((a, b) => {
        switch (sortBy) {
          case 'rating-high':
            return b.overall - a.overall;
          case 'rating-low':
            return a.overall - b.overall;
          case 'date-new':
            return new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime();
          case 'date-old':
            return new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime();
          default:
            return 0;
        }
      });
  }, [reviews, sortBy, minScore]);

  const clearFilters = () => {
    setMinScore(1);
    setSortBy('rating-high');
  };

  const hasActiveFilter = minScore > 1;

  // No reviews at all
  if (reviews.length === 0) {
    return (
      <ReviewsEmptyState
        hasAnyReviews={false}
        popularCafes={popularCafes}
      />
    );
  }

  return (
    <div>
      {/* Sort/Filter controls */}
      <div className="mb-6 flex flex-wrap items-center gap-4">
        {/* Sort dropdown */}
        <div className="flex items-center gap-2">
          <span className="text-sm text-muted-foreground">{t('reviews.sort.label')}:</span>
          <Select value={sortBy} onValueChange={(v) => setSortBy(v as SortOption)}>
            <SelectTrigger className="w-[160px]">
              <SelectValue />
            </SelectTrigger>
            <SelectContent>
              <SelectItem value="rating-high">{t('reviews.sort.ratingHigh')}</SelectItem>
              <SelectItem value="rating-low">{t('reviews.sort.ratingLow')}</SelectItem>
              <SelectItem value="date-new">{t('reviews.sort.dateNew')}</SelectItem>
              <SelectItem value="date-old">{t('reviews.sort.dateOld')}</SelectItem>
            </SelectContent>
          </Select>
        </div>

        {/* Min score filter slider */}
        <div className="flex items-center gap-3">
          <span className="text-sm text-muted-foreground">{t('reviews.filter.minScore')}:</span>
          <Slider
            value={[minScore]}
            onValueChange={([v]) => setMinScore(v)}
            min={1}
            max={5}
            step={1}
            className="w-24"
          />
          <span className="text-sm font-medium w-4">{minScore}</span>
        </div>
      </div>

      {/* Filtered to zero - show empty state with clear option */}
      {filteredReviews.length === 0 && hasActiveFilter ? (
        <ReviewsEmptyState
          hasAnyReviews={true}
          onClearFilters={clearFilters}
        />
      ) : (
        <>
          {/* Review cards list */}
          <div className="space-y-4">
            {filteredReviews.map(review => (
              <ReviewCard key={review.id} review={review} />
            ))}
          </div>

          {/* Stats footer */}
          <ReviewStats
            allReviews={reviews}
            filteredReviews={filteredReviews}
          />
        </>
      )}
    </div>
  );
}
```

Key implementation details:
- Default sort: `rating-high` (highest rated first) per CONTEXT.md
- Sort options: rating-high, rating-low, date-new, date-old (explicit names to avoid confusion)
- Min score filter via Slider (1-5 range)
- Client-side filtering with useMemo (instant updates)
- Shows filtered-empty state when filters exclude all reviews
- Stats footer shows at bottom of list
- Reviews rendered via ReviewCard component
  </action>
  <verify>
1. Run `npx tsc --noEmit` - no TypeScript errors
2. Verify sort options work correctly
3. Verify slider filters reviews in real-time
4. Verify stats update when filters change
  </verify>
  <done>
- MyReviewsList component created at `src/components/reviews/my-reviews-list.tsx`
- Has sort dropdown with 4 options
- Has min score slider filter
- Client-side filtering with useMemo
- Integrates ReviewCard, ReviewsEmptyState, ReviewStats
- Default sort is highest rated
  </done>
</task>

<task type="auto">
  <name>Task 3: Update reviews page with data fetching</name>
  <files>src/app/profile/reviews/page.tsx</files>
  <action>
Replace the placeholder `src/app/profile/reviews/page.tsx` with the complete implementation:

```typescript
import { Metadata } from 'next';
import { cookies } from 'next/headers';
import { getMyRatingsWithImages } from '@/lib/actions/ratings';
import { MyReviewsList } from '@/components/reviews/my-reviews-list';
import { Card, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';
import { getTranslation } from '@/lib/i18n/translations';
import { LanguageCode, DEFAULT_LANGUAGE, LANGUAGE_COOKIE_NAME } from '@/lib/i18n/languages';
import { createClient } from '@/lib/supabase/server';
import { getLocalizedText } from '@/types/cafe';

async function getLanguageFromCookies(): Promise<LanguageCode> {
  const cookieStore = await cookies();
  const langCookie = cookieStore.get(LANGUAGE_COOKIE_NAME);

  if (langCookie?.value && ['en', 'ko', 'fr', 'zh', 'vi'].includes(langCookie.value)) {
    return langCookie.value as LanguageCode;
  }

  return DEFAULT_LANGUAGE;
}

export const metadata: Metadata = {
  title: 'My Reviews | Cafes Seoul',
  description: 'View your cafe reviews and ratings',
};

export default async function ReviewsPage() {
  const lang = await getLanguageFromCookies();

  // Fetch user's ratings with cafe images
  const result = await getMyRatingsWithImages();

  if (!result.success || !result.ratings) {
    return (
      <Card>
        <CardHeader>
          <CardTitle>{getTranslation(lang, 'reviews.title')}</CardTitle>
          <CardDescription>Failed to load reviews. Please try again.</CardDescription>
        </CardHeader>
      </Card>
    );
  }

  // Fetch popular cafes for empty state suggestion
  // Get top 3 cafes by total_ratings for suggestions
  const supabase = await createClient();
  const { data: popularCafesData } = await supabase
    .from('cafes')
    .select('slug, name')
    .eq('status', 'active')
    .order('total_ratings', { ascending: false })
    .limit(3);

  const popularCafes = popularCafesData?.map(cafe => ({
    slug: cafe.slug,
    name: getLocalizedText(cafe.name as Record<string, string>, lang),
  })) || [];

  return (
    <>
      <Card className="border-none shadow-none bg-transparent">
        <CardHeader className="px-0">
          <CardTitle className="text-3xl font-bold">
            {getTranslation(lang, 'reviews.title')}
          </CardTitle>
          <CardDescription className="text-lg">
            {getTranslation(lang, 'reviews.subtitle')}
          </CardDescription>
        </CardHeader>
      </Card>

      <div className="mt-4">
        <MyReviewsList
          reviews={result.ratings}
          popularCafes={popularCafes}
        />
      </div>
    </>
  );
}
```

Key implementation details:
- Server Component for initial data fetching
- Fetches ratings via `getMyRatingsWithImages()` action
- Fetches top 3 popular cafes for empty state suggestions
- Error handling for failed data fetch
- Language-aware page title and subtitle
- Passes data to MyReviewsList client component
- Follows existing page pattern from submissions page
  </action>
  <verify>
1. Run `npx tsc --noEmit` - no TypeScript errors
2. Navigate to /profile/reviews - page should load
3. If user has reviews, they should be displayed
4. If user has no reviews, empty state should show
  </verify>
  <done>
- Reviews page updated at `src/app/profile/reviews/page.tsx`
- Server-side data fetching with `getMyRatingsWithImages()`
- Popular cafes fetched for empty state
- Proper error handling
- Title and subtitle from translations
- MyReviewsList component integrated
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete My Reviews tab with rating history display, sort/filter controls, expandable cards, and stats footer</what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Log in to your account (or create test account)
3. Navigate to /profile/reviews
4. If you have rated cafes:
   - Verify list shows your rated cafes with thumbnails
   - Click a card - verify it expands to show dimension scores
   - Click "View cafe" - verify it navigates to cafe page
   - Try sort dropdown - verify sorting works (highest, lowest, newest, oldest)
   - Move the min score slider - verify filtering works
   - Check stats footer updates when filtering
5. If you have no ratings:
   - Verify encouraging empty state message
   - Verify "Explore cafes" button works
   - Verify popular cafe suggestions appear
6. Test filtering to zero results:
   - Set min score to 5 (if you have no 5-star ratings)
   - Verify "No reviews match" message with "Clear filter" button
   - Click clear filter - verify it resets

Expected: All interactions work smoothly, translations display correctly, UI matches cafe detail page styling.
  </how-to-verify>
  <resume-signal>Type "approved" if everything works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Run `npx tsc --noEmit` - should pass with no errors
2. Run dev server and test the complete flow
3. Test with reviews and without reviews
4. Test all sort options and filter slider
5. Verify stats update dynamically
6. Verify navigation to cafe pages works
</verification>

<success_criteria>
- [ ] User can see all cafes they've rated in My Reviews tab
- [ ] Each rating shows cafe name, thumbnail, date, overall score
- [ ] Clicking card expands to show all dimension scores
- [ ] "View cafe" button navigates to cafe detail page
- [ ] Sort dropdown works (highest, lowest, newest, oldest)
- [ ] Min score slider filters reviews in real-time
- [ ] Stats footer shows total count, filtered count, averages
- [ ] Empty state shows encouraging message when no reviews
- [ ] Filtered-empty shows clear filter option
- [ ] All text uses i18n translations
- [ ] No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-profile-foundation/13-03-SUMMARY.md`
</output>
