---
phase: 13-profile-foundation
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/components/reviews/review-card.tsx
  - src/components/reviews/reviews-empty-state.tsx
autonomous: true

must_haves:
  truths:
    - "ReviewCard displays cafe thumbnail, name, date, and overall score"
    - "ReviewCard expands in-place to show all dimension scores"
    - "ReviewCard has a separate 'View cafe' button for navigation"
    - "Empty state shows encouraging message when user has no reviews"
    - "Filtered-empty state shows 'clear filter' option"
  artifacts:
    - path: "src/components/reviews/review-card.tsx"
      provides: "Expandable review card component"
      exports: ["ReviewCard"]
      min_lines: 80
    - path: "src/components/reviews/reviews-empty-state.tsx"
      provides: "Empty and filtered-empty state components"
      exports: ["ReviewsEmptyState"]
      min_lines: 40
  key_links:
    - from: "src/components/reviews/review-card.tsx"
      to: "@/types/ratings"
      via: "UserRatingWithImage type import"
      pattern: "UserRatingWithImage"
    - from: "src/components/reviews/review-card.tsx"
      to: "@/lib/i18n"
      via: "useI18n hook for translations"
      pattern: "useI18n"
---

<objective>
Create the ReviewCard component with expand/collapse functionality and the empty state component for the My Reviews tab.

Purpose: Users need to see their review history in a scannable format with the ability to expand and see full rating details. Empty states guide users when they have no reviews.

Output: Two client components ready for use in the reviews list.
</objective>

<execution_context>
@C:\Users\t\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\t\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-profile-foundation/13-CONTEXT.md
@.planning/phases/13-profile-foundation/13-RESEARCH.md
@.planning/phases/13-profile-foundation/13-01-SUMMARY.md
@src/types/ratings.ts
@src/components/submissions/submission-status-card.tsx
@src/components/ui/card.tsx
@src/lib/i18n/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReviewCard component with expand/collapse</name>
  <files>src/components/reviews/review-card.tsx</files>
  <action>
Create `src/components/reviews/review-card.tsx` as a client component:

```typescript
'use client';

import { useState } from 'react';
import Link from 'next/link';
import Image from 'next/image';
import { ChevronDown, Star, ExternalLink, Coffee } from 'lucide-react';
import { Card, CardContent, CardHeader } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { cn } from '@/lib/utils';
import { useI18n } from '@/lib/i18n';
import { getLocalizedText } from '@/types/cafe';
import type { UserRatingWithImage, OptionalRatingDimension, RATING_DIMENSION_LABELS } from '@/types/ratings';

interface ReviewCardProps {
  review: UserRatingWithImage;
}

export function ReviewCard({ review }: ReviewCardProps) {
  const [expanded, setExpanded] = useState(false);
  const { language, t } = useI18n();

  const cafeName = getLocalizedText(review.cafe.name, language);
  const ratedDate = new Date(review.createdAt).toLocaleDateString(
    language === 'ko' ? 'ko-KR' : language === 'zh' ? 'zh-CN' : language === 'vi' ? 'vi-VN' : language === 'fr' ? 'fr-FR' : 'en-US',
    { year: 'numeric', month: 'short', day: 'numeric' }
  );

  // Get all rated dimensions (non-zero values)
  const dimensions: OptionalRatingDimension[] = [
    'drinks', 'wifi', 'priceValue', 'quietness',
    'seating', 'comfort', 'food', 'lighting', 'outlets'
  ];
  const ratedDimensions = dimensions.filter(dim => review[dim] > 0);

  return (
    <Card className="transition-shadow hover:shadow-md">
      <CardHeader
        className="cursor-pointer pb-3"
        onClick={() => setExpanded(!expanded)}
      >
        <div className="flex items-start gap-4">
          {/* Cafe thumbnail */}
          <div className="relative h-16 w-16 shrink-0 overflow-hidden rounded-lg bg-muted">
            {review.cafe.primaryImageUrl ? (
              <Image
                src={review.cafe.primaryImageUrl}
                alt={cafeName}
                fill
                className="object-cover"
                sizes="64px"
              />
            ) : (
              <div className="flex h-full w-full items-center justify-center">
                <Coffee className="h-6 w-6 text-muted-foreground" />
              </div>
            )}
          </div>

          {/* Card info */}
          <div className="flex-1 min-w-0">
            <h3 className="font-semibold text-lg truncate">{cafeName}</h3>
            <p className="text-sm text-muted-foreground">
              {t('reviews.card.ratedOn')}: {ratedDate}
            </p>
          </div>

          {/* Overall score and expand icon */}
          <div className="flex items-center gap-3 shrink-0">
            <div className="flex items-center gap-1.5 rounded-full bg-primary/10 px-3 py-1.5">
              <Star className="h-4 w-4 fill-primary text-primary" />
              <span className="font-semibold text-primary">{review.overall}</span>
            </div>
            <ChevronDown
              className={cn(
                "h-5 w-5 text-muted-foreground transition-transform duration-200",
                expanded && "rotate-180"
              )}
            />
          </div>
        </div>
      </CardHeader>

      {/* Expanded content */}
      <div
        className={cn(
          "overflow-hidden transition-all duration-200",
          expanded ? "max-h-[500px] opacity-100" : "max-h-0 opacity-0"
        )}
      >
        <CardContent className="pt-0 border-t">
          {/* Dimension scores */}
          {ratedDimensions.length > 0 && (
            <div className="mt-4">
              <h4 className="text-sm font-medium text-muted-foreground mb-3">
                {t('reviews.card.dimensions')}
              </h4>
              <div className="grid grid-cols-2 sm:grid-cols-3 gap-3">
                {ratedDimensions.map(dim => (
                  <div
                    key={dim}
                    className="flex items-center justify-between rounded-md bg-muted/50 px-3 py-2"
                  >
                    <span className="text-sm capitalize">
                      {getDimensionLabel(dim, language)}
                    </span>
                    <div className="flex items-center gap-1">
                      <Star className="h-3.5 w-3.5 fill-amber-400 text-amber-400" />
                      <span className="text-sm font-medium">{review[dim]}</span>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          )}

          {/* Pet friendly indicator */}
          {review.petFriendly && (
            <div className="mt-3 text-sm text-muted-foreground">
              Pet-friendly
            </div>
          )}

          {/* View cafe button */}
          <div className="mt-4 pt-3 border-t">
            <Button variant="outline" size="sm" asChild>
              <Link href={`/cafes/${review.cafe.slug}`}>
                {t('reviews.card.viewCafe')}
                <ExternalLink className="ml-1.5 h-3.5 w-3.5" />
              </Link>
            </Button>
          </div>
        </CardContent>
      </div>
    </Card>
  );
}

// Helper to get dimension label in current language
function getDimensionLabel(dim: OptionalRatingDimension, language: string): string {
  const labels: Record<OptionalRatingDimension, { ko: string; en: string }> = {
    drinks: { ko: '음료', en: 'Drinks' },
    wifi: { ko: '와이파이', en: 'Wi-Fi' },
    priceValue: { ko: '가성비', en: 'Value' },
    quietness: { ko: '조용함', en: 'Quietness' },
    seating: { ko: '좌석', en: 'Seating' },
    comfort: { ko: '편안함', en: 'Comfort' },
    food: { ko: '음식', en: 'Food' },
    lighting: { ko: '조명', en: 'Lighting' },
    outlets: { ko: '콘센트', en: 'Outlets' },
  };
  return language === 'ko' ? labels[dim].ko : labels[dim].en;
}
```

Key implementation details:
- Click anywhere on the header to expand/collapse
- Thumbnail uses Next.js Image with fallback Coffee icon
- Overall score prominently displayed with star icon
- Chevron rotates 180deg when expanded
- Dimension scores shown in a grid when expanded
- "View cafe" button is separate from expand trigger (in expanded content)
- Uses existing UI components (Card, Button) and patterns (useI18n, cn)
- Smooth max-height transition for expand animation
  </action>
  <verify>
1. Run `npx tsc --noEmit` - no TypeScript errors
2. Check component renders with mock data
3. Verify clicking header expands/collapses the card
4. Verify "View cafe" link navigates to `/cafes/{slug}`
  </verify>
  <done>
- ReviewCard component created at `src/components/reviews/review-card.tsx`
- Displays thumbnail, cafe name, rating date, overall score
- Expands to show dimension scores
- Has "View cafe" button in expanded state
- Uses proper i18n for labels
- Follows existing component patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: Create empty state components</name>
  <files>src/components/reviews/reviews-empty-state.tsx</files>
  <action>
Create `src/components/reviews/reviews-empty-state.tsx`:

```typescript
'use client';

import Link from 'next/link';
import { Coffee, Star, MapPin, Filter } from 'lucide-react';
import { Card, CardContent } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { useI18n } from '@/lib/i18n';

interface ReviewsEmptyStateProps {
  /** Whether user has ANY reviews (vs just filtered to zero) */
  hasAnyReviews: boolean;
  /** Callback to clear active filters */
  onClearFilters?: () => void;
  /** Popular cafes to suggest (for no-reviews state) */
  popularCafes?: Array<{
    slug: string;
    name: string;
  }>;
}

export function ReviewsEmptyState({
  hasAnyReviews,
  onClearFilters,
  popularCafes = [],
}: ReviewsEmptyStateProps) {
  const { t } = useI18n();

  // Filtered to zero - show clear filter option
  if (hasAnyReviews) {
    return (
      <Card className="mx-auto max-w-md">
        <CardContent className="py-12 text-center">
          <div className="mx-auto mb-4 flex h-16 w-16 items-center justify-center rounded-full bg-muted">
            <Filter className="h-8 w-8 text-muted-foreground" />
          </div>
          <h3 className="text-lg font-semibold mb-2">
            {t('reviews.emptyFiltered.title')}
          </h3>
          <p className="text-muted-foreground mb-6">
            {t('reviews.emptyFiltered.message')}
          </p>
          <Button variant="outline" onClick={onClearFilters}>
            {t('reviews.emptyFiltered.clearFilter')}
          </Button>
        </CardContent>
      </Card>
    );
  }

  // No reviews at all - encouraging message + suggestions
  return (
    <Card className="mx-auto max-w-lg">
      <CardContent className="py-12 text-center">
        {/* Illustration using icons */}
        <div className="mx-auto mb-6 flex items-center justify-center gap-2">
          <div className="flex h-12 w-12 items-center justify-center rounded-full bg-amber-50">
            <Coffee className="h-6 w-6 text-amber-600" />
          </div>
          <div className="flex h-14 w-14 items-center justify-center rounded-full bg-primary/10">
            <Star className="h-7 w-7 text-primary" />
          </div>
          <div className="flex h-12 w-12 items-center justify-center rounded-full bg-green-50">
            <MapPin className="h-6 w-6 text-green-600" />
          </div>
        </div>

        <h3 className="text-xl font-semibold mb-2">
          {t('reviews.emptyState.title')}
        </h3>
        <p className="text-muted-foreground mb-6 max-w-sm mx-auto">
          {t('reviews.emptyState.message')}
        </p>

        <Button asChild>
          <Link href="/cafes">
            {t('reviews.emptyState.explore')}
          </Link>
        </Button>

        {/* Popular cafes suggestions */}
        {popularCafes.length > 0 && (
          <div className="mt-8 pt-6 border-t">
            <p className="text-sm text-muted-foreground mb-3">
              {t('reviews.popularCafes')}
            </p>
            <div className="flex flex-wrap justify-center gap-2">
              {popularCafes.slice(0, 3).map(cafe => (
                <Button key={cafe.slug} variant="ghost" size="sm" asChild>
                  <Link href={`/cafes/${cafe.slug}`}>
                    {cafe.name}
                  </Link>
                </Button>
              ))}
            </div>
          </div>
        )}
      </CardContent>
    </Card>
  );
}
```

Key implementation details:
- Two distinct states: no reviews vs filtered to zero
- No-reviews: encouraging icons (Coffee, Star, MapPin), message, "Explore cafes" button
- Filtered-empty: Filter icon, message, "Clear filter" button with callback
- Optional popular cafes shown as ghost buttons linking to cafe pages
- Uses existing Card, Button components
- All text via i18n translations
  </action>
  <verify>
1. Run `npx tsc --noEmit` - no TypeScript errors
2. Check both states render correctly (hasAnyReviews true vs false)
3. Verify clear filter button calls onClearFilters callback
4. Verify explore button links to /cafes
  </verify>
  <done>
- ReviewsEmptyState component created at `src/components/reviews/reviews-empty-state.tsx`
- Handles no-reviews state with encouraging message and explore button
- Handles filtered-empty state with clear filter option
- Shows optional popular cafe suggestions
- Uses proper i18n translations
- Follows existing component patterns
  </done>
</task>

</tasks>

<verification>
1. Run `npx tsc --noEmit` - should pass with no errors
2. Both components should be importable: `import { ReviewCard } from '@/components/reviews/review-card'`
3. Both components should be importable: `import { ReviewsEmptyState } from '@/components/reviews/reviews-empty-state'`
4. Components should follow existing patterns (useI18n, cn, Card/Button usage)
</verification>

<success_criteria>
- [ ] ReviewCard component displays thumbnail, name, date, overall score
- [ ] ReviewCard expands to show dimension scores on click
- [ ] ReviewCard has "View cafe" button linking to cafe page
- [ ] ReviewsEmptyState shows encouraging message when no reviews
- [ ] ReviewsEmptyState shows clear filter option when filtered to zero
- [ ] Both components use i18n translations
- [ ] No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-profile-foundation/13-02-SUMMARY.md`
</output>
