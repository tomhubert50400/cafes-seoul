---
phase: 01-auth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/client.ts
  - package.json
  - .env.example
autonomous: true

must_haves:
  truths:
    - "Middleware refreshes expired tokens on every request via getUser()"
    - "Server client creates valid Supabase instance with cookie handling"
    - "Browser client is a singleton that persists across renders"
    - "Package versions are current (no security vulnerabilities)"
    - "Required env vars are documented in .env.example"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Request interception for session refresh"
      contains: "updateSession"
    - path: "src/lib/supabase/middleware.ts"
      provides: "Session refresh logic with getUser() call"
      contains: "getUser()"
    - path: "src/lib/supabase/server.ts"
      provides: "Server-side Supabase client factory"
      exports: ["createClient"]
    - path: "src/lib/supabase/client.ts"
      provides: "Browser-side Supabase client singleton"
      contains: "let browserClient"
    - path: ".env.example"
      provides: "Environment variable documentation"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/supabase/middleware.ts"
      via: "import updateSession"
      pattern: "import.*updateSession.*middleware"
    - from: "src/lib/supabase/middleware.ts"
      to: "supabase.auth.getUser()"
      via: "token refresh trigger"
      pattern: "supabase\\.auth\\.getUser\\(\\)"
---

<objective>
Audit and finalize the existing Supabase Auth infrastructure for Phase 1 foundation.

Purpose: Verify the middleware, server client, and browser client are correctly configured before building auth features in Phase 2. Fix the browser client singleton pattern and update packages.

Output: Verified auth infrastructure with singleton browser client, updated packages, and documented env vars.
</objective>

<execution_context>
@C:\Users\t\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\t\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing auth infrastructure (read before modifying)
@src/middleware.ts
@src/lib/supabase/middleware.ts
@src/lib/supabase/server.ts
@src/lib/supabase/client.ts
@package.json
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing auth infrastructure and fix browser client singleton</name>
  <files>src/lib/supabase/client.ts</files>
  <action>
    AUDIT PHASE (read and verify):
    1. Read src/middleware.ts - verify it imports and calls updateSession()
    2. Read src/lib/supabase/middleware.ts - verify it:
       - Uses createServerClient from @supabase/ssr
       - Has proper cookie getAll/setAll handlers
       - Calls supabase.auth.getUser() AFTER createServerClient (CRITICAL for token refresh)
       - Does NOT call getSession() (security issue per Supabase docs)
    3. Read src/lib/supabase/server.ts - verify it has async createClient with cookie handling

    FIX PHASE:
    4. Update src/lib/supabase/client.ts to use singleton pattern:
       - Store client in module-level variable
       - Only create new client if none exists
       - This prevents memory leaks from creating clients on every render

    Pattern to implement:
    ```typescript
    import { createBrowserClient } from '@supabase/ssr';
    import type { SupabaseClient } from '@supabase/supabase-js';

    let browserClient: SupabaseClient | null = null;

    export function createClient() {
      if (browserClient) return browserClient;

      browserClient = createBrowserClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
      );

      return browserClient;
    }
    ```
  </action>
  <verify>
    - grep "getUser()" src/lib/supabase/middleware.ts returns match (confirms token refresh)
    - grep "let browserClient" src/lib/supabase/client.ts returns match (confirms singleton)
    - npm run build succeeds with no TypeScript errors
  </verify>
  <done>
    - Middleware calls getUser() for token refresh (verified)
    - Server client has proper cookie handling (verified)
    - Browser client uses singleton pattern (fixed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Supabase packages and document env vars</name>
  <files>package.json, .env.example</files>
  <action>
    PACKAGE UPDATE:
    1. Run: npm install @supabase/supabase-js@latest @supabase/ssr@latest
    2. Verify no breaking changes (both are minor updates)

    ENV VAR DOCUMENTATION:
    3. Update .env.example to clearly document auth-related env vars:
       - NEXT_PUBLIC_SUPABASE_URL (required)
       - NEXT_PUBLIC_SUPABASE_ANON_KEY (required)
       - NEXT_PUBLIC_APP_URL (required for OAuth callbacks)

    4. Add validation comment in .env.example noting these are required for auth

    Note: Do NOT add runtime env validation in this phase - that adds complexity.
    Just ensure .env.example clearly documents what's needed.
  </action>
  <verify>
    - npm ls @supabase/supabase-js shows latest version (2.93.x or higher)
    - npm ls @supabase/ssr shows latest version
    - .env.example contains NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY
    - npm run build succeeds
  </verify>
  <done>
    - Supabase packages updated to latest
    - Environment variables documented in .env.example
    - Build passes with updated packages
  </done>
</task>

<task type="checkpoint:verification">
  <name>Task 3: Verify complete auth infrastructure with test build</name>
  <files>None (verification only)</files>
  <action>
    Final verification of entire auth foundation:

    1. Run npm run build - must succeed
    2. Run npm run dev and verify:
       - App starts without errors
       - No console errors about missing env vars
       - Middleware runs (check terminal for request logs)

    3. Verify all must_haves are satisfied:
       - Middleware refreshes tokens (getUser call exists)
       - Server client exists with cookie handling
       - Browser client is singleton
       - Packages are current
       - Env vars are documented

    If any issues found, fix them before completing this task.
  </action>
  <verify>
    - npm run build exits with code 0
    - npm run dev starts without errors
    - All must_haves from frontmatter are satisfied
  </verify>
  <done>
    - Full build passes
    - Dev server starts cleanly
    - Auth infrastructure verified and ready for Phase 2
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Grep verification:
   - `grep -n "getUser()" src/lib/supabase/middleware.ts` - must show getUser call
   - `grep -n "let browserClient" src/lib/supabase/client.ts` - must show singleton variable

2. Package verification:
   - `npm ls @supabase/supabase-js` - must show 2.93.x or higher
   - `npm ls @supabase/ssr` - must show latest

3. Build verification:
   - `npm run build` - must succeed with exit code 0

4. Runtime verification:
   - `npm run dev` - must start without errors
</verification>

<success_criteria>
Phase 1 is complete when:
- [ ] Middleware correctly calls getUser() to refresh tokens (CRITICAL)
- [ ] Server client factory exists at src/lib/supabase/server.ts
- [ ] Browser client uses singleton pattern at src/lib/supabase/client.ts
- [ ] @supabase/supabase-js and @supabase/ssr are latest versions
- [ ] .env.example documents required Supabase env vars
- [ ] npm run build succeeds
- [ ] npm run dev starts without auth-related errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-foundation/01-01-SUMMARY.md`
</output>
