---
phase: 07-cafe-submissions
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/profile/submissions/[id]/edit/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can navigate to edit page for pending submissions"
    - "Edit form is pre-populated with existing submission data"
    - "Save updates the submission correctly"
  artifacts:
    - path: "src/app/profile/submissions/[id]/edit/page.tsx"
      provides: "Edit submission page with Server Component data fetching"
      exports: ["default"]
  key_links:
    - from: "src/components/submissions/submission-status-card.tsx"
      to: "src/app/profile/submissions/[id]/edit/page.tsx"
      via: "Next.js routing with dynamic segment [id]"
    - from: "src/app/profile/submissions/[id]/edit/page.tsx"
      to: "src/components/submissions/cafe-submission-form.tsx"
      via: "Props: mode='edit', submission, initialData"
---

<objective>
Create the edit submission page at `/profile/submissions/[id]/edit` that allows users to edit their pending cafe submissions.

Purpose: Complete the edit flow that currently has a link but no destination page, enabling users to modify their submissions before they are approved.
Output: A working edit page that fetches existing submission data and pre-populates the CafeSubmissionForm component.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Existing Components to Reuse

The following already exist and must be reused:
- `CafeSubmissionForm` component supports `mode="edit"` prop
- `updateSubmission` Server Action handles the update logic
- `submissionSchema` validates form data
- TypeScript types: `CafeSubmission`, `CafeSubmissionInput`

## Link Source

`src/components/submissions/submission-status-card.tsx` contains:
```tsx
<Link href={`/profile/submissions/${submission.id}/edit`}>
  <Pencil className="mr-1.5 h-3.5 w-3.5" />
  Edit
</Link>
```
</context>

<tasks>

<task type="auto">
  <name>Create Edit Submission Page</name>
  <files>src/app/profile/submissions/[id]/edit/page.tsx</files>
  <action>
    Create the dynamic route page at `src/app/profile/submissions/[id]/edit/page.tsx`:

    1. **Route Parameters**: Accept `{ params: { id: string } }` as props

    2. **Data Fetching** (Server Component):
       - Import and call `getSubmissionById(id)` or equivalent function from `src/lib/supabase/submissions.ts`
       - If submission not found, call `notFound()` from next/navigation
       - Verify submission status is "pending" - if not pending, redirect to `/profile/submissions` with appropriate message
       - Ensure the current user owns the submission (check user_id matches)

    3. **Data Transformation**:
       - Transform the `CafeSubmission` database record into the form shape expected by `CafeSubmissionForm`
       - Map fields: name -> name, address -> address, neighborhood_id -> neighborhoodId, instagram_url -> instagramUrl, etc.

    4. **Page Layout**:
       - Wrap in standard profile layout structure (look at other profile pages for patterns)
       - Add page header: "Edit Submission" with back button to `/profile/submissions`
       - Show submission status badge

    5. **Form Integration**:
       - Import `CafeSubmissionForm` from `src/components/submissions/cafe-submission-form`
       - Render with `mode="edit"`, `submission={submission}`, and `initialData={transformedData}`

    6. **Metadata**: Add proper metadata with title "Edit Submission | Seoul Cafés"

    7. **Error Handling**:
       - Handle case where user is not authenticated (redirect to login)
       - Handle case where user doesn't own the submission (show 403 or redirect)
  </action>
  <verify>
    1. Build passes: `npm run build` completes without errors
    2. Type check passes: `npx tsc --noEmit` shows no type errors
    3. Page file exists at correct path with proper exports
  </verify>
  <done>
    - Page exists at `src/app/profile/submissions/[id]/edit/page.tsx`
    - Page fetches submission by ID using Server Component
    - Page pre-populates CafeSubmissionForm with existing data
    - Page handles not-found and unauthorized cases correctly
    - Edit link in submission-status-card now navigates to working page
  </done>
</task>

</tasks>

<verification>
### Gap Closure Verification

1. **Truth Verification** - "User can edit pending submissions":
   - Navigate to `/profile/submissions` as a user with pending submissions
   - Click "Edit" button on a pending submission card
   - Verify navigation to `/profile/submissions/[id]/edit` succeeds
   - Verify form shows pre-populated data matching the submission
   - Make a change and save - verify submission updates

2. **Integration Verification**:
   - Verify edit link from `submission-status-card.tsx` resolves correctly
   - Verify form submission calls `updateSubmission` action
   - Verify updated data reflects in submission list

3. **Edge Cases**:
   - Non-pending submissions should redirect or show error
   - Other users' submissions should show unauthorized
   - Non-existent submission IDs should show 404
</verification>

<success_criteria>
- Edit page renders correctly at `/profile/submissions/[id]/edit`
- Form is pre-populated with existing submission data
- Save operation updates the submission
- Non-pending submissions cannot be edited
- Users can only edit their own submissions
</success_criteria>

<output>
After completion, create `.planning/phases/07-cafe-submissions/07-05-SUMMARY.md`
</output>
